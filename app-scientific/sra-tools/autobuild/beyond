abinfo "Moving files ..."
# FIXME: sra-tools install some files into /usr/jar/
# https://github.com/ncbi/sra-tools/blob/3.3.0/ngs/ngs-java/CMakeLists.txt#L117
mkdir -pv "$PKGDIR"/usr/share/java/
mv -v "$PKGDIR"/usr/jar "$PKGDIR"/usr/share/java/
mv -v "$PKGDIR"/usr/li "$PKGDIR"/usr/lib/
