abinfo "Configuring polymake ..."
"$SRCDIR"/configure \
    --prefix=/usr \
    "${AUTOTOOLS_AFTER[@]}"

abinfo "Building polymake ..."
make

abinfo "Installing polymake ..."
make install \
    DESTDIR="$PKGDIR"
