abinfo "Patching deno ..."
ab_apply_patches "$SRCDIR"/autobuild/patches/*.patch

# Note: Build V8 from source as the upstream does not provide enough
# binaries for the architectures we support.
abinfo "Patching rusty_v8 ..."
pushd "$SRCDIR"/../rusty_v8
ab_apply_patches "$SRCDIR"/autobuild/patches/*.deferred
popd

# FIXME: Fix binding generation with newer LLVM.
abinfo "Patching libuv-sys-lite ..."
pushd "$SRCDIR"/../libuv-sys-lite
ab_apply_patches "$SRCDIR"/autobuild/patches/*.libuv
popd
