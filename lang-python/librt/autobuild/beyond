abinfo 'Running librt smoke tests ...'

_platlib="$(python3 -c \
    'import sysconfig; print(sysconfig.get_path("platlib"))')"

PYTHONPATH="${PKGDIR}${_platlib}" \
    python3 -m pytest -q --import-mode=importlib \
    ./librt_src/smoke_tests.py \
    || abdie 'librt smoke tests failed.'
