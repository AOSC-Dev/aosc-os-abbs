diff '--color=auto' -rpu PyQt5-5.15.11.orig/qpy/QtCore/qpycore_qstring.cpp PyQt5-5.15.11/qpy/QtCore/qpycore_qstring.cpp
--- PyQt5-5.15.11.orig/qpy/QtCore/qpycore_qstring.cpp	2024-07-13 17:45:29.058345800 +0800
+++ PyQt5-5.15.11/qpy/QtCore/qpycore_qstring.cpp	2025-05-24 21:41:04.988588236 +0800
@@ -58,8 +58,11 @@ PyObject *qpycore_PyObject_FromQString(c
     int kind;
     void *data;
 
-    if ((obj = sipUnicodeNew(qt_len, 0x007f, &kind, &data)) == NULL)
-        return NULL;
+    if ((obj = sipUnicodeNew(qt_len, 0x007f, &kind, &data)) == NULL) {
+      Py_FatalError("PyQt5.QtCore: qpycore_PyObject_FromQString string "
+                    "conversion failure (path 1)");
+      return NULL;
+    }
 
     const QChar *qch = qstr.constData();
 
@@ -103,8 +106,11 @@ PyObject *qpycore_PyObject_FromQString(c
             }
 
             // Create the correctly sized object.
-            if ((obj = sipUnicodeNew(py_len, maxchar, &kind, &data)) == NULL)
+            if ((obj = sipUnicodeNew(py_len, maxchar, &kind, &data)) == NULL) {
+                Py_FatalError("PyQt5.QtCore: qpycore_PyObject_FromQString string "
+                              "conversion failure (path 2)");
                 return NULL;
+            }
 
             qch = qstr.constData();
 
@@ -139,6 +145,7 @@ PyObject *qpycore_PyObject_FromQString(c
         sipUnicodeWrite(kind, data, qt_i, uch);
     }
 #elif defined(Py_UNICODE_WIDE)
+Py_FatalError("PyQt5.QtCore: qpycore_PyObject_FromQString unexpected path 1");
     QVector<uint> ucs4 = qstr.toUcs4();
 
     if ((obj = PyUnicode_FromUnicode(NULL, ucs4.size())) == NULL)
@@ -147,6 +154,7 @@ PyObject *qpycore_PyObject_FromQString(c
     memcpy(PyUnicode_AS_UNICODE(obj), ucs4.constData(),
             ucs4.size() * sizeof (Py_UNICODE));
 #else
+    Py_FatalError("PyQt5.QtCore: qpycore_PyObject_FromQString unexpected path 2");
     if ((obj = PyUnicode_FromUnicode(NULL, qstr.length())) == NULL)
         return NULL;
 
@@ -162,6 +170,7 @@ PyObject *qpycore_PyObject_FromQString(c
 QString qpycore_PyObject_AsQString(PyObject *obj)
 {
 #if PY_MAJOR_VERSION < 3
+    Py_FatalError("PyQt5.QtCore: qpycore_PyObject_AsQString unexpected path 3");
     if (PyString_Check(obj))
     {
         const char *obj_s = PyString_AsString(obj);
@@ -174,8 +183,8 @@ QString qpycore_PyObject_AsQString(PyObj
 #endif
 
 #if defined(PYQT_PEP_393)
-    int char_size;
-    Py_ssize_t len;
+    int char_size = 0;
+    Py_ssize_t len = -1;
     void *data = sipUnicodeData(obj, &char_size, &len);
 
     if (char_size == 1)
@@ -188,11 +197,14 @@ QString qpycore_PyObject_AsQString(PyObj
     if (char_size == 4)
         return QString::fromUcs4(reinterpret_cast<uint *>(data), len);
 
+    Py_FatalError("PyQt5.QtCore: qpycore_PyObject_AsQString string conversion failure");
     return QString();
 #elif defined(Py_UNICODE_WIDE)
+    Py_FatalError("PyQt5.QtCore: qpycore_PyObject_AsQString unexpected path 1");
     return QString::fromUcs4((const uint *)PyUnicode_AS_UNICODE(obj),
             PyUnicode_GET_SIZE(obj));
 #else
+    Py_FatalError("PyQt5.QtCore: qpycore_PyObject_AsQString unexpected path 2");
     return QString::fromUtf16((const ushort *)PyUnicode_AS_UNICODE(obj),
             PyUnicode_GET_SIZE(obj));
 #endif
diff '--color=auto' -rpu PyQt5-5.15.11.orig/qpy/QtCore/qpycore_qt_conf.cpp PyQt5-5.15.11/qpy/QtCore/qpycore_qt_conf.cpp
--- PyQt5-5.15.11.orig/qpy/QtCore/qpycore_qt_conf.cpp	2024-07-13 17:45:29.044480600 +0800
+++ PyQt5-5.15.11/qpy/QtCore/qpycore_qt_conf.cpp	2025-05-24 20:34:56.742084781 +0800
@@ -72,20 +72,26 @@ bool qpycore_qt_conf()
     // Get PyQt5.__file__.
     PyObject *pyqt5 = PyImport_ImportModule("PyQt5");
 
-    if (!pyqt5)
+    if (!pyqt5) {
+        Py_FatalError("PyQt5.QtCore: qpycore_qt_conf: PyQt5 module not found");
         return false;
+    }
 
     PyObject *init = PyObject_GetAttrString(pyqt5, "__file__");
     Py_DECREF(pyqt5);
 
-    if (!init)
-        return false;
+    if (!init) {
+      Py_FatalError("PyQt5.QtCore: qpycore_qt_conf: PyQt5.__file__ found");
+      return false;
+    }
 
     QString init_impl(qpycore_PyObject_AsQString(init));
     Py_DECREF(init);
 
-    if (init_impl.isEmpty())
-        return false;
+    if (init_impl.isEmpty()) {
+      Py_FatalError("PyQt5.QtCore: qpycore_qt_conf: PyQt5.__file__ to QString failure");
+      return false;
+    }
 
     // Get the directory containing the PyQt5 extension modules.
     QDir pyqt5_dir = QFileInfo(QDir::fromNativeSeparators(init_impl)).absoluteDir();
