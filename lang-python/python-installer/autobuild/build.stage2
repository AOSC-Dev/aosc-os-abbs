abinfo "Bootstrapping python-installer ..."
python3 -m flit_core.wheel

abinfo "Installing bootstrapped python-installer ..."
# Using a subshell to isolate the environment.
(
	PYTHONPATH="$SRCDIR"/src \
	python3 -m installer \
		--destdir "$PKGDIR" \
		"$SRCDIR"/dist/*.whl
)
