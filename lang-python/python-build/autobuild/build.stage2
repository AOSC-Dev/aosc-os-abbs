abinfo "Bootstrapping python-build ..."
(
	PYTHONPATH="$SRCDIR"/src \
		python3 -m build \
			--no-isolation \
			--wheel \
			--outdir "$SRCDIR"/dist
)

abinfo "Installing bootstrapped python-build ..."
python3 -m installer \
	--destdir "$PKGDIR" \
	"$SRCDIR"/dist/*.whl
