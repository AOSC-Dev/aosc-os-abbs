[ -x /usr/bin/systemd-tmpfiles ] && systemd-tmpfiles --create sudo.conf

echo -e "\
Please note that the user group that allows sudo-ing is \033[31m'wheel'\033[0m
instead of the old \033[31m'users'\033[0m group.

If you are still using the 'aosc' user that was pre-configured
in a distribution tarball, you will need to change settings of
your current user in order to keep using 'sudo'."
