mkdir -p build
cd build
../configure --prefix=/opt/abcross/arm64 --target=aarch64-unknown-linux-gnu \
             --with-sysroot=/var/ab/cross-root/arm64 --enable-shared --disable-multilib --with-arch=armv8-a
make configure-host
make 
make DESTDIR=$PKGDIR install
