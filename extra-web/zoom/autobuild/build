abinfo "Installing files..."
mkdir -pv "$PKGDIR"/usr/share/zoom
cp -rv "$SRCDIR"/zoom "$PKGDIR"/usr/share/

abinfo "Linking executables..."
mkdir -pv "$PKGDIR"/usr/bin
pushd "$PKGDIR"/usr/bin
ln -sv ../share/zoom/ZoomLauncher zoom
popd
