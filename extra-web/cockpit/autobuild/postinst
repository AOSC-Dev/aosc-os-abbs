systemd-sysusers cockpit-ws.sysuser.conf
systemd-sysusers cockpit-wsinstance.sysuser.conf
systemd-tmpfiles --create cockpit-tempfiles.conf

systemctl enable cockpit.socket

cat << EOF
If you want to use cockpit immediately, perform the following steps:

1. Use root permission run:

    systemctl start cockpit.socket

2. Use browser open url:

    http://YOUR-IP:9090

EOF
