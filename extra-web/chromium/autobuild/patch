wget -c https://github.com/foutrelis/chromium-launcher/archive/v2.tar.gz
tar xf v2.tar.gz

patch -p1 -i autobuild/patches/0001-NSS-reject-DH-groups-smaller-than-1024-bits.patch
patch -p1 -i autobuild/patches/0001-Use-the-correct-URL-for-ERR_SSL_WEAK_SERVER_EPHEMERA.patch
patch -p1 -i autobuild/patches/widevine.patch

patch -p1 -d third_party/boringssl/src < autobuild/patches/Demand-for-newer-POSIX-macro.patch
