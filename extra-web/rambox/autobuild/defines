PKGNAME=rambox
PKGSEC=web
PKGDEP="alsa-lib gconf gtk-2 libnotify nss"
PKGDES="Free and Open Source messaging and emailing app that combines common Web applications into one"

FAIL_ARCH="!(amd64)"
ABSTRIP=0
