if [[ "${CROSS:-$ARCH}" != "ppc64" && "${CROSS:-$ARCH}" != "powerpc" ]]; then
    VER=50.0
else
    VER=45.5.0esr
fi
SRCTBL="http://download-origin.cdn.mozilla.net/pub/firefox/releases/$VER/source/firefox-$VER.source.tar.xz"
