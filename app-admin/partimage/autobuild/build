abinfo "Configure partimage..."
"$SRCDIR"/configure \
    ${AUTOTOOLS_DEF[@]} \
    ${AUTOTOOLS_AFTER[@]}

abinfo "Start make..."
make
make pamfile

abinfo "Install partimage..."
make install DESTDIR="$PKGDIR"
