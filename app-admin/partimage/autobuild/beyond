abinfo "Install partimage pam..."
install -Dvm0644 "$SRCDIR"/partimaged.pam "$PKGDIR"/etc/pam.d/partimaged

abinfo "Modify partimage permissions..."
chmod -v 644 "$PKGDIR"/etc/partimaged/partimagedusers
