diff -ur a/src/client/imagefile.cpp b/src/client/imagefile.cpp
--- a/src/client/imagefile.cpp	2010-07-25 23:30:31.000000000 +0800
+++ b/src/client/imagefile.cpp	2025-08-21 05:45:52.749853035 +0800
@@ -783,7 +783,7 @@
   else if (m_options.dwCompression == COMPRESS_GZIP) // Gzip compression
     {
       showDebug(1, "open gzip\n");
-      m_gzImageFile = (gzFile *) gzdopen(m_nFdImage, "wb"); //"wb1h");
+      m_gzImageFile = gzdopen(m_nFdImage, "wb"); //"wb1h");
       if (m_gzImageFile == NULL)
 	{
 	  showDebug(1, "error:%d %s\n", errno, strerror(errno));
@@ -1098,7 +1098,7 @@
     }
   else if (m_options.dwCompression == COMPRESS_GZIP) // Gzip compression
     {
-      m_gzImageFile = (gzFile *) gzdopen(m_nFdImage, "rb");
+      m_gzImageFile = gzdopen(m_nFdImage, "rb");
       if (m_gzImageFile == NULL)
         THROW(ERR_ERRNO, errno);
       else
diff -ur a/src/client/imagefile.h b/src/client/imagefile.h
--- a/src/client/imagefile.h	2010-07-25 23:30:31.000000000 +0800
+++ b/src/client/imagefile.h	2025-08-21 05:50:58.515368285 +0800
@@ -41,7 +41,7 @@
   COptions m_options;
 
   FILE *m_fImageFile;
-  gzFile *m_gzImageFile;
+  gzFile m_gzImageFile;
   BZFILE *m_bzImageFile;
 
   int m_nFdImage;
