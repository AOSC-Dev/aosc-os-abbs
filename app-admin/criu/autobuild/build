abinfo "Building criu ..."
make \
    DESTDIR="$PKGDIR" \
    PREFIX=/usr \
    LIBDIR=/usr/lib \
    SBINDIR=/usr/bin \
    RUNDIR=/run/criu

abinfo "Installing criu ..."
make install \
    DESTDIR="$PKGDIR" \
    PREFIX=/usr \
    LIBDIR=/usr/lib \
    SBINDIR=/usr/bin \
    RUNDIR=/run/criu
