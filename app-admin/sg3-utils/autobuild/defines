PKGNAME=sg3-utils
PKGSEC=utils
PKGDEP="glibc"
PKGDES="Low level utilities for devices that use a SCSI command set"

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
