abinfo "Installing misc setup scripts"
install -Dvm644 "$SRCDIR"/redhat/rdma.conf \
    "$PKGDIR"/etc/rdma/rdma.conf
install -Dvm755 "$SRCDIR"/redhat/rdma.mlx4-setup.sh \
    "$PKGDIR"/usr/lib/rdma/mlx4-setup.sh
install -Dvm644 "$SRCDIR"/redhat/rdma.mlx4.conf \
    "$PKGDIR"/etc/rdma/mlx4.conf
install -Dvm644 "$SRCDIR"/redhat/rdma.mlx4.sys.modprobe \
    "$PKGDIR"/usr/lib/modprobe.d/libmlx4.conf

abinfo "Removing deprecated /etc/init.d ..."
rm -rv "$PKGDIR"/etc/init.d
