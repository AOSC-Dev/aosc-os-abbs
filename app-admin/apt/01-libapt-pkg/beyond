abinfo "Installing libapt-pkg ..."
rm -rv "$PKGDIR"/usr/{libexec,share/man,share/locale,share/doc/apt-utils,share/doc/apt-doc,share/doc/apt,bin}
rm -v "$PKGDIR"/usr/lib/libapt-private.so.0.0.0
rm -rv "$PKGDIR"/var
rm -rv "$PKGDIR"/etc
