VER=353
# Note: ab_apply_patch() uses patch(1), which does not support Git binary
# diffs. To make things easier, just use our repository and tracking
# branch. Please, please, please, still note patch tracking information in
# your commit message!
#
# Hash below corresponds to the HEAD of your respective tracking branch
# for the release.
#
# Note: copy-repo=true is needed, lest:
#
#   + cmd_make_package_lock_json
#   + '[' '!' -e .git ']'
#   + '[' package-lock.json -nt package.json ']'
#   + echo '*** Can'\''t update node modules unless running from git'
#   *** Can't update node modules unless running from git
#   + exit 1
SRCS="git::commit=d2a6fca152062dafdc2fa8b187c849e3a2989d27;copy-repo=true::https://github.com/AOSC-Tracking/cockpit"
CHKSUMS="SKIP"
CHKUPDATE="anitya::id=8849"
