abinfo "Removing uneeded files ..."

abinfo "If usr/bin/waagent2.0 exists, remove it, since we have python3 support"
if [ -f "$PKGDIR"/usr/bin/waagent2.0 ]; then
    rm -f "$PKGDIR"/usr/bin/waagent2.0
fi
