abinfo "Fixing up Makefile ..."
sed -e "s#/sbin#/bin#" \
    -i "$SRCDIR"/Makefile
sed -e "s#/usr##" \
    -i "$SRCDIR"/man/Makefile

abinfo "Installing distribution configuration ..."
cp -v "$SRCDIR"/{autobuild/,}config.h
