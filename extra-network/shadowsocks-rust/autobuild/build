#SODIUM_USE_PKG_CONFIG=1 cargo build --release --features "openssl-vendored"
SODIUM_USE_PKG_CONFIG=1 cargo build --release

abinfo "Installing binaries..."
make TARGET="release" DESTDIR="$PKGDIR" PREFIX="/usr/bin" install
