abinfo "Tweaking configure to honour system flags ..."
sed -e '/^CFLAGS="$CFLAGS/ s/INGS"/INGS -D_GNU_SOURCE"/' \
    -i "$SRCDIR"/configure

abinfo "Appending -fcommon to fix build with GCC 10+ ..."
export CFLAGS="${CFLAGS} -fcommon"
