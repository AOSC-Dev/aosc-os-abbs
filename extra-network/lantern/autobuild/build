# This is shown in the software
export VERSION=${PKGVER}-${PKGREL:-0}-aosc

# XXX: amd64 only
cd archive
make linux-amd64
install -Dm755 "${SRCDIR}/archive/lantern_linux_amd64" "${PKGDIR}/usr/bin/lantern"
