## Binary Package
dpkg -x lantern-$PKGVER.bin "${PKGDIR}"

## Will switch to src pack when lantern use official Go compiler

# Disable git update index
# sed -e '/update-index/d' -i Makefile

# Build Lantern and copy binary
# make lantern
# install -Dm755 "${SRCDIR}/lantern" "${PKGDIR}/usr/bin/lantern"
