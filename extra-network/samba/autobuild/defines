PKGNAME=samba
PKGSEC=net
PKGDEP="glibc libbsd popt tdb ldb tevent libgcrypt python-2 talloc readline gnutls openldap cups gamin libcap iniparser libaio"
PKGDES="SMB Fileserver and AD Domain server"

ABRPMAUTOPROVONLY=yes
AB_FLAGS_PIE=0
NOSTATIC=no

if [[ "${CROSS:-$ARCH}" != "amd64" ]]; then
    NOPARALLEL=yes
fi

# FIXME: Python was built with LTO on all platforms.
NOLTO=no
