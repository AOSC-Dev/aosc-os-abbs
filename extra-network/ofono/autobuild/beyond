mkdir -pv "$PKGDIR"/usr/lib/ofono/bin
cp -v test/* "$PKGDIR"/usr/lib/ofono/bin
cp -v build/tools/{huawei-audio,auto-enable,get-location,lookup-apn,lookup-provider-name,tty-redirector} "$PKGDIR"/usr/lib/ofono/bin
