abinfo "Removing helper for non-system prefixes (which do not conform to FHS) ..."
rm -v "$PKGDIR"/usr/tenacity

abinfo "Removing a conflicting icon with Audacity ..."
rm -v "$PKGDIR"/usr/share/pixmaps/gnome-mime-application-x-audacity-project.xpm
