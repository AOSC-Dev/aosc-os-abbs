echo "Refreshing dynamic library cache ..."
ldconfig

echo "Remove dangling nscd symlink in case the user has enabled it ..."
systemctl stop nscd &> /dev/null || true
rm -vf /etc/systemd/system/multi-user.target.wants/nscd.service
