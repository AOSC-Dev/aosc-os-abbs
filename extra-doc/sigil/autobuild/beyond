for _pic in 16 32 48 128 256; do
    abinfo "Installing ${_pic}x${_pic} icons ..."
    install -Dvm0644 "$SRCDIR"/src/Resource_Files/icon/app_icons_orig/app_icon_${_pic}.png \
        "$PKGDIR"/usr/share/icons/hicolor/${_pic}x${_pic}/apps/sigil.png
done
