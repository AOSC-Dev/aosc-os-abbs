for sz in 22 32 48 64; do
    install -Dvm644 "$SRCDIR"/desktopfiles/prebuilt-hi${sz}-djvu.png \
        "$PKGDIR"/usr/share/icons/hicolor/${sz}x${sz}/mimetypes/image-vnd.djvu.mime.png
done

chmod --verbose +x "$PKGDIR"/usr/lib/*.so*
