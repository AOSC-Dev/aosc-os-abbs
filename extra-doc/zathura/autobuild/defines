PKGNAME=zathura
PKGSEC=doc
PKGDEP="girara sqlite desktop-file-utils file"
[ "$ARCH" = "amd64" ] && PKGDEP+=" texlive"
BUILDDEP="docutils"
[ "$ARCH" = "amd64" ] && BUILDDEP+=" texlive"
PKGDES="A minimalistic document viewer"

[ "$ARCH" = "amd64" ] && MAKE_AFTER="WITH_SYNCTEX=1"
