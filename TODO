cross-armel:
	util-linux & procps-ng needs systemd support
	glib needs gamin support
	libxml2 needs python support

amd64:
	merge packages to this tree (all 3000 or so of them)

all:
	deprecate all PM level call (like some package removing duplicated files)
