abinfo "Removing unneeded icon theme cache ..."
rm -fv "$PKGDIR"/usr/share/icons/gnome/icon-theme.cache

abinfo "Moving locale directory /usr/locale => /usr/share/locale ..."
mv -v "$PKGDIR"/usr{,/share}/locale
