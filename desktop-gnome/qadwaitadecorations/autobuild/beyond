abinfo "Building QAdwaitaDecorations Qt-5 version ..."
mkdir -pv "$SRCDIR"/build-qt5
pushd "$SRCDIR"/build-qt5
cmake "$SRCDIR" \
    "${CMAKE_DEF[@]}" \
    "-DHAS_QT6_SUPPORT=true" \
    "-GNinja"
cmake --build .

abinfo "Installing QAdwaitaDecorations Qt-5 version ..."
DESTDIR="$PKGDIR" cmake --install .
popd
