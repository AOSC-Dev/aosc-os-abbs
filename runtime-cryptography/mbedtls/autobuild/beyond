abinfo "Fixing libmbedcrypto.so permission ..."
chmod -v 755 "$PKGDIR/usr/lib/libmbedcrypto.so.$PKGVER"

if ab_match_archgroup retro; then
    abinfo "Dropping executables - leave runtime only ..."
    rm -rv "$PKGDIR"/usr/bin
fi
