# FIXME: Thumb does not have enough registers.
#
# [...]/libgcrypt-1.12.2/mpi/ec-nist.c: In function '_gcry_mpi_ec_nist256_mod':
# [...]/libgcrypt-1.12.2/mpi/ec-inline.h:839:3: error: 'asm' operand has impossible constraints or there are not enough registers
#   839 |   __asm__ ("adds %3, %3, %11\n" \
#       |   ^~~~~~~
if ab_match_arch armv7hf; then
    export CFLAGS="${CFLAGS/-mthumb/}"
fi
