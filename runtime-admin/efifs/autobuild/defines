PKGNAME=efifs
PKGSEC=libs
# NOTE: EFI drivers only
PKGDEP=""
BUILDDEP="gcc"
BUILDDEP__AMD64="${BUILDDEP} nasm"
BUILDDEP__I486="${BUILDDEP} nasm"
PKGDES="EFI file system drivers"

ABTYPE=self

# NOTE: EFI drivers only
ABSPLITDBG=0

# NOTE: These are the architectures supported by the upstream (see Make.common)
FAIL_ARCH="!(amd64|arm64|loongarch64|loongarch64_nosimd|riscv64|i486|armv7)"
