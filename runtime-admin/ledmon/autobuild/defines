PKGNAME=ledmon
PKGSEC=libs
PKGDEP="glibc gcc-runtime sg3-utils pciutils systemd"
PKGDES="Library and command-line utilities for controlling LEDs"

AUTOTOOLS_AFTER=(
    "--enable-library"
    "--disable-test"
    "--enable-doc"
    "--enable-systemd"
)
