cd "${SRCDIR}"
mkdir -p "${PKGDIR}/usr/share/icewm/themes"
cp -r "${SRCDIR}"/* "${PKGDIR}/usr/share/icewm/themes/"
find "${PKGDIR}" -type d -exec chmod 755 {} \;
find "${PKGDIR}" -type f -exec chmod 644 {} \;
