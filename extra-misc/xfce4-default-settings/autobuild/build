abinfo "Installing default configurations ..."
mkdir -p "$PKGDIR"/etc/skel/.config/
cp -rv "$SRCDIR"/xfce4 "$PKGDIR"/etc/skel/.config/

abinfo "Touching up permissions ..."
chmod -v 700 "$PKGDIR"/etc/skel/.config/xfce4/panel/launcher-*
