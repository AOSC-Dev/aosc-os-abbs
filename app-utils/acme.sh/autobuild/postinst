if [[ "$1" = "install" ]]; then
	echo "For certificate auto renewal, run acme.sh --install-cronjob to install cron."
    echo "Or alternatively, enable systemd USER timer acme-sh.timer with:"
    echo "    systemctl --user enable --now acme-sh.timer"
fi
