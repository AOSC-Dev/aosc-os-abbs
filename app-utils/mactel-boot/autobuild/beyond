abinfo "Moving /usr/sbin to /usr/bin ..."
mv -v "$PKGDIR"/usr/sbin "$PKGDIR"/usr/bin

abinfo "Installing SystemVersion.plist ..."
install -Dvm0644 "$SRCDIR"/SystemVersion.plist \
    "$PKGDIR"/usr/share/mactel-boot/SystemVersion.plist
