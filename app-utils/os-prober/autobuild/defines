PKGNAME=os-prober
PKGDES="Utility to detect other operating systems on a set of drives"
PKGSEC=utils
PKGDEP="bash"

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
