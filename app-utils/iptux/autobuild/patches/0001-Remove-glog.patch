# patch from: https://github.com/iptux-src/iptux/pull/672

From 26e527a9cbdcc8a16918e9664f4f6434c119d2ef Mon Sep 17 00:00:00 2001
From: LI Daobing <lidaobing@gmail.com>
Date: Mon, 17 Feb 2025 15:51:01 -0800
Subject: [PATCH 1/5] 1

---
 .github/workflows/ci.yml                 |  4 ++--
 .github/workflows/codeql.yml             |  2 +-
 README.md                                |  4 ++--
 examples/count-bot/main.cpp              |  7 +++---
 examples/count-bot/meson.build           |  2 +-
 meson.build                              |  1 -
 snap/snapcraft.yaml                      |  2 --
 src/iptux-core/CoreThread.cpp            | 11 +++++-----
 src/iptux-core/internal/RecvFileData.cpp |  3 +--
 src/iptux-core/internal/SendFileData.cpp |  5 ++---
 src/iptux-core/meson.build               | 11 +++++-----
 src/iptux/AboutDialog.cpp                |  8 +++----
 src/iptux/Application.cpp                |  9 ++++----
 src/iptux/DetectPal.cpp                  | 11 ++++------
 src/iptux/DialogBase.cpp                 |  3 +--
 src/iptux/DialogGroup.cpp                |  7 ++----
 src/iptux/DialogGroup.h                  |  2 +-
 src/iptux/DialogPeer.cpp                 |  3 +--
 src/iptux/LogSystem.cpp                  |  3 +--
 src/iptux/MainWindow.cpp                 | 27 +++++++++++++-----------
 src/iptux/MainWindow.h                   |  2 +-
 src/iptux/RevisePal.cpp                  |  7 +-----
 src/iptux/ShareFile.cpp                  |  3 +--
 src/iptux/UiCoreThread.cpp               |  1 -
 src/iptux/UiModels.cpp                   |  7 +++---
 src/iptux/dialog.cpp                     |  3 +--
 src/iptux/meson.build                    |  3 +--
 src/main/meson.build                     |  2 +-
 28 files changed, 64 insertions(+), 89 deletions(-)

diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index b8e7440df..865a7fbd4 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -8,11 +8,11 @@ on:
 
 env:
   DEB_PKGS: >-
-    g++ meson appstream gettext libgoogle-glog-dev libgtk-3-dev libglib2.0-dev
+    g++ meson appstream gettext libgtk-3-dev libglib2.0-dev
     libjsoncpp-dev libsigc++-2.0-dev libayatana-appindicator3-dev
   DEB_TEST_PKGS: xvfb
   MAC_PKGS: >-
-    meson appstream gettext glog gtk+3 jsoncpp gtk-mac-integration libsigc++@2
+    meson appstream gettext gtk+3 jsoncpp gtk-mac-integration libsigc++@2
 
 jobs:
   build-on-mac:
diff --git a/.github/workflows/codeql.yml b/.github/workflows/codeql.yml
index fbfba7d6a..1a22b5c50 100644
--- a/.github/workflows/codeql.yml
+++ b/.github/workflows/codeql.yml
@@ -76,7 +76,7 @@ jobs:
 
     - run: >-
         sudo apt-get update -q -y &&
-        sudo apt-get install -q -y git libgoogle-glog-dev libgtk-3-dev
+        sudo apt-get install -q -y git libgtk-3-dev
         libglib2.0-dev libjsoncpp-dev g++ meson xvfb libsigc++-2.0-dev
         appstream gettext &&
         meson --version &&
diff --git a/README.md b/README.md
index ef0429726..07a8c848f 100644
--- a/README.md
+++ b/README.md
@@ -29,7 +29,7 @@ brew install iptux
 ### Linux (Debian and Ubuntu)
 
 ```sh
-sudo apt-get install git libgoogle-glog-dev libgtk-3-dev libglib2.0-dev libjsoncpp-dev g++ meson libsigc++-2.0-dev libayatana-appindicator3-dev appstream gettext
+sudo apt-get install git libgtk-3-dev libglib2.0-dev libjsoncpp-dev g++ meson libsigc++-2.0-dev libayatana-appindicator3-dev appstream gettext
 git clone git://github.com/iptux-src/iptux.git
 cd iptux
 meson setup build
@@ -41,7 +41,7 @@ iptux
 ### Mac OS X
 
 ```sh
-brew install meson gettext gtk+3 jsoncpp glog gtk-mac-integration libsigc++@2 appstream
+brew install meson gettext gtk+3 jsoncpp gtk-mac-integration libsigc++@2 appstream
 git clone git://github.com/iptux-src/iptux.git
 cd iptux
 meson setup build
diff --git a/examples/count-bot/main.cpp b/examples/count-bot/main.cpp
index d12803d3a..29efcbfdd 100644
--- a/examples/count-bot/main.cpp
+++ b/examples/count-bot/main.cpp
@@ -4,7 +4,6 @@
 
 #include <unistd.h>
 
-#include <glog/logging.h>
 #include <iptux-core/CoreThread.h>
 #include <iptux-core/Exception.h>
 
@@ -20,10 +19,10 @@ void usage(const char* progname) {
 void processNewMessageEvent(shared_ptr<CoreThread> ct,
                             const NewMessageEvent* event) {
   auto para = event->getMsgPara();
-  LOG(INFO) << "New Message Event: " << endl;
-  LOG(INFO) << "  From: " << para.getPal()->GetKey().ToString() << endl;
+  cout << "New Message Event: " << endl;
+  cout << "  From: " << para.getPal()->GetKey().ToString() << endl;
   for (auto& chip : para.dtlist) {
-    LOG(INFO) << "  Message: " << chip.ToString() << endl;
+    cout << "  Message: " << chip.ToString() << endl;
     ostringstream oss;
     oss << "your message has " << chip.data.size() << " bytes.";
     ct->SendMessage(para.getPal(), oss.str());
diff --git a/examples/count-bot/meson.build b/examples/count-bot/meson.build
index f4fa034c2..4b1cd6953 100644
--- a/examples/count-bot/meson.build
+++ b/examples/count-bot/meson.build
@@ -2,7 +2,7 @@ bot_sources = files(['main.cpp'])
 executable('count-bot',
     bot_sources,
     include_directories: include_directories('../../src/api'),
-    dependencies: [jsoncpp_dep, thread_dep, glog_dep, gflags_dep, glib_dep, sigc_dep],
+    dependencies: [jsoncpp_dep, thread_dep, gflags_dep, glib_dep, sigc_dep],
     link_with: [libiptux_core],
     install: false
 )
diff --git a/meson.build b/meson.build
index 7ed9f416e..bf798e8a7 100644
--- a/meson.build
+++ b/meson.build
@@ -29,7 +29,6 @@ if meson.version().version_compare('>=0.57')
     'glib-2.0': glib_dep,
     'gtk+-3.0': gtk_dep,
     'jsoncpp': jsoncpp_dep,
-    'libglog': glog_dep,
     'gflags': gflags_dep,
     'sigc++-2.0': sigc_dep,
     }
diff --git a/snap/snapcraft.yaml b/snap/snapcraft.yaml
index 2c4716a21..b9573e6f1 100644
--- a/snap/snapcraft.yaml
+++ b/snap/snapcraft.yaml
@@ -27,7 +27,6 @@ parts:
     build-packages:
       [
         gettext,
-        libgoogle-glog-dev,
         libjsoncpp-dev,
         libsigc++-2.0-dev,
         libayatana-appindicator3-dev,
@@ -45,7 +44,6 @@ parts:
       snap/iptux/current/usr: usr
     stage-packages:
       - libgflags2.2
-      - libgoogle-glog0v6t64
       - libjsoncpp25
       - libsigc++-2.0-0v5
       - libayatana-appindicator3-1
diff --git a/src/iptux-core/CoreThread.cpp b/src/iptux-core/CoreThread.cpp
index c1bd8354d..c94b7c996 100644
--- a/src/iptux-core/CoreThread.cpp
+++ b/src/iptux-core/CoreThread.cpp
@@ -13,7 +13,6 @@
 
 #include <glib/gi18n.h>
 #include <glib/gstdio.h>
-#include <glog/logging.h>
 #include <poll.h>
 #include <sys/stat.h>
 
@@ -227,7 +226,7 @@ void CoreThread::RecvUdpData(CoreThread* self) {
     if (ret == 0) {
       continue;
     }
-    CHECK(ret == 1);
+    g_assert(ret == 1);
     len = sizeof(addr);
     if ((size = recvfrom(self->udpSock, buf, MAX_UDPLEN, 0,
                          (struct sockaddr*)&addr, &len)) == -1)
@@ -257,7 +256,7 @@ void CoreThread::RecvTcpData(CoreThread* pcthrd) {
     if (ret == 0) {
       continue;
     }
-    CHECK(ret == 1);
+    g_assert(ret == 1);
     if ((subsock = accept(pcthrd->tcpSock, NULL, NULL)) == -1)
       continue;
     thread([](CoreThread* coreThread,
@@ -607,9 +606,9 @@ void CoreThread::SetAccessPublicLimit(const string& val) {
 }
 
 void CoreThread::AddPrivateFile(PFileInfo file) {
-  CHECK(file);
-  CHECK(file->fileid >= MAX_SHAREDFILE);
-  CHECK(pImpl->privateFiles.count(file->fileid) == 0);
+  g_assert(file);
+  g_assert(file->fileid >= MAX_SHAREDFILE);
+  g_assert(pImpl->privateFiles.count(file->fileid) == 0);
   pImpl->privateFiles[file->fileid] = file;
 }
 
diff --git a/src/iptux-core/internal/RecvFileData.cpp b/src/iptux-core/internal/RecvFileData.cpp
index 508f22cd5..ead8995bf 100644
--- a/src/iptux-core/internal/RecvFileData.cpp
+++ b/src/iptux-core/internal/RecvFileData.cpp
@@ -15,7 +15,6 @@
 #include <memory>
 
 #include <fcntl.h>
-#include <glog/logging.h>
 #include <sys/socket.h>
 #include <sys/time.h>
 #include <unistd.h>
@@ -55,7 +54,7 @@ RecvFileData::~RecvFileData() {}
  * 接收文件数据入口.
  */
 void RecvFileData::RecvFileDataEntry() {
-  CHECK(GetTaskId() > 0);
+  g_assert(GetTaskId() > 0);
 
   CreateUIPara();
   coreThread->emitEvent(make_shared<RecvFileStartedEvent>(GetTaskId()));
diff --git a/src/iptux-core/internal/SendFileData.cpp b/src/iptux-core/internal/SendFileData.cpp
index f0011a0a4..90d87d179 100644
--- a/src/iptux-core/internal/SendFileData.cpp
+++ b/src/iptux-core/internal/SendFileData.cpp
@@ -24,7 +24,6 @@
 #include <unistd.h>
 
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 
 #include "iptux-core/internal/AnalogFS.h"
 
@@ -56,7 +55,7 @@ SendFileData::~SendFileData() {}
  * 发送文件数据入口.
  */
 void SendFileData::SendFileDataEntry() {
-  CHECK(GetTaskId() > 0);
+  g_assert(GetTaskId() > 0);
   CreateUIPara();
   coreThread->emitEvent(make_shared<SendFileStartedEvent>(GetTaskId()));
 
@@ -69,7 +68,7 @@ void SendFileData::SendFileDataEntry() {
       SendDirFiles();
       break;
     default:
-      CHECK(false);
+      g_assert(false);
       break;
   }
   UpdateUIParaToOver();
diff --git a/src/iptux-core/meson.build b/src/iptux-core/meson.build
index 0512f16c0..3d3d47e93 100644
--- a/src/iptux-core/meson.build
+++ b/src/iptux-core/meson.build
@@ -1,6 +1,5 @@
 glib_dep = dependency('glib-2.0', version: '>=2.35')
 jsoncpp_dep = dependency('jsoncpp', version: '>=1.0')
-glog_dep = dependency('libglog')
 gflags_dep = dependency('gflags')
 sigc_dep = dependency('sigc++-2.0')
 
@@ -36,14 +35,14 @@ thread_dep = dependency('threads')
 if get_option('static-link')
     libiptux_core = static_library('iptux-core',
         core_sources,
-        dependencies: [glib_dep, jsoncpp_dep, glog_dep, gflags_dep, sigc_dep, thread_dep],
+        dependencies: [glib_dep, jsoncpp_dep, gflags_dep, sigc_dep, thread_dep],
         link_with: [libiptux_utils],
         include_directories: inc,
     )
 else
     libiptux_core = shared_library('iptux-core',
         core_sources,
-        dependencies: [glib_dep, jsoncpp_dep, glog_dep, gflags_dep, sigc_dep, thread_dep],
+        dependencies: [glib_dep, jsoncpp_dep, gflags_dep, sigc_dep, thread_dep],
         link_with: [libiptux_utils],
         include_directories: inc,
         install: true,
@@ -56,8 +55,8 @@ else
         name: 'iptux-core',
         version: meson.project_version(),
         requires: [jsoncpp_dep, glib_dep, sigc_dep, thread_dep],
-        requires_private: [glog_dep, gflags_dep],
-        libraries: [libiptux_core, glog_dep],
+        requires_private: [gflags_dep],
+        libraries: [libiptux_core],
     )
 endif
 
@@ -67,7 +66,7 @@ core_test_helper_sources = files([
 ])
 libiptux_core_test_helper = static_library('iptux-test-helper',
     core_test_helper_sources,
-    dependencies: [glib_dep, jsoncpp_dep, sigc_dep, glog_dep],
+    dependencies: [glib_dep, jsoncpp_dep, sigc_dep],
     link_with: [libiptux_core, libiptux_utils_test_helper],
     include_directories: inc
 )
diff --git a/src/iptux/AboutDialog.cpp b/src/iptux/AboutDialog.cpp
index 773e72504..256a4e659 100644
--- a/src/iptux/AboutDialog.cpp
+++ b/src/iptux/AboutDialog.cpp
@@ -12,7 +12,6 @@
 #include "AboutDialog.h"
 
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 #include <string>
 
 #include "iptux/UiHelper.h"
@@ -63,10 +62,11 @@ AboutDialog* aboutDialogNew() {
   auto builder =
       gtk_builder_new_from_resource(IPTUX_RESOURCE "gtk/AboutDialog.ui");
   gtk_builder_connect_signals(builder, nullptr);
-  auto aboutDialog = GTK_ABOUT_DIALOG(
-      CHECK_NOTNULL(gtk_builder_get_object(builder, "about_dialog")));
+  auto aboutDialog =
+      GTK_ABOUT_DIALOG(gtk_builder_get_object(builder, "about_dialog"));
   g_object_unref(builder);
-
+  if (!aboutDialog)
+    return nullptr;
   CreateAboutDialog(aboutDialog);
   return aboutDialog;
 }
diff --git a/src/iptux/Application.cpp b/src/iptux/Application.cpp
index b9360254b..fa835ca26 100644
--- a/src/iptux/Application.cpp
+++ b/src/iptux/Application.cpp
@@ -2,7 +2,6 @@
 #include "Application.h"
 
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 #include <sys/stat.h>
 
 #include "iptux-core/Exception.h"
@@ -310,8 +309,8 @@ void Application::onEvent(shared_ptr<const Event> _event) {
   }
   if (type == EventType::SEND_FILE_STARTED ||
       type == EventType::RECV_FILE_STARTED) {
-    auto event =
-        CHECK_NOTNULL(dynamic_cast<const AbstractTaskIdEvent*>(_event.get()));
+    auto event = dynamic_cast<const AbstractTaskIdEvent*>(_event.get());
+    g_assert(event);
     auto taskId = event->GetTaskId();
     auto para = cthrd->GetTransTaskStat(taskId);
     if (!para.get()) {
@@ -328,8 +327,8 @@ void Application::onEvent(shared_ptr<const Event> _event) {
 
   if (type == EventType::SEND_FILE_FINISHED ||
       type == EventType::RECV_FILE_FINISHED) {
-    auto event =
-        CHECK_NOTNULL(dynamic_cast<const AbstractTaskIdEvent*>(_event.get()));
+    auto event = dynamic_cast<const AbstractTaskIdEvent*>(_event.get());
+    g_assert(event);
     auto taskId = event->GetTaskId();
     auto para = cthrd->GetTransTaskStat(taskId);
     this->updateItemToTransTree(*para);
diff --git a/src/iptux/DetectPal.cpp b/src/iptux/DetectPal.cpp
index 8020751e4..22caa7c29 100644
--- a/src/iptux/DetectPal.cpp
+++ b/src/iptux/DetectPal.cpp
@@ -13,7 +13,6 @@
 #include "DetectPal.h"
 
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 
 #include "iptux-core/Exception.h"
 #include "iptux/UiCoreThread.h"
@@ -38,13 +37,11 @@ DetectPal::DetectPal(Application* app, GtkWindow* window) : app(app) {
   auto builder =
       gtk_builder_new_from_resource(IPTUX_RESOURCE "gtk/DetectPal.ui");
 
-  g_set_object(&detectPalDialog,
-               CHECK_NOTNULL(GTK_DIALOG(
-                   gtk_builder_get_object(builder, "detect_pal_dialog"))));
+  g_set_object(&detectPalDialog, GTK_DIALOG(gtk_builder_get_object(
+                                     builder, "detect_pal_dialog")));
   gtk_window_set_transient_for(GTK_WINDOW(this->detectPalDialog), window);
-  g_set_object(&detectPalIpv4Entry,
-               CHECK_NOTNULL(GTK_ENTRY(
-                   gtk_builder_get_object(builder, "detect_pal_ipv4_entry"))));
+  g_set_object(&detectPalIpv4Entry, GTK_ENTRY(gtk_builder_get_object(
+                                        builder, "detect_pal_ipv4_entry")));
   g_signal_connect(detectPalIpv4Entry, "insert-text",
                    G_CALLBACK(entry_insert_numeric), nullptr);
   g_signal_connect(detectPalIpv4Entry, "key-release-event",
diff --git a/src/iptux/DialogBase.cpp b/src/iptux/DialogBase.cpp
index 27126c3bb..a16443116 100644
--- a/src/iptux/DialogBase.cpp
+++ b/src/iptux/DialogBase.cpp
@@ -16,7 +16,6 @@
 
 #include "UiModels.h"
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 #include <sys/stat.h>
 
 #include "iptux-utils/output.h"
@@ -352,7 +351,7 @@ GSList* DialogBase::PickEnclosure(FileAttr fileattr) {
     action = GTK_FILE_CHOOSER_ACTION_SELECT_FOLDER;
     title = _("Choose enclosure folders");
   } else {
-    CHECK(false);
+    g_assert(false);
   }
   dialog = gtk_file_chooser_dialog_new(title, GTK_WINDOW(getWindow()), action,
                                        _("_Open"), GTK_RESPONSE_ACCEPT,
diff --git a/src/iptux/DialogGroup.cpp b/src/iptux/DialogGroup.cpp
index 53fb9d495..fc2f07c39 100644
--- a/src/iptux/DialogGroup.cpp
+++ b/src/iptux/DialogGroup.cpp
@@ -13,7 +13,6 @@
 #include "DialogGroup.h"
 
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 
 #include "iptux-core/Const.h"
 #include "iptux-utils/output.h"
@@ -32,8 +31,7 @@ namespace iptux {
  * @param grp 群组信息
  */
 DialogGroup::DialogGroup(Application* app, GroupInfo* grp)
-    : DialogBase(CHECK_NOTNULL(app), CHECK_NOTNULL(grp)),
-      config(app->getConfig()) {
+    : DialogBase(app, grp), config(app->getConfig()) {
   InitSublayerSpecify();
 }
 
@@ -50,8 +48,7 @@ DialogGroup::~DialogGroup() {
  */
 DialogGroup* DialogGroup::GroupDialogEntry(Application* app,
                                            GroupInfo* grpinf) {
-  CHECK_NOTNULL(grpinf);
-  CHECK_NE(grpinf->getType(), GROUP_BELONG_TYPE_REGULAR);
+  g_assert(grpinf->getType() != GROUP_BELONG_TYPE_REGULAR);
   DialogGroup* dlggrp;
   GtkWidget *window, *widget;
 
diff --git a/src/iptux/DialogGroup.h b/src/iptux/DialogGroup.h
index 726276b53..f1bf0f44f 100644
--- a/src/iptux/DialogGroup.h
+++ b/src/iptux/DialogGroup.h
@@ -21,7 +21,7 @@ namespace iptux {
 
 class DialogGroup : public DialogBase {
  public:
-  DialogGroup(Application* app, GroupInfo* grp);
+  DialogGroup(Application* app, GroupInfo* grp) __attribute__((nonnull));
   virtual ~DialogGroup();
 
   static DialogGroup* GroupDialogEntry(Application* app, GroupInfo* grpinf);
diff --git a/src/iptux/DialogPeer.cpp b/src/iptux/DialogPeer.cpp
index 8dac8fb58..0c5c1b56f 100644
--- a/src/iptux/DialogPeer.cpp
+++ b/src/iptux/DialogPeer.cpp
@@ -22,7 +22,6 @@
 
 #include <gdk/gdkkeysyms.h>
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 
 #include "iptux-core/Const.h"
 #include "iptux-utils/output.h"
@@ -41,7 +40,7 @@ namespace iptux {
  * @param grp 好友群组信息
  */
 DialogPeer::DialogPeer(Application* app, GroupInfo* grp)
-    : DialogBase(CHECK_NOTNULL(app), CHECK_NOTNULL(grp)),
+    : DialogBase(app, grp),
       config(app->getConfig()),
       torcvsize(0),
       rcvdsize(0),
diff --git a/src/iptux/LogSystem.cpp b/src/iptux/LogSystem.cpp
index 0f4a4a09a..5f21271cd 100644
--- a/src/iptux/LogSystem.cpp
+++ b/src/iptux/LogSystem.cpp
@@ -14,7 +14,6 @@
 
 #include <fcntl.h>
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 
 #include "iptux-core/Const.h"
 #include "iptux-utils/utils.h"
@@ -28,7 +27,7 @@ namespace iptux {
 
 LogSystem::LogSystem(shared_ptr<const ProgramData> programData)
     : programData(programData), fdc(-1), fds(-1) {
-  CHECK_NOTNULL(programData.get());
+  g_assert(programData.get());
   InitSublayer();
 }
 
diff --git a/src/iptux/MainWindow.cpp b/src/iptux/MainWindow.cpp
index 609f01ed9..fe4d18fce 100644
--- a/src/iptux/MainWindow.cpp
+++ b/src/iptux/MainWindow.cpp
@@ -25,7 +25,6 @@
 #include "iptux/dialog.h"
 #include <ctime>
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 #include <string>
 #include <thread>
 
@@ -422,7 +421,7 @@ void MainWindow::InitSublayer() {
   g_datalist_init(&widset);
   g_datalist_init(&mdlset);
 
-  CHECK_EQ(int(timerid), 0);
+  g_assert(int(timerid) == 0);
   timerid = g_timeout_add_seconds(1, GSourceFunc(UpdateUI), this);
 
   model = palTreeModelNew(sort_key_, sort_type_);
@@ -1384,32 +1383,35 @@ void MainWindow::onFind(void*, void*, MainWindow& self) {
 }
 
 void MainWindow::onDeletePal(void*, void*, MainWindow& self) {
-  GroupInfo* groupInfo = CHECK_NOTNULL(self.currentGroupInfo);
+  g_assert(self.currentGroupInfo);
+  GroupInfo* groupInfo = self.currentGroupInfo;
   switch (groupInfo->getType()) {
     case GROUP_BELONG_TYPE_REGULAR:
       self.DeletePalItem(groupInfo);
       break;
     default:
-      CHECK(false);
+      g_assert(false);
       break;
   }
 }
 
 void MainWindow::onPalChangeInfo(void*, void*, MainWindow& self) {
-  GroupInfo* groupInfo = CHECK_NOTNULL(self.currentGroupInfo);
+  g_assert(self.currentGroupInfo);
+  GroupInfo* groupInfo = self.currentGroupInfo;
   switch (groupInfo->getType()) {
     case GROUP_BELONG_TYPE_REGULAR:
       RevisePal::ReviseEntry(self.app, GTK_WINDOW(self.window),
                              groupInfo->getMembers()[0].get());
       break;
     default:
-      CHECK(false);
+      g_assert(false);
       break;
   }
 }
 
 void MainWindow::onPalSendMessage(void*, void*, MainWindow& self) {
-  GroupInfo* groupInfo = CHECK_NOTNULL(self.currentGroupInfo);
+  g_assert(self.currentGroupInfo);
+  GroupInfo* groupInfo = self.currentGroupInfo;
   if (groupInfo->getDialog()) {
     gtk_window_present(GTK_WINDOW(groupInfo->getDialog()));
     return;
@@ -1424,18 +1426,19 @@ void MainWindow::onPalSendMessage(void*, void*, MainWindow& self) {
       DialogGroup::GroupDialogEntry(self.app, groupInfo);
       break;
     default:
-      CHECK(false);
+      g_assert(false);
       break;
   }
 }
 void MainWindow::onPalRequestSharedResources(void*, void*, MainWindow& self) {
-  GroupInfo* groupInfo = CHECK_NOTNULL(self.currentGroupInfo);
+  g_assert(self.currentGroupInfo);
+  GroupInfo* groupInfo = self.currentGroupInfo;
   switch (groupInfo->getType()) {
     case GROUP_BELONG_TYPE_REGULAR:
       self.coreThread.SendAskShared(groupInfo->getMembers()[0]);
       break;
     default:
-      CHECK(false);
+      g_assert(false);
       break;
   }
 }
@@ -1745,7 +1748,7 @@ void MainWindow::ProcessEvent(shared_ptr<const Event> _event) {
 
   if (type == EventType::NEW_SHARE_FILE_FROM_FRIEND) {
     auto event = dynamic_cast<const NewShareFileFromFriendEvent*>(_event.get());
-    CHECK_NOTNULL(event);
+    g_assert(event);
     auto file = new FileInfo(event->GetFileInfo());
     coreThread.PushItemToEnclosureList(file);
     return;
@@ -1755,7 +1758,7 @@ void MainWindow::ProcessEvent(shared_ptr<const Event> _event) {
 }
 
 void MainWindow::setCurrentGroupInfo(GroupInfo* groupInfo) {
-  this->currentGroupInfo = CHECK_NOTNULL(groupInfo);
+  this->currentGroupInfo = groupInfo;
   switch (currentGroupInfo->getType()) {
     case GROUP_BELONG_TYPE_REGULAR:
       g_action_map_enable_actions(G_ACTION_MAP(window), "pal.send_message",
diff --git a/src/iptux/MainWindow.h b/src/iptux/MainWindow.h
index 6eeaac686..adf9fdfc8 100644
--- a/src/iptux/MainWindow.h
+++ b/src/iptux/MainWindow.h
@@ -80,7 +80,7 @@ class MainWindow : public sigc::trackable {
   GroupInfoStyle info_style_ = GroupInfoStyle::IP;
 
  private:
-  void setCurrentGroupInfo(GroupInfo* groupInfo);
+  void setCurrentGroupInfo(GroupInfo* groupInfo) __attribute__((nonnull));
 
   void InitSublayer();
   void LoadConfig();
diff --git a/src/iptux/RevisePal.cpp b/src/iptux/RevisePal.cpp
index c3d30cd4b..e93cf208b 100644
--- a/src/iptux/RevisePal.cpp
+++ b/src/iptux/RevisePal.cpp
@@ -15,7 +15,6 @@
 #include <cinttypes>
 #include <dirent.h>
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 
 #include "iptux-core/Const.h"
 #include "iptux-utils/utils.h"
@@ -29,11 +28,7 @@ namespace iptux {
  * @param pl
  */
 RevisePal::RevisePal(Application* app, GtkWindow* parent, PalInfo* pl)
-    : widset(NULL),
-      mdlset(NULL),
-      app(CHECK_NOTNULL(app)),
-      parent(parent),
-      pal(CHECK_NOTNULL(pl)) {
+    : widset(NULL), mdlset(NULL), app(app), parent(parent), pal(pl) {
   InitSublayer();
 }
 
diff --git a/src/iptux/ShareFile.cpp b/src/iptux/ShareFile.cpp
index e82747df3..a4b1be4ad 100644
--- a/src/iptux/ShareFile.cpp
+++ b/src/iptux/ShareFile.cpp
@@ -13,7 +13,6 @@
 #include "ShareFile.h"
 
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 #include <sys/stat.h>
 
 #include "iptux-utils/utils.h"
@@ -404,7 +403,7 @@ GSList* PickSharedFile(ShareFile* self, FileAttr fileattr) {
   const char* title;
   GSList* list;
 
-  CHECK(FileAttrIsValid(fileattr));
+  g_assert(FileAttrIsValid(fileattr));
 
   if (fileattr == FileAttr::REGULAR) {
     action = GTK_FILE_CHOOSER_ACTION_OPEN;
diff --git a/src/iptux/UiCoreThread.cpp b/src/iptux/UiCoreThread.cpp
index 40b876a01..6ff0e2ff3 100644
--- a/src/iptux/UiCoreThread.cpp
+++ b/src/iptux/UiCoreThread.cpp
@@ -17,7 +17,6 @@
 #include <sys/stat.h>
 
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 
 #include "iptux-core/Const.h"
 #include "iptux-utils/output.h"
diff --git a/src/iptux/UiModels.cpp b/src/iptux/UiModels.cpp
index c7f8104de..72cc5b20f 100644
--- a/src/iptux/UiModels.cpp
+++ b/src/iptux/UiModels.cpp
@@ -1,16 +1,15 @@
 #include "config.h"
 #include "UiModels.h"
 
-#include <cstring>
-#include <glib/gi18n.h>
-#include <glog/logging.h>
-
 #include "iptux-core/Const.h"
 #include "iptux-core/Models.h"
 #include "iptux-utils/output.h"
 #include "iptux-utils/utils.h"
 #include "iptux/DialogBase.h"
 #include "iptux/UiHelper.h"
+#include <cstring>
+#include <glib/gi18n.h>
+#include <inttypes.h>
 #include <netinet/in.h>
 
 using namespace std;
diff --git a/src/iptux/dialog.cpp b/src/iptux/dialog.cpp
index 386a1d3a8..21e2db66b 100644
--- a/src/iptux/dialog.cpp
+++ b/src/iptux/dialog.cpp
@@ -14,7 +14,6 @@
 
 #include "iptux-utils/utils.h"
 #include <glib/gi18n.h>
-#include <glog/logging.h>
 
 #include "iptux/callback.h"
 
@@ -161,7 +160,7 @@ char* pop_obtain_shared_passwd(GtkWindow* parent, PalInfo* pal) {
  * @return password string
  */
 char* pop_password_settings(GtkWidget* parent) {
-  CHECK_NOTNULL(parent);
+  g_return_val_if_fail(parent != NULL, NULL);
   GtkWidget *dialog, *hbox, *passwd, *repeat;
   gchar *text1, *text2;
   gint result;
diff --git a/src/iptux/meson.build b/src/iptux/meson.build
index ff2807b17..0965f443d 100644
--- a/src/iptux/meson.build
+++ b/src/iptux/meson.build
@@ -1,6 +1,5 @@
 gtk_dep = dependency('gtk+-3.0', version: '>=3.22')
 jsoncpp_dep = dependency('jsoncpp', version: '>=1.0')
-glog_dep = dependency('libglog')
 gflags_dep = dependency('gflags')
 sigc_dep = dependency('sigc++-2.0')
 
@@ -26,7 +25,7 @@ sources = files([
     'UiModels.cpp',
     'WindowConfig.cpp',
 ])
-dependencies = [gtk_dep, jsoncpp_dep, glog_dep, gflags_dep, sigc_dep]
+dependencies = [gtk_dep, jsoncpp_dep, gflags_dep, sigc_dep]
 
 if appindicator_dep.found()
     sources += ['AppIndicator.cpp']
diff --git a/src/main/meson.build b/src/main/meson.build
index 265391825..6a4c28814 100644
--- a/src/main/meson.build
+++ b/src/main/meson.build
@@ -2,7 +2,7 @@ iptux_sources = files(['iptux.cpp', 'iptux_crash_utils.cpp'])
 executable('iptux',
     iptux_sources,
     include_directories: inc,
-    dependencies: [gtk_dep, jsoncpp_dep, thread_dep, glog_dep, gflags_dep, sigc_dep],
+    dependencies: [gtk_dep, jsoncpp_dep, thread_dep, gflags_dep, sigc_dep],
     link_with: [libiptux],
     export_dynamic: true,
     install: true,

From c1fca6e45034e672f197d512b33d7966d351ab2e Mon Sep 17 00:00:00 2001
From: LI Daobing <lidaobing@gmail.com>
Date: Mon, 17 Feb 2025 15:58:48 -0800
Subject: [PATCH 2/5] remove gflags

---
 examples/count-bot/meson.build | 2 +-
 meson.build                    | 1 -
 snap/snapcraft.yaml            | 1 -
 src/iptux-core/meson.build     | 6 ++----
 src/iptux/meson.build          | 3 +--
 src/main/meson.build           | 2 +-
 6 files changed, 5 insertions(+), 10 deletions(-)

diff --git a/examples/count-bot/meson.build b/examples/count-bot/meson.build
index 4b1cd6953..000d746e6 100644
--- a/examples/count-bot/meson.build
+++ b/examples/count-bot/meson.build
@@ -2,7 +2,7 @@ bot_sources = files(['main.cpp'])
 executable('count-bot',
     bot_sources,
     include_directories: include_directories('../../src/api'),
-    dependencies: [jsoncpp_dep, thread_dep, gflags_dep, glib_dep, sigc_dep],
+    dependencies: [jsoncpp_dep, thread_dep, glib_dep, sigc_dep],
     link_with: [libiptux_core],
     install: false
 )
diff --git a/meson.build b/meson.build
index bf798e8a7..928fb0484 100644
--- a/meson.build
+++ b/meson.build
@@ -29,7 +29,6 @@ if meson.version().version_compare('>=0.57')
     'glib-2.0': glib_dep,
     'gtk+-3.0': gtk_dep,
     'jsoncpp': jsoncpp_dep,
-    'gflags': gflags_dep,
     'sigc++-2.0': sigc_dep,
     }
 
diff --git a/snap/snapcraft.yaml b/snap/snapcraft.yaml
index b9573e6f1..3736d426b 100644
--- a/snap/snapcraft.yaml
+++ b/snap/snapcraft.yaml
@@ -43,7 +43,6 @@ parts:
     organize:
       snap/iptux/current/usr: usr
     stage-packages:
-      - libgflags2.2
       - libjsoncpp25
       - libsigc++-2.0-0v5
       - libayatana-appindicator3-1
diff --git a/src/iptux-core/meson.build b/src/iptux-core/meson.build
index 3d3d47e93..785b8651f 100644
--- a/src/iptux-core/meson.build
+++ b/src/iptux-core/meson.build
@@ -1,6 +1,5 @@
 glib_dep = dependency('glib-2.0', version: '>=2.35')
 jsoncpp_dep = dependency('jsoncpp', version: '>=1.0')
-gflags_dep = dependency('gflags')
 sigc_dep = dependency('sigc++-2.0')
 
 core_sources = files([
@@ -35,14 +34,14 @@ thread_dep = dependency('threads')
 if get_option('static-link')
     libiptux_core = static_library('iptux-core',
         core_sources,
-        dependencies: [glib_dep, jsoncpp_dep, gflags_dep, sigc_dep, thread_dep],
+        dependencies: [glib_dep, jsoncpp_dep, sigc_dep, thread_dep],
         link_with: [libiptux_utils],
         include_directories: inc,
     )
 else
     libiptux_core = shared_library('iptux-core',
         core_sources,
-        dependencies: [glib_dep, jsoncpp_dep, gflags_dep, sigc_dep, thread_dep],
+        dependencies: [glib_dep, jsoncpp_dep, sigc_dep, thread_dep],
         link_with: [libiptux_utils],
         include_directories: inc,
         install: true,
@@ -55,7 +54,6 @@ else
         name: 'iptux-core',
         version: meson.project_version(),
         requires: [jsoncpp_dep, glib_dep, sigc_dep, thread_dep],
-        requires_private: [gflags_dep],
         libraries: [libiptux_core],
     )
 endif
diff --git a/src/iptux/meson.build b/src/iptux/meson.build
index 0965f443d..a790d3129 100644
--- a/src/iptux/meson.build
+++ b/src/iptux/meson.build
@@ -1,6 +1,5 @@
 gtk_dep = dependency('gtk+-3.0', version: '>=3.22')
 jsoncpp_dep = dependency('jsoncpp', version: '>=1.0')
-gflags_dep = dependency('gflags')
 sigc_dep = dependency('sigc++-2.0')
 
 sources = files([
@@ -25,7 +24,7 @@ sources = files([
     'UiModels.cpp',
     'WindowConfig.cpp',
 ])
-dependencies = [gtk_dep, jsoncpp_dep, gflags_dep, sigc_dep]
+dependencies = [gtk_dep, jsoncpp_dep, sigc_dep]
 
 if appindicator_dep.found()
     sources += ['AppIndicator.cpp']
diff --git a/src/main/meson.build b/src/main/meson.build
index 6a4c28814..79f3f27d8 100644
--- a/src/main/meson.build
+++ b/src/main/meson.build
@@ -2,7 +2,7 @@ iptux_sources = files(['iptux.cpp', 'iptux_crash_utils.cpp'])
 executable('iptux',
     iptux_sources,
     include_directories: inc,
-    dependencies: [gtk_dep, jsoncpp_dep, thread_dep, gflags_dep, sigc_dep],
+    dependencies: [gtk_dep, jsoncpp_dep, thread_dep, sigc_dep],
     link_with: [libiptux],
     export_dynamic: true,
     install: true,

From b414eb714986c0fd4ba92a2aabdd0e948ae14008 Mon Sep 17 00:00:00 2001
From: LI Daobing <lidaobing@gmail.com>
Date: Mon, 17 Feb 2025 16:09:32 -0800
Subject: [PATCH 3/5] 1

---
 src/iptux-core/CoreThread.cpp | 1 +
 src/iptux/LogSystem.cpp       | 1 +
 2 files changed, 2 insertions(+)

diff --git a/src/iptux-core/CoreThread.cpp b/src/iptux-core/CoreThread.cpp
index c94b7c996..e0efe587e 100644
--- a/src/iptux-core/CoreThread.cpp
+++ b/src/iptux-core/CoreThread.cpp
@@ -27,6 +27,7 @@
 #include "iptux-core/internal/support.h"
 #include "iptux-utils/output.h"
 #include "iptux-utils/utils.h"
+#include <unistd.h>
 
 using namespace std;
 using namespace std::placeholders;
diff --git a/src/iptux/LogSystem.cpp b/src/iptux/LogSystem.cpp
index 5f21271cd..e6afd88a5 100644
--- a/src/iptux/LogSystem.cpp
+++ b/src/iptux/LogSystem.cpp
@@ -17,6 +17,7 @@
 
 #include "iptux-core/Const.h"
 #include "iptux-utils/utils.h"
+#include <unistd.h>
 
 #define LOG_START_HEADER "====================================="
 #define LOG_END_HEADER "-------------------------------------"

From a2e3cef578979290600a24ce6f189f72aa57539d Mon Sep 17 00:00:00 2001
From: LI Daobing <lidaobing@gmail.com>
Date: Mon, 17 Feb 2025 16:14:53 -0800
Subject: [PATCH 4/5] 1

---
 src/iptux/UiModels.cpp | 1 +
 1 file changed, 1 insertion(+)

diff --git a/src/iptux/UiModels.cpp b/src/iptux/UiModels.cpp
index 72cc5b20f..69fd4683b 100644
--- a/src/iptux/UiModels.cpp
+++ b/src/iptux/UiModels.cpp
@@ -11,6 +11,7 @@
 #include <glib/gi18n.h>
 #include <inttypes.h>
 #include <netinet/in.h>
+#include <sstream>
 
 using namespace std;
 

From 8e9c8ffbcf7e102604fe8c9ff3de1e46485da165 Mon Sep 17 00:00:00 2001
From: LI Daobing <lidaobing@gmail.com>
Date: Mon, 17 Feb 2025 16:20:08 -0800
Subject: [PATCH 5/5] 1

---
 examples/count-bot/main.cpp | 7 +++----
 1 file changed, 3 insertions(+), 4 deletions(-)

diff --git a/examples/count-bot/main.cpp b/examples/count-bot/main.cpp
index 29efcbfdd..fd174d65b 100644
--- a/examples/count-bot/main.cpp
+++ b/examples/count-bot/main.cpp
@@ -1,11 +1,10 @@
 #include <cstdio>
 #include <iostream>
-#include <string>
-
-#include <unistd.h>
-
 #include <iptux-core/CoreThread.h>
 #include <iptux-core/Exception.h>
+#include <sstream>
+#include <string>
+#include <unistd.h>
 
 using namespace std;
 using namespace iptux;
