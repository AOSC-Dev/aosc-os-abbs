abinfo "Installing systemd services..."
install -Dvm644 "$SRCDIR/data/supergfxd.service" \
    "$PKGDIR/usr/lib/systemd/system/supergfxd.service"

abinfo "Installing D-Bus policy configuration..."
install -Dvm644 "$SRCDIR/data/org.supergfxctl.Daemon.conf" \
    "$PKGDIR/usr/share/dbus-1/system.d/org.supergfxctl.Daemon.conf"
