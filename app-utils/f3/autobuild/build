abinfo "Building f3 ..."
make all extra

abinfo "Installing f3 ..."
make install install-extra \
    DESTDIR="$PKGDIR" \
    PREFIX="/usr"

install -Dvm755 "$SRCDIR"/f3write.h2w -t "$PKGDIR"/usr/bin
install -Dvm755 "$SRCDIR"/log-f3wr -t "$PKGDIR"/usr/bin
