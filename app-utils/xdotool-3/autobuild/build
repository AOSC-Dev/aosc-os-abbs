abinfo "Building xdotool-3 ..."
make

abinfo "Installing xdotool-3 (runtime only) ..."
make installlib \
    DESTDIR="$PKGDIR" \
    PREFIX="/usr"

abinfo "Dropping unversioned library ..."
rm -v "$PKGDIR"/usr/lib/libxdo.so
