abinfo "Dropping duplicated files from libpng ..."
rm -v "$PKGDIR"/usr/share/man/man5/pbm.5
rm -v "$PKGDIR"/usr/share/man/man5/pgm.5
rm -v "$PKGDIR"/usr/bin/pnmtopng
rm -v "$PKGDIR"/usr/bin/pngtopnm

abinfo "Dropping unexpected files ..."
rm -rv "$PKGDIR"/usr/staticlink
rm -rv "$PKGDIR"/usr/sharedlink

# Note: Update this with SONAME changes.
abinfo "Adding a symlink to enable linking ..."
ln -sv libnetpbm.so.11 \
    "$PKGDIR"/usr/lib/libnetpbm.so
