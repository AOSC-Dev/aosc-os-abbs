PKGNAME=dialog
PKGSEC=utils
PKGDEP="bash ncurses"
PKGDES="A tool to display dialog boxes from shell scripts"

AUTOTOOLS_AFTER="--with-ncursesw --enable-nls"
MAKE_AFTER="install-full"

RECONF=0

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
