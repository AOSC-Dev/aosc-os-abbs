# Note: Electron binary is unnecessary for building this package.
export ELECTRON_SKIP_BINARY_DOWNLOAD=1

abinfo "Building asar ..."
yarn install --verbose
yarn build

abinfo "Installing asar ..."
npm install \
    --verbose \
    --install-links \
    --global \
    --prefix "${PKGDIR}"/usr \
    "${SRCDIR}"
