abinfo "Installing default configuration  ..."
install -Dvm644 "$SRCDIR"/debian/hd-idle.default \
    "$PKGDIR"/etc/conf.d/hd-idle

abinfo "Moving binary executables to /usr/bin ..."
mv -v "$PKGDIR"/usr/sbin \
    "$PKGDIR"/usr/bin
