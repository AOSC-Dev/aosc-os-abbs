PKGNAME=pciutils
PKGDES="PCI bus configuration space access library and tools"
PKGDEP="glibc hwdata kmod"
PKGSEC=utils

PKGPROV="lspci"

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
