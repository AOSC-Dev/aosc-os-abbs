PKGNAME=chrpath
PKGSEC=utils
PKGDES="A utility for changing rpath in ELF formatted files"
PKGDEP="glibc"


# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
