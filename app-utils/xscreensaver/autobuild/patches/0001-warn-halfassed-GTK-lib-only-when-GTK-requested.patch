From f92a90bdeeafcd05d2554696c1632d1c61e6b597 Mon Sep 17 00:00:00 2001
From: Student Main <studentmain@aosc.io>
Date: Thu, 27 Aug 2026 00:21:55 +0800
Subject: [PATCH] warn halfassed GTK lib only when GTK requested

---
 configure    | 1000 +++++++++++++++++++++++++++++++++++++++++++++++---
 configure.ac |    2 +-
 2 files changed, 949 insertions(+), 53 deletions(-)

diff --git a/configure b/configure
index 2901414..2bb098b 100755
--- a/configure
+++ b/configure
@@ -33,6 +33,8 @@ esac ;;
 esac
 fi
 
+
+
 # Reset variables that may have inherited troublesome values from
 # the environment.
 
@@ -79,6 +81,7 @@ if ${PATH_SEPARATOR+false} :; then
   }
 fi
 
+
 # Find who we are.  Look in the path if we contain no directory separator.
 as_myself=
 case $0 in #((
@@ -108,6 +111,7 @@ if test ! -f "$as_myself"; then
   exit 1
 fi
 
+
 # Use a proper internal environment variable to ensure we don't fall
   # into an infinite loop, continuously re-executing ourselves.
   if test x"${_as_can_reexec}" != xno && test "x$CONFIG_SHELL" != x; then
@@ -232,6 +236,7 @@ fi ;;
 esac
 fi
 
+
       if test "x$CONFIG_SHELL" != x
 then :
   export CONFIG_SHELL
@@ -291,6 +296,7 @@ as_fn_unset ()
 }
 as_unset=as_fn_unset
 
+
 # as_fn_set_status STATUS
 # -----------------------
 # Set $? to STATUS, without forking.
@@ -354,6 +360,7 @@ printf "%s\n" X"$as_dir" |
     test -z "$as_dirs" || eval "mkdir $as_dirs"
   } || test -d "$as_dir" || as_fn_error $? "cannot create directory $as_dir"
 
+
 } # as_fn_mkdir_p
 
 # as_fn_executable_p FILE
@@ -402,6 +409,7 @@ else case e in #(
 esac
 fi # as_fn_arith
 
+
 # as_fn_error STATUS ERROR [LINENO LOG_FD]
 # ----------------------------------------
 # Output "`basename $0`: error: ERROR" to stderr. If LINENO and LOG_FD are
@@ -463,6 +471,7 @@ as_cr_Letters=$as_cr_letters$as_cr_LETTERS
 as_cr_digits='0123456789'
 as_cr_alnum=$as_cr_Letters$as_cr_digits
 
+
   as_lineno_1=$LINENO as_lineno_1a=$LINENO
   as_lineno_2=$LINENO as_lineno_2a=$LINENO
   eval 'test "x$as_lineno_1'$as_run'" != "x$as_lineno_2'$as_run'" &&
@@ -500,6 +509,7 @@ as_cr_alnum=$as_cr_Letters$as_cr_digits
   exit
 }
 
+
 # Determine whether it's possible to make 'echo' print without a newline.
 # These variables are no longer used directly by Autoconf, but are AC_SUBSTed
 # for compatibility with existing Makefiles.
@@ -567,6 +577,7 @@ as_tr_cpp="eval sed '$as_sed_cpp'" # deprecated
 as_sed_sh="y%*+%pp%;s%[^_$as_cr_alnum]%_%g"
 as_tr_sh="eval sed '$as_sed_sh'" # deprecated
 
+
 test -n "$DJDIR" || exec 7<&0 </dev/null
 exec 6>&1
 
@@ -907,6 +918,7 @@ CPPFLAGS
 CPP
 XMKMF'
 
+
 # Initialize some variables set by options.
 ac_init_help=
 ac_init_version=false
@@ -1378,6 +1390,7 @@ test -n "$host_alias" && ac_tool_prefix=$host_alias-
 
 test "$silent" = yes && exec 6>/dev/null
 
+
 ac_pwd=`pwd` && test -n "$ac_pwd" &&
 ac_ls_di=`ls -di .` &&
 ac_pwd_ls_di=`cd "$ac_pwd" && ls -di .` ||
@@ -1385,6 +1398,7 @@ ac_pwd_ls_di=`cd "$ac_pwd" && ls -di .` ||
 test "X$ac_ls_di" = "X$ac_pwd_ls_di" ||
   as_fn_error $? "pwd does not report name of working directory"
 
+
 # Find the source files, if location was not specified.
 if test -z "$srcdir"; then
   ac_srcdir_defaulted=yes
@@ -1462,6 +1476,8 @@ Defaults for the options are specified in brackets.
 
 Configuration:
   -h, --help              display this help and exit
+      --help=short        display options specific to this package
+      --help=recursive    display the short help of all the included packages
   -V, --version           display version information and exit
   -q, --quiet, --silent   do not print 'checking ...' messages
       --cache-file=FILE   cache test results in FILE [disabled]
@@ -1475,6 +1491,42 @@ Installation directories:
   --exec-prefix=EPREFIX   install architecture-dependent files in EPREFIX
                           [PREFIX]
 
+By default, 'make install' will install all the files in
+'$ac_default_prefix/bin', '$ac_default_prefix/lib' etc.  You can specify
+an installation prefix other than '$ac_default_prefix' using '--prefix',
+for instance '--prefix=\$HOME'.
+
+For better control, use the options below.
+
+Fine tuning of the installation directories:
+  --bindir=DIR            user executables [EPREFIX/bin]
+  --sbindir=DIR           system admin executables [EPREFIX/sbin]
+  --libexecdir=DIR        program executables [EPREFIX/libexec]
+  --sysconfdir=DIR        read-only single-machine data [PREFIX/etc]
+  --sharedstatedir=DIR    modifiable architecture-independent data [PREFIX/com]
+  --localstatedir=DIR     modifiable single-machine data [PREFIX/var]
+  --runstatedir=DIR       modifiable per-process data [LOCALSTATEDIR/run]
+  --libdir=DIR            object code libraries [EPREFIX/lib]
+  --includedir=DIR        C header files [PREFIX/include]
+  --oldincludedir=DIR     C header files for non-gcc [/usr/include]
+  --datarootdir=DIR       read-only arch.-independent data root [PREFIX/share]
+  --datadir=DIR           read-only architecture-independent data [DATAROOTDIR]
+  --infodir=DIR           info documentation [DATAROOTDIR/info]
+  --localedir=DIR         locale-dependent data [DATAROOTDIR/locale]
+  --mandir=DIR            man documentation [DATAROOTDIR/man]
+  --docdir=DIR            documentation root [DATAROOTDIR/doc/PACKAGE]
+  --htmldir=DIR           html documentation [DOCDIR]
+  --dvidir=DIR            dvi documentation [DOCDIR]
+  --pdfdir=DIR            pdf documentation [DOCDIR]
+  --psdir=DIR             ps documentation [DOCDIR]
+_ACEOF
+
+  cat <<\_ACEOF
+
+X features:
+  --x-includes=DIR    X include files are in DIR
+  --x-libraries=DIR   X library files are in DIR
+
 System types:
   --build=BUILD     configure for building on BUILD [guessed]
   --host=HOST       cross-compile to build programs to run on HOST [BUILD]
@@ -1486,6 +1538,26 @@ if test -n "$ac_init_help"; then
 
   cat <<\_ACEOF
 
+Optional Features:
+  --disable-option-checking  ignore unrecognized --enable/--with options
+  --disable-FEATURE       do not include FEATURE (same as --enable-FEATURE=no)
+  --enable-FEATURE[=ARG]  include FEATURE [ARG=yes]
+  --disable-largefile     omit support for large files
+  --disable-nls           do not use Native Language Support
+  --disable-rpath         do not hardcode runtime library paths
+
+  --enable-year2038       support timestamps after 2038
+
+Optional Packages:
+  --with-PACKAGE[=ARG]    use PACKAGE [ARG=yes]
+  --without-PACKAGE       do not use PACKAGE (same as --with-PACKAGE=no)
+  --with-x                use the X Window System
+  --with-gnu-ld           assume the C compiler uses GNU ld [default=no]
+  --with-libiconv-prefix[=DIR]  search for libiconv in DIR/include and DIR/lib
+  --without-libiconv-prefix     don't search for libiconv in includedir and libdir
+  --with-libintl-prefix[=DIR]  search for libintl in DIR/include and DIR/lib
+  --without-libintl-prefix     don't search for libintl in includedir and libdir
+
 Installation Options:
 
   --prefix=PREFIX         Install root of architecture-independent files.
@@ -1502,6 +1574,7 @@ Server Extension Options:
   --with-dpms-ext         Include support for power management.
   --with-xinerama-ext     Include support for multiple monitors.
   --with-randr-ext        Include support for multiple monitors.
+  --with-xinput-ext       The XInput2 extension is required.
   --with-xshm-ext         Include support for the Shared Memory extension.
   --with-xdbe-ext         Include support for the DOUBLE-BUFFER extension.
   --with-xkb-ext          Include support for the X Keyboard extension.
@@ -1528,6 +1601,7 @@ User Interface Options:
   --with-text-file=FILE   Some demos will use this as a source of text.
   --with-browser=BROWSER  The web browser used to open the "Help" URL.
 
+
 Graphics Options:
 
   --with-gles             Emulate OpenGL 1.3 in terms of OpenGL ES 1.x.
@@ -1553,10 +1627,12 @@ Some influential environment variables:
   CPPFLAGS    (Objective) C/C++ preprocessor flags, e.g. -I<include dir> if
               you have headers in a nonstandard directory <include dir>
   CPP         C preprocessor
+  XMKMF       Path to xmkmf, Makefile generator for X Window System
 
 Use these variables to override the choices made by 'configure' or to help
 it to find libraries and programs with nonstandard names/locations.
 
+Report bugs to the package provider.
 _ACEOF
 ac_status=$?
 fi
@@ -1640,7 +1716,7 @@ fi
 ac_fn_c_try_compile ()
 {
   as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
-  rm -rf conftest.$ac_objext conftest.beam
+  rm -f conftest.$ac_objext conftest.beam
   if { { ac_try="$ac_compile"
 case "(($ac_try" in
   *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
@@ -1813,7 +1889,7 @@ printf "%s\n" "$ac_res" >&6; }
 ac_fn_c_try_link ()
 {
   as_lineno=${as_lineno-"$1"} as_lineno_stack=as_lineno_stack=$as_lineno_stack
-  rm -rf conftest.$ac_objext conftest.beam conftest$ac_exeext
+  rm -f conftest.$ac_objext conftest.beam conftest$ac_exeext
   if { { ac_try="$ac_link"
 case "(($ac_try" in
   *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
@@ -2098,12 +2174,14 @@ IFS=$as_save_IFS
 
 cat >&5 <<_ACEOF
 
+
 ## ----------- ##
 ## Core tests. ##
 ## ----------- ##
 
 _ACEOF
 
+
 # Keep a trace of the command line.
 # Strip out --no-create and --no-recursion so they do not pile up.
 # Strip out --silent because we don't want to record it for future runs.
@@ -2270,6 +2348,7 @@ printf "%s\n" "#define PACKAGE_BUGREPORT \"$PACKAGE_BUGREPORT\"" >>confdefs.h
 
 printf "%s\n" "#define PACKAGE_URL \"$PACKAGE_URL\"" >>confdefs.h
 
+
 # Let the site file select an alternate cache file if it wants to.
 # Prefer an explicitly selected file to automatically selected ones.
 if test -n "$CONFIG_SITE"; then
@@ -2736,6 +2815,7 @@ else case e in #(
 esac
 fi
 
+
 # These three variables are undocumented and unsupported,
 # and are intended to be withdrawn in a future Autoconf release.
 # They can cause serious problems if a builder's source tree is in a directory
@@ -2818,8 +2898,11 @@ ac_compile='$CC -c $CFLAGS $CPPFLAGS conftest.$ac_ext >&5'
 ac_link='$CC -o conftest$ac_exeext $CFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
 ac_compiler_gnu=$ac_cv_c_compiler_gnu
 
+
+
 ac_config_headers="$ac_config_headers config.h"
 
+
 echo "current directory: `pwd`"
 echo "command line was: $0 $@"
 
@@ -2834,6 +2917,8 @@ fi
 #
 ###############################################################################
 
+
+
 # Defines in config.h are dumped out alphabetically rather than in any kind
 # of sane order, sigh.
 
@@ -2841,38 +2926,153 @@ fi
 # OS stuff.
 #
 
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
 #
 # X11 server stuff.
 #
 
+
+
+
+
+
 #AH_TEMPLATE([HAVE_XF86VMODE_GAMMA],
 #	    [Define this if you have XF86VidModeSetGamma, for fading.])
 
 #AH_TEMPLATE([HAVE_XF86VMODE_GAMMA_RAMP],
 #	    [Define this if you have XF86VidModeSetGammaRamp, for fading.])
 
+
+
+
+
+
+
+
+
+
+
+
+
 #AH_TEMPLATE([HAVE_XCOMPOSITE_EXTENSION],
 #	    [Define this if you have the X Composite Extension.])
 
+
+
+
+
+
+
+
+
 #
 # GUI stuff.
 #
 
+
+
+
+
+
+
+
+
+
+
+
+
 #
 # Image loading.
 #
 
+
+
+
+
+
+
+
 #
 # Graphics libraries.
 #
 
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
 #
 # Locking.
 #
 
+
+
+
+
+
+
+
+
+
+
+
+
+
 #AH_TEMPLATE([ALLOW_ROOT_PASSWD],
 #	    [Define this to allow root to unlock, when not using PAM.])
 
+
+
+
+
+
+
+
+
+
+
+
+
+
 # After checking to see that --srcdir is correct (which AC_INIT does)
 # check for some random other files that come later in the tar file,
 # to make sure everything is here.
@@ -2894,18 +3094,27 @@ done
 #
 ###############################################################################
 
+
+
+
 ###############################################################################
 #
 #       Function to figure out how to run the compiler.
 #
 ###############################################################################
 
+
+
+
 ###############################################################################
 #
 #       Function to figure out how to create directory trees.
 #
 ###############################################################################
 
+
+
+
 ###############################################################################
 #
 #       Function to check whether gettimeofday() exists, and how to call it.
@@ -2913,6 +3122,9 @@ done
 #
 ###############################################################################
 
+
+
+
 ###############################################################################
 #
 #       Function to find perl5 (defines PERL and PERL_VERSION.)
@@ -2923,12 +3135,23 @@ done
 
 perl_version_cmd='print $]'
 
+
+
+
+
+
+
 ###############################################################################
 #
 #       Functions to check how to do ICMP PING requests.
 #
 ###############################################################################
 
+
+
+
+
+
 ###############################################################################
 #
 #       Functions to check for various X11 crap.
@@ -2940,10 +3163,29 @@ perl_version_cmd='print $]'
 # (AC_PATH_X_APP_DEFAULTS_XMKMF is no longer used.)
 #
 
+
+
+
+
+
+
+
+
+
+
+
 # Random special-cases for X on certain pathological OSes.
 # You know who you are.
 #
 
+
+
+
+
+
+
+
+
 ###############################################################################
 #
 #       Some utility functions to make checking for X things easier.
@@ -2953,20 +3195,28 @@ perl_version_cmd='print $]'
 # Like AC_CHECK_HEADER, but it uses the already-computed -I directories.
 #
 
+
 # Like AC_EGREP_HEADER, but it uses the already-computed -I directories.
 #
 
+
 # Like AC_COMPILE_IFELSE but it uses the already-computed -I directories.
 #
 
+
+
 # Like AC_CHECK_LIB, but it uses the already-computed -I and -L directories.
 # Use this sparingly; it probably doesn't work very well on X programs.
 #
 
+
 # Like AC_RUN_IFELSE but it uses the already-computed -I directories.
 # (But not the -L directories!)
 #
 
+
+
+
 # Usage: HANDLE_X_PATH_ARG([variable_name],
 #                          [--command-line-option],
 #                          [descriptive string])
@@ -2983,6 +3233,8 @@ perl_version_cmd='print $]'
 # as appropriate.
 #
 
+
+
 ###############################################################################
 ###############################################################################
 #
@@ -2995,12 +3247,15 @@ perl_version_cmd='print $]'
 #
 export PATH="$PATH:/usr/bin:/bin:/usr/sbin:/sbin"
 
+
 # WTF!  autoconf emits this *way* too late.  Do it earlier.
 test "x$prefix" = xNONE && prefix=$ac_default_prefix
 test "x$exec_prefix" = xNONE && exec_prefix='${prefix}'
 
 # random compiler setup
 
+
+
   # Make sure we can run config.sub.
 $SHELL "${ac_aux_dir}config.sub" sun4 >/dev/null 2>&1 ||
   as_fn_error $? "cannot run $SHELL ${ac_aux_dir}config.sub" "$LINENO" 5
@@ -3040,6 +3295,7 @@ build_os=$*
 IFS=$ac_save_IFS
 case $build_os in *\ *) build_os=`echo "$build_os" | sed 's/ /-/g'`;; esac
 
+
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking host system type" >&5
 printf %s "checking host system type... " >&6; }
 if test ${ac_cv_host+y}
@@ -3074,8 +3330,18 @@ host_os=$*
 IFS=$ac_save_IFS
 case $host_os in *\ *) host_os=`echo "$host_os" | sed 's/ /-/g'`;; esac
 
+
+
 ac_original_cc=$CC
 
+
+
+
+
+
+
+
+
 ac_ext=c
 ac_cpp='$CPP $CPPFLAGS'
 ac_compile='$CC -c $CFLAGS $CPPFLAGS conftest.$ac_ext >&5'
@@ -3124,6 +3390,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
 fi
 if test -z "$ac_cv_prog_CC"; then
   ac_ct_CC=$CC
@@ -3228,6 +3495,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
   fi
 fi
 if test -z "$CC"; then
@@ -3290,6 +3558,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
 fi
 if test -z "$CC"; then
   if test -n "$ac_tool_prefix"; then
@@ -3337,6 +3606,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
     test -n "$CC" && break
   done
 fi
@@ -3386,6 +3656,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
   test -n "$ac_ct_CC" && break
 done
 
@@ -3447,6 +3718,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
 fi
 if test -z "$ac_cv_prog_CC"; then
   ac_ct_CC=$CC
@@ -3509,6 +3781,7 @@ fi
 
 fi
 
+
 test -z "$CC" && { { printf "%s\n" "$as_me:${as_lineno-$LINENO}: error: in '$ac_pwd':" >&5
 printf "%s\n" "$as_me: error: in '$ac_pwd':" >&2;}
 as_fn_error $? "no acceptable C compiler found in \$PATH
@@ -3534,7 +3807,7 @@ printf "%s\n" "$ac_try_echo"; } >&5
          10q' conftest.err >conftest.er1
     cat conftest.er1 >&5
   fi
-  rm -rf conftest.er1 conftest.err
+  rm -f conftest.er1 conftest.err
   printf "%s\n" "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }
 done
@@ -3678,11 +3951,11 @@ as_fn_error $? "cannot compute suffix of executables: cannot compile and link
 See 'config.log' for more details" "$LINENO" 5; } ;;
 esac
 fi
-rm -rf conftest conftest$ac_cv_exeext
+rm -f conftest conftest$ac_cv_exeext
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $ac_cv_exeext" >&5
 printf "%s\n" "$ac_cv_exeext" >&6; }
 
-rm -rf conftest.$ac_ext
+rm -f conftest.$ac_ext
 EXEEXT=$ac_cv_exeext
 ac_exeext=$EXEEXT
 cat confdefs.h - <<_ACEOF >conftest.$ac_ext
@@ -3744,7 +4017,7 @@ fi
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $cross_compiling" >&5
 printf "%s\n" "$cross_compiling" >&6; }
 
-rm -rf conftest.$ac_ext conftest$ac_cv_exeext \
+rm -f conftest.$ac_ext conftest$ac_cv_exeext \
   conftest.o conftest.obj conftest.out
 ac_clean_files=$ac_clean_files_save
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for suffix of object files" >&5
@@ -3764,7 +4037,7 @@ main (void)
   return 0;
 }
 _ACEOF
-rm -rf conftest.o conftest.obj
+rm -f conftest.o conftest.obj
 if { { ac_try="$ac_compile"
 case "(($ac_try" in
   *\"* | *\`* | *\\*) ac_try_echo=\$ac_try;;
@@ -3795,7 +4068,7 @@ as_fn_error $? "cannot compute suffix of object files: cannot compile
 See 'config.log' for more details" "$LINENO" 5; } ;;
 esac
 fi
-rm -rf conftest.$ac_cv_objext conftest.$ac_ext ;;
+rm -f conftest.$ac_cv_objext conftest.$ac_ext ;;
 esac
 fi
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $ac_cv_objext" >&5
@@ -3955,7 +4228,7 @@ fi
 rm -f core conftest.err conftest.$ac_objext conftest.beam
   test "x$ac_cv_prog_cc_c11" != "xno" && break
 done
-rm -rf conftest.$ac_ext
+rm -f conftest.$ac_ext
 CC=$ac_save_CC ;;
 esac
 fi
@@ -4004,7 +4277,7 @@ fi
 rm -f core conftest.err conftest.$ac_objext conftest.beam
   test "x$ac_cv_prog_cc_c99" != "xno" && break
 done
-rm -rf conftest.$ac_ext
+rm -f conftest.$ac_ext
 CC=$ac_save_CC ;;
 esac
 fi
@@ -4053,7 +4326,7 @@ fi
 rm -f core conftest.err conftest.$ac_objext conftest.beam
   test "x$ac_cv_prog_cc_c89" != "xno" && break
 done
-rm -rf conftest.$ac_ext
+rm -f conftest.$ac_ext
 CC=$ac_save_CC ;;
 esac
 fi
@@ -4085,6 +4358,8 @@ ac_compile='$CC -c $CFLAGS $CPPFLAGS conftest.$ac_ext >&5'
 ac_link='$CC -o conftest$ac_exeext $CFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
 ac_compiler_gnu=$ac_cv_c_compiler_gnu
 
+
+
   if test -z "$GCC"; then
     # not using GCC
     { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for extra compiler flags" >&5
@@ -4394,7 +4669,7 @@ else case e in #(
 continue ;;
 esac
 fi
-rm -rf conftest.err conftest.i conftest.$ac_ext
+rm -f conftest.err conftest.i conftest.$ac_ext
 
   # OK, works on sane cases.  Now check whether nonexistent headers
   # can be detected and how.
@@ -4412,11 +4687,11 @@ ac_preproc_ok=:
 break ;;
 esac
 fi
-rm -rf conftest.err conftest.i conftest.$ac_ext
+rm -f conftest.err conftest.i conftest.$ac_ext
 
 done
 # Because of 'break', _AC_PREPROC_IFELSE's cleaning code was skipped.
-rm -rf conftest.i conftest.err conftest.$ac_ext
+rm -f conftest.i conftest.err conftest.$ac_ext
 if $ac_preproc_ok
 then :
   break
@@ -4453,7 +4728,7 @@ else case e in #(
 continue ;;
 esac
 fi
-rm -rf conftest.err conftest.i conftest.$ac_ext
+rm -f conftest.err conftest.i conftest.$ac_ext
 
   # OK, works on sane cases.  Now check whether nonexistent headers
   # can be detected and how.
@@ -4471,11 +4746,11 @@ ac_preproc_ok=:
 break ;;
 esac
 fi
-rm -rf conftest.err conftest.i conftest.$ac_ext
+rm -f conftest.err conftest.i conftest.$ac_ext
 
 done
 # Because of 'break', _AC_PREPROC_IFELSE's cleaning code was skipped.
-rm -rf conftest.i conftest.err conftest.$ac_ext
+rm -f conftest.i conftest.err conftest.$ac_ext
 if $ac_preproc_ok
 then :
 
@@ -4493,6 +4768,8 @@ ac_compile='$CC -c $CFLAGS $CPPFLAGS conftest.$ac_ext >&5'
 ac_link='$CC -o conftest$ac_exeext $CFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
 ac_compiler_gnu=$ac_cv_c_compiler_gnu
 
+
+
 # stuff for Makefiles
 
   # Find a good install program.  We prefer a C program (faster),
@@ -4671,7 +4948,7 @@ case `${MAKE-make} -f conftest.make 2>/dev/null` in
   *)
     eval ac_cv_prog_make_${ac_make}_set=no;;
 esac
-rm -rf conftest.make ;;
+rm -f conftest.make ;;
 esac
 fi
 if eval test \$ac_cv_prog_make_${ac_make}_set = yes; then
@@ -4684,6 +4961,7 @@ printf "%s\n" "no" >&6; }
   SET_MAKE="MAKE=${MAKE-make}"
 fi
 
+
 # By default, autoconf sets INSTALL_SCRIPT to '${INSTALL_PROGRAM}'.
 # That's wrong: it should be set to '${INSTALL}', so that one can
 # implement the "install-strip" target properly (strip executables,
@@ -4709,6 +4987,13 @@ do
   fi
 done
 
+
+
+
+
+
+
+
 if test $ac_cv_header_stdlib_h = yes && test $ac_cv_header_string_h = yes
 then :
 
@@ -4739,6 +5024,7 @@ printf "%s\n" "#define mode_t int" >>confdefs.h
 esac
 fi
 
+
   ac_fn_c_check_type "$LINENO" "pid_t" "ac_cv_type_pid_t" "$ac_includes_default
 "
 if test "x$ac_cv_type_pid_t" = xyes
@@ -4776,6 +5062,7 @@ printf "%s\n" "#define pid_t $ac_pid_type" >>confdefs.h
 esac
 fi
 
+
 ac_fn_c_check_type "$LINENO" "size_t" "ac_cv_type_size_t" "$ac_includes_default"
 if test "x$ac_cv_type_size_t" = xyes
 then :
@@ -4787,6 +5074,7 @@ printf "%s\n" "#define size_t unsigned int" >>confdefs.h
 esac
 fi
 
+
 printf "%s\n" "#define RETSIGTYPE void" >>confdefs.h
 
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for sys/wait.h that is POSIX.1 compatible" >&5
@@ -5393,6 +5681,7 @@ then :
 
 printf "%s\n" "#define HAVE_STRUCT_SOCKADDR_SA_LEN 1" >>confdefs.h
 
+
 fi
 
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for struct icmp" >&5
@@ -5657,6 +5946,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
   test -n "$PERL" && break
 done
 
@@ -5724,9 +6014,11 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
   test -n "$TAR" && break
 done
 
+
 if test -z "$PERL" ; then
   # don't let it be blank...
   PERL=/usr/bin/perl
@@ -5734,9 +6026,11 @@ fi
 
 if test -z "$TAR" ; then TAR=tar ; fi
 
+
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for X" >&5
 printf %s "checking for X... " >&6; }
 
+
 # Check whether --with-x was given.
 if test ${with_x+y}
 then :
@@ -5886,7 +6180,7 @@ else case e in #(
 done ;;
 esac
 fi
-rm -rf conftest.err conftest.i conftest.$ac_ext
+rm -f conftest.err conftest.i conftest.$ac_ext
 fi # $ac_x_includes = no
 
 if test "$ac_x_libraries" = no; then
@@ -6532,6 +6826,7 @@ fi
 
 fi
 
+
 if test "$have_x" != yes; then
   as_fn_error $? "Couldn't find X11 headers/libs.  Try \`$0 --help'." "$LINENO" 5
 fi
@@ -6623,6 +6918,7 @@ case "$host" in
         X_LIBS="-L/opt/Mesa/lib $X_LIBS"
       fi
 
+
       # On HPUX, default to installing in /opt/xscreensaver/ instead of
       # in /usr/local/, unless there is already an xscreensaver in
       # /usr/local/bin/.  This can be overridden with the --prefix arg
@@ -6805,6 +7101,7 @@ fi
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $ac_cv_libxt" >&5
 printf "%s\n" "$ac_cv_libxt" >&6; }
 
+
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking whether this is macOS" >&5
 printf %s "checking whether this is macOS... " >&6; }
   ac_macosx=no
@@ -6820,6 +7117,7 @@ printf %s "checking whether this is macOS... " >&6; }
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $ac_macosx" >&5
 printf "%s\n" "$ac_macosx" >&6; }
 
+
 ###############################################################################
 #
 #       pkg-config
@@ -6875,6 +7173,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
 fi
 if test -z "$ac_cv_path_pkg_config"; then
   ac_pt_pkg_config=$pkg_config
@@ -6938,6 +7237,7 @@ else
   pkg_config="$ac_cv_path_pkg_config"
 fi
 
+
 if test -z "$pkg_config" ; then
   { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: pkg-config not found!" >&5
 printf "%s\n" "$as_me: WARNING: pkg-config not found!" >&2;}
@@ -6975,6 +7275,7 @@ printf "%s\n" "no" >&6; }
   fi
 }
 
+
 ###############################################################################
 #
 #       Gettext support
@@ -6989,6 +7290,7 @@ printf "%s\n" "no" >&6; }
 # that uses wildcards, and write a sane set of rules to detect gettext(3) and
 # msgfmt(1) and just do the obviously straightforward thing?
 
+
   { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking whether NLS is requested" >&5
 printf %s "checking whether NLS is requested... " >&6; }
     # Check whether --enable-nls was given.
@@ -7003,6 +7305,9 @@ fi
   { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $USE_NLS" >&5
 printf "%s\n" "$USE_NLS" >&6; }
 
+
+
+
 case "$am__api_version" in
     1.01234)
 	as_fn_error $? "Automake 1.5 or newer is required to use intltool" "$LINENO" 5
@@ -7068,6 +7373,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
 # Extract the first word of "intltool-merge", so it can be a program name with args.
 set dummy intltool-merge; ac_word=$2
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for $ac_word" >&5
@@ -7113,6 +7419,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
 # Extract the first word of "intltool-extract", so it can be a program name with args.
 set dummy intltool-extract; ac_word=$2
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for $ac_word" >&5
@@ -7158,6 +7465,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
 if test -z "$INTLTOOL_UPDATE" -o -z "$INTLTOOL_MERGE" -o -z "$INTLTOOL_EXTRACT"; then
     as_fn_error $? "The intltool scripts were not found. Please install intltool." "$LINENO" 5
 fi
@@ -7166,37 +7474,140 @@ if test -z "$AM_DEFAULT_VERBOSITY"; then
   AM_DEFAULT_VERBOSITY=1
 fi
 
+
 INTLTOOL_V_MERGE='$(INTLTOOL__v_MERGE_$(V))'
 INTLTOOL__v_MERGE_='$(INTLTOOL__v_MERGE_$(AM_DEFAULT_VERBOSITY))'
 INTLTOOL__v_MERGE_0='@echo "  ITMRG " $@;'
 
-INTLTOOL_V_MERGE_OPTIONS='$(intltool__v_merge_options_$(V))'
-intltool__v_merge_options_='$(intltool__v_merge_options_$(AM_DEFAULT_VERBOSITY))'
-intltool__v_merge_options_0='-q'
 
-  INTLTOOL_DESKTOP_RULE='%.desktop:   %.desktop.in   $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-INTLTOOL_DIRECTORY_RULE='%.directory: %.directory.in $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-     INTLTOOL_KEYS_RULE='%.keys:      %.keys.in      $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -k -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-     INTLTOOL_PROP_RULE='%.prop:      %.prop.in      $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-      INTLTOOL_OAF_RULE='%.oaf:       %.oaf.in       $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -o -p $(top_srcdir)/po $< $@'
-     INTLTOOL_PONG_RULE='%.pong:      %.pong.in      $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-   INTLTOOL_SERVER_RULE='%.server:    %.server.in    $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -o -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-    INTLTOOL_SHEET_RULE='%.sheet:     %.sheet.in     $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-INTLTOOL_SOUNDLIST_RULE='%.soundlist: %.soundlist.in $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-       INTLTOOL_UI_RULE='%.ui:        %.ui.in        $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-      INTLTOOL_XML_RULE='%.xml:       %.xml.in       $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-if test "$INTLTOOL_APPLIED_VERSION_AS_INT" -ge 5000; then
-      INTLTOOL_XML_NOMERGE_RULE='%.xml:       %.xml.in       $(INTLTOOL_MERGE) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u --no-translations $< $@'
-else
-      INTLTOOL_XML_NOMERGE_RULE='%.xml:       %.xml.in       $(INTLTOOL_MERGE) ; $(INTLTOOL_V_MERGE)_it_tmp_dir=tmp.intltool.$$RANDOM && mkdir $$_it_tmp_dir && LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u $$_it_tmp_dir $< $@ && rmdir $$_it_tmp_dir'
-fi
-      INTLTOOL_XAM_RULE='%.xam:       %.xml.in       $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-      INTLTOOL_KBD_RULE='%.kbd:       %.kbd.in       $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -m -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-    INTLTOOL_CAVES_RULE='%.caves:     %.caves.in     $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-  INTLTOOL_SCHEMAS_RULE='%.schemas:   %.schemas.in   $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -s -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-    INTLTOOL_THEME_RULE='%.theme:     %.theme.in     $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-    INTLTOOL_SERVICE_RULE='%.service: %.service.in   $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
-   INTLTOOL_POLICY_RULE='%.policy:    %.policy.in    $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+
+
+INTLTOOL_V_MERGE_OPTIONS='$(intltool__v_merge_options_$(V))'
+intltool__v_merge_options_='$(intltool__v_merge_options_$(AM_DEFAULT_VERBOSITY))'
+intltool__v_merge_options_0='-q'
+
+
+
+
+  INTLTOOL_DESKTOP_RULE='%.desktop:   %.desktop.in   $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+INTLTOOL_DIRECTORY_RULE='%.directory: %.directory.in $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+     INTLTOOL_KEYS_RULE='%.keys:      %.keys.in      $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -k -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+     INTLTOOL_PROP_RULE='%.prop:      %.prop.in      $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+      INTLTOOL_OAF_RULE='%.oaf:       %.oaf.in       $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -o -p $(top_srcdir)/po $< $@'
+     INTLTOOL_PONG_RULE='%.pong:      %.pong.in      $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+   INTLTOOL_SERVER_RULE='%.server:    %.server.in    $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -o -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+    INTLTOOL_SHEET_RULE='%.sheet:     %.sheet.in     $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+INTLTOOL_SOUNDLIST_RULE='%.soundlist: %.soundlist.in $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+       INTLTOOL_UI_RULE='%.ui:        %.ui.in        $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+      INTLTOOL_XML_RULE='%.xml:       %.xml.in       $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+if test "$INTLTOOL_APPLIED_VERSION_AS_INT" -ge 5000; then
+      INTLTOOL_XML_NOMERGE_RULE='%.xml:       %.xml.in       $(INTLTOOL_MERGE) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u --no-translations $< $@'
+else
+      INTLTOOL_XML_NOMERGE_RULE='%.xml:       %.xml.in       $(INTLTOOL_MERGE) ; $(INTLTOOL_V_MERGE)_it_tmp_dir=tmp.intltool.$$RANDOM && mkdir $$_it_tmp_dir && LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u $$_it_tmp_dir $< $@ && rmdir $$_it_tmp_dir'
+fi
+      INTLTOOL_XAM_RULE='%.xam:       %.xml.in       $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+      INTLTOOL_KBD_RULE='%.kbd:       %.kbd.in       $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -m -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+    INTLTOOL_CAVES_RULE='%.caves:     %.caves.in     $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+  INTLTOOL_SCHEMAS_RULE='%.schemas:   %.schemas.in   $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -s -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+    INTLTOOL_THEME_RULE='%.theme:     %.theme.in     $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+    INTLTOOL_SERVICE_RULE='%.service: %.service.in   $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -d -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+   INTLTOOL_POLICY_RULE='%.policy:    %.policy.in    $(INTLTOOL_MERGE) $(wildcard $(top_srcdir)/po/*.po) ; $(INTLTOOL_V_MERGE)LC_ALL=C $(INTLTOOL_MERGE) $(INTLTOOL_V_MERGE_OPTIONS) -x -u -c $(top_builddir)/po/.intltool-merge-cache $(top_srcdir)/po $< $@'
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
 
 # Check the gettext tools to make sure they are GNU
 # Extract the first word of "xgettext", so it can be a program name with args.
@@ -7244,6 +7655,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
 # Extract the first word of "msgmerge", so it can be a program name with args.
 set dummy msgmerge; ac_word=$2
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for $ac_word" >&5
@@ -7289,6 +7701,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
 # Extract the first word of "msgfmt", so it can be a program name with args.
 set dummy msgfmt; ac_word=$2
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for $ac_word" >&5
@@ -7334,6 +7747,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
 # Extract the first word of "gmsgfmt", so it can be a program name with args.
 set dummy gmsgfmt; ac_word=$2
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for $ac_word" >&5
@@ -7380,6 +7794,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
 if test -z "$XGETTEXT" -o -z "$MSGMERGE" -o -z "$MSGFMT"; then
     as_fn_error $? "GNU gettext tools not found; required for intltool" "$LINENO" 5
 fi
@@ -7392,10 +7807,16 @@ fi
 
 # Substitute ALL_LINGUAS so we can use it in po/Makefile
 
+
+
+
+
 GETTEXT_PACKAGE=xscreensaver
 
 printf "%s\n" "#define GETTEXT_PACKAGE \"$GETTEXT_PACKAGE\"" >>confdefs.h
 
+
+
 ALL_LINGUAS="da de es et fi fr hu it ja ko nb nl pl pt pt_BR ru sk sv vi wa zh_CN zh_TW"
 
   { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for a race-free mkdir -p" >&5
@@ -7499,7 +7920,7 @@ case `"$ac_path_SED" --version 2>&1` in #(
     # 10*(2^10) chars as input seems more than enough
     test $ac_count -gt 10 && break
   done
-  rm -rf conftest.in conftest.tmp conftest.nl conftest.out;;
+  rm -f conftest.in conftest.tmp conftest.nl conftest.out;;
 esac
 
       $ac_path_SED_found && break 3
@@ -7519,10 +7940,15 @@ fi
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $ac_cv_path_SED" >&5
 printf "%s\n" "$ac_cv_path_SED" >&6; }
  SED="$ac_cv_path_SED"
-  rm -rf conftest.sed
+  rm -f conftest.sed
+
+
 
       GETTEXT_MACRO_VERSION=0.20
 
+
+
+
 # Prepare PATH_SEPARATOR.
 # The user is always right.
 if test "${PATH_SEPARATOR+set}" != set; then
@@ -7639,11 +8065,15 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
+
     case `$GMSGFMT --version | sed 1q | sed -e 's,^[^0-9]*,,'` in
     '' | 0.[0-9] | 0.[0-9].* | 0.1[0-4] | 0.1[0-4].*) GMSGFMT_015=: ;;
     *) GMSGFMT_015=$GMSGFMT ;;
   esac
 
+
+
 # Prepare PATH_SEPARATOR.
 # The user is always right.
 if test "${PATH_SEPARATOR+set}" != set; then
@@ -7721,6 +8151,8 @@ fi
     *) XGETTEXT_015=$XGETTEXT ;;
   esac
 
+
+
 # Prepare PATH_SEPARATOR.
 # The user is always right.
 if test "${PATH_SEPARATOR+set}" != set; then
@@ -7790,6 +8222,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
     if LC_ALL=C $MSGMERGE --help | grep ' --for-msgfmt ' >/dev/null; then
     MSGMERGE_FOR_MSGFMT_OPTION='--for-msgfmt'
   else
@@ -7800,10 +8233,14 @@ fi
     fi
   fi
 
+
     test -n "${XGETTEXT_EXTRA_OPTIONS+set}" || XGETTEXT_EXTRA_OPTIONS=
 
+
   ac_config_commands="$ac_config_commands po-directories"
 
+
+
       if test "X$prefix" = "XNONE"; then
     acl_final_prefix="$ac_default_prefix"
   else
@@ -7819,6 +8256,8 @@ fi
   eval acl_final_exec_prefix=\"$acl_final_exec_prefix\"
   prefix="$acl_saved_prefix"
 
+
+
 # Check whether --with-gnu-ld was given.
 if test ${with_gnu_ld+y}
 then :
@@ -8003,6 +8442,11 @@ fi
 printf "%s\n" "$acl_cv_prog_gnu_ld" >&6; }
 with_gnu_ld=$acl_cv_prog_gnu_ld
 
+
+
+
+
+
                                                 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for shared library run path origin" >&5
 printf %s "checking for shared library run path origin... " >&6; }
 if test ${acl_cv_rpath+y}
@@ -8038,6 +8482,9 @@ else case e in #(
 esac
 fi
 
+
+
+
   { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking 32-bit host C ABI" >&5
 printf %s "checking 32-bit host C ABI... " >&6; }
 if test ${gl_cv_host_cpu_c_abi_32bit+y}
@@ -8159,7 +8606,7 @@ case `"$ac_path_EGREP_TRADITIONAL" --version 2>&1` in #(
     # 10*(2^10) chars as input seems more than enough
     test $ac_count -gt 10 && break
   done
-  rm -rf conftest.in conftest.tmp conftest.nl conftest.out;;
+  rm -f conftest.in conftest.tmp conftest.nl conftest.out;;
 esac
 
       $ac_path_EGREP_TRADITIONAL_found && break 3
@@ -8221,7 +8668,7 @@ case `"$ac_path_EGREP_TRADITIONAL" --version 2>&1` in #(
     # 10*(2^10) chars as input seems more than enough
     test $ac_count -gt 10 && break
   done
-  rm -rf conftest.in conftest.tmp conftest.nl conftest.out;;
+  rm -f conftest.in conftest.tmp conftest.nl conftest.out;;
 esac
 
       $ac_path_EGREP_TRADITIONAL_found && break 3
@@ -8244,6 +8691,10 @@ fi
 printf "%s\n" "$ac_cv_path_EGREP_TRADITIONAL" >&6; }
  EGREP_TRADITIONAL=$ac_cv_path_EGREP_TRADITIONAL
 
+
+
+
+
   { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for ELF binary format" >&5
 printf %s "checking for ELF binary format... " >&6; }
 if test ${gl_cv_elf+y}
@@ -8405,6 +8856,16 @@ printf "%s\n" "$acl_cv_libdirstems" >&6; }
   acl_libdirstem2=`echo "$acl_cv_libdirstems" | sed -e 's/^[^,]*,//' -e 's/,.*//'`
   acl_libdirstem3=`echo "$acl_cv_libdirstems" | sed -e 's/^[^,]*,[^,]*,//' -e 's/,.*//'`
 
+
+
+
+
+
+
+
+
+
+
     use_additional=yes
 
   acl_saved_prefix="$prefix"
@@ -8420,6 +8881,7 @@ printf "%s\n" "$acl_cv_libdirstems" >&6; }
   exec_prefix="$acl_saved_exec_prefix"
   prefix="$acl_saved_prefix"
 
+
 # Check whether --with-libiconv-prefix was given.
 if test ${with_libiconv_prefix+y}
 then :
@@ -8885,6 +9347,12 @@ fi
     done
   fi
 
+
+
+
+
+
+
   gl_sed_double_backslashes='s/\\/\\\\/g'
   gl_sed_escape_doublequotes='s/"/\\"/g'
   gl_sed_escape_for_make_1="s,\\([ \"&'();<>\\\\\`|]\\),\\\\\\1,g"
@@ -8894,6 +9362,28 @@ fi
     *)  gl_tr_cr='\r' ;;
   esac
 
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
     { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for CFPreferencesCopyAppValue" >&5
 printf %s "checking for CFPreferencesCopyAppValue... " >&6; }
 if test ${gt_cv_func_CFPreferencesCopyAppValue+y}
@@ -8976,6 +9466,11 @@ printf "%s\n" "#define HAVE_CFLOCALECOPYPREFERREDLANGUAGES 1" >>confdefs.h
                     INTL_MACOSX_LIBS="-Wl,-framework -Wl,CoreFoundation -Wl,-framework -Wl,CoreServices"
   fi
 
+
+
+
+
+
   LIBINTL=
   LTLIBINTL=
   POSUB=
@@ -8991,6 +9486,7 @@ printf "%s\n" "#define HAVE_CFLOCALECOPYPREFERREDLANGUAGES 1" >>confdefs.h
     if test "$USE_NLS" = "yes"; then
     gt_use_preinstalled_gnugettext=no
 
+
         if test $gt_api_version -ge 3; then
           gt_revision_test_code='
 #ifndef __GNU_GETTEXT_SUPPORTED_REVISION
@@ -9054,6 +9550,10 @@ printf "%s\n" "$ac_res" >&6; }
 
         if { eval "gt_val=\$$gt_func_gnugettext_libc"; test "$gt_val" != "yes"; }; then
 
+
+
+
+
           gl_saved_CPPFLAGS="$CPPFLAGS"
 
   for element in $INCICONV; do
@@ -9078,6 +9578,7 @@ printf "%s\n" "$ac_res" >&6; }
     fi
   done
 
+
   { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for iconv" >&5
 printf %s "checking for iconv... " >&6; }
 if test ${am_cv_func_iconv+y}
@@ -9327,6 +9828,15 @@ printf "%s\n" "$LIBICONV" >&6; }
     LTLIBICONV=
   fi
 
+
+
+
+
+
+
+
+
+
     use_additional=yes
 
   acl_saved_prefix="$prefix"
@@ -9342,6 +9852,7 @@ printf "%s\n" "$LIBICONV" >&6; }
   exec_prefix="$acl_saved_exec_prefix"
   prefix="$acl_saved_prefix"
 
+
 # Check whether --with-libintl-prefix was given.
 if test ${with_libintl_prefix+y}
 then :
@@ -9807,6 +10318,10 @@ fi
     done
   fi
 
+
+
+
+
           { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for GNU gettext in libintl" >&5
 printf %s "checking for GNU gettext in libintl... " >&6; }
 if eval test \${$gt_func_gnugettext_libintl+y}
@@ -9921,6 +10436,8 @@ printf "%s\n" "$ac_res" >&6; }
           INCINTL=
         fi
 
+
+
     if test -n "$INTL_MACOSX_LIBS"; then
       if test "$gt_use_preinstalled_gnugettext" = "yes" \
          || test "$nls_cv_use_gnu_gettext" = "yes"; then
@@ -9992,8 +10509,10 @@ printf "%s\n" "$LIBINTL" >&6; }
 
       fi
 
+
 printf "%s\n" "#define HAVE_GETTEXT 1" >>confdefs.h
 
+
 printf "%s\n" "#define HAVE_DCGETTEXT 1" >>confdefs.h
 
     fi
@@ -10001,8 +10520,17 @@ printf "%s\n" "#define HAVE_DCGETTEXT 1" >>confdefs.h
         POSUB=po
   fi
 
+
+
+
         INTLLIBS="$LIBINTL"
 
+
+
+
+
+
+
       gt_save_prefix="${prefix}"
   gt_save_datarootdir="${datarootdir}"
   gt_save_localedir="${localedir}"
@@ -10012,6 +10540,10 @@ printf "%s\n" "#define HAVE_DCGETTEXT 1" >>confdefs.h
   eval datarootdir="$datarootdir"
   eval localedir="$localedir"
 
+
+
+
+
     gl_final_localedir="$localedir"
     case "$build_os" in
     cygwin*)
@@ -10024,11 +10556,13 @@ printf "%s\n" "#define HAVE_DCGETTEXT 1" >>confdefs.h
     localedir_c=`printf '%s\n' "$gl_final_localedir" | sed -e "$gl_sed_double_backslashes" -e "$gl_sed_escape_doublequotes" | tr -d "$gl_tr_cr"`
   localedir_c='"'"$localedir_c"'"'
 
+
     localedir_c_make=`printf '%s\n' "$localedir_c" | sed -e "$gl_sed_escape_for_make_1" -e "$gl_sed_escape_for_make_2" | tr -d "$gl_tr_cr"`
       if test "$localedir_c_make" = '\"'"${gl_final_localedir}"'\"'; then
     localedir_c_make='\"$(localedir)\"'
   fi
 
+
   localedir="${gt_save_localedir}"
   datarootdir="${gt_save_datarootdir}"
   prefix="${gt_save_prefix}"
@@ -10112,6 +10646,10 @@ fi
 fi
 LIBINTL="$INTLLIBS"
 
+
+
+
+
 ###############################################################################
 #
 #       The --with-x-app-defaults option
@@ -10120,6 +10658,7 @@ LIBINTL="$INTLLIBS"
 
 #### H1
 
+
 # Check whether --with-app-defaults was given.
 if test ${with_app_defaults+y}
 then :
@@ -10129,6 +10668,7 @@ else case e in #(
 esac
 fi
 
+
     { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for X app-defaults directory" >&5
 printf %s "checking for X app-defaults directory... " >&6; }
 if test ${ac_cv_x_app_defaults+y}
@@ -10222,6 +10762,8 @@ then :
   enableval=$enable_dependency_tracking;
 fi
 
+
+
 ###############################################################################
 #
 #       Handle the --with-hackdir option
@@ -10240,6 +10782,7 @@ else case e in #(
 esac
 fi
 
+
 if test x"$with_hackdir" = xyes; then
   HACKDIR='${libexecdir}/xscreensaver'
 elif test x"$with_hackdir" = xno; then
@@ -10272,6 +10815,7 @@ else case e in #(
 esac
 fi
 
+
 if test x"$with_configdir" = xyes; then
   HACK_CONF_DIR='${datadir}/xscreensaver/config'
 elif test x"$with_configdir" = xno; then
@@ -10288,6 +10832,8 @@ else
   fi
 fi
 
+
+
 ###############################################################################
 #
 #       Handle the --with-fontdir option
@@ -10306,6 +10852,7 @@ else case e in #(
 esac
 fi
 
+
 if test x"$with_fontdir" = xyes; then
   FONT_DIR='${datarootdir}/fonts/xscreensaver'
   if test "$ac_macosx" = yes; then
@@ -10324,6 +10871,8 @@ else
   fi
 fi
 
+
+
 ###############################################################################
 #
 #       Check for the DPMS server extension.
@@ -10344,6 +10893,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_dpms" in
     yes) ;;
     no)  ;;
@@ -10387,6 +10938,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_dpms" = yes; then
 
   # first check for dpms.h
@@ -10486,6 +11038,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
     # if that failed, look in -lXdpms (this is where it was in XFree86 3.x)
     if test "$have_dpms" = no; then
 
@@ -10565,6 +11118,7 @@ fi
     fi
   fi
 
+
   # if that succeeded, then we've really got it.
   if test "$have_dpms" = yes; then
     printf "%s\n" "#define HAVE_DPMS_EXTENSION 1" >>confdefs.h
@@ -10576,6 +11130,7 @@ elif test "$with_dpms" != no; then
   exit 1
 fi
 
+
 ###############################################################################
 #
 #       Check for the XINERAMA server extension.
@@ -10594,6 +11149,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_xinerama" in
     yes) ;;
     no)  ;;
@@ -10637,6 +11194,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_xinerama" = yes; then
 
   # first check for Xinerama.h
@@ -10735,6 +11293,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
     # if that failed, look in -lXinerama (this is where it is in XFree86 4.1.)
     if test "$have_xinerama" = no; then
 
@@ -10826,6 +11385,7 @@ elif test "$with_xinerama" != no; then
   exit 1
 fi
 
+
 ###############################################################################
 #
 #       Check for the RANDR (Resize and Rotate) server extension.
@@ -10848,6 +11408,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_randr" in
     yes) ;;
     no)  ;;
@@ -10891,6 +11453,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_randr" = yes; then
 
   # first check for Xrandr.h
@@ -10989,6 +11552,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
     # first look for RANDR in -lXext
     have_randr=no
 
@@ -11065,6 +11629,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
     # if that failed, look in -lXrandr
     if test "$have_randr" = no; then
 
@@ -11149,6 +11714,7 @@ fi
   if test "$have_randr" = yes; then
     printf "%s\n" "#define HAVE_RANDR 1" >>confdefs.h
 
+
     # Now check for version 1.2 in the same libs.
     # Try to compile, since on macOS 10.5.7, headers are older than libs!
     { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for XRRGetScreenResources" >&5
@@ -11200,11 +11766,13 @@ printf "%s\n" "$ac_cv_randr_12" >&6; }
 #                  [true], $SAVER_LIBS)
   fi
 
+
 elif test "$with_randr" != no; then
   echo "error: must be yes or no: --with-randr-ext=$with_randr"
   exit 1
 fi
 
+
 ###############################################################################
 #
 #       Check for the XINPUT server extension.
@@ -11224,6 +11792,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_xinput" in
     yes) ;;
     no)  ;;
@@ -11267,6 +11837,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_xinput" = yes; then
 
   # first check for Xinput2.h
@@ -11382,6 +11953,7 @@ elif test "$with_xinput" != no; then
   exit 1
 fi
 
+
 ###############################################################################
 #
 #       Check for the XF86VMODE server extension (for gamma fading.)
@@ -11443,6 +12015,7 @@ fi
 #  exit 1
 #fi
 
+
 ###############################################################################
 #
 #       Check for the XSHM server extension.
@@ -11461,6 +12034,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_xshm" in
     yes) ;;
     no)  ;;
@@ -11504,6 +12079,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_xshm" = yes; then
 
   # first check for Xshm.h.
@@ -11660,6 +12236,7 @@ elif test "$with_xshm" != no; then
   exit 1
 fi
 
+
 ###############################################################################
 #
 #       Check for the DOUBLE-BUFFER server extension.
@@ -11678,6 +12255,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_xdbe" in
     yes) ;;
     no)  ;;
@@ -11721,8 +12300,10 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_xdbe" = yes; then
 
+
   ac_save_CPPFLAGS="$CPPFLAGS"
   if test \! -z "$includedir" ; then
     CPPFLAGS="$CPPFLAGS -I$includedir"
@@ -11747,6 +12328,7 @@ elif test "$with_xdbe" != no; then
   exit 1
 fi
 
+
 ###############################################################################
 #
 #       Check for the COMPOSITE server extension.
@@ -11776,6 +12358,7 @@ fi
 #  exit 1
 #fi
 
+
 ###############################################################################
 #
 #       Check for the XKB server extension.
@@ -11794,6 +12377,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_xkb" in
     yes) ;;
     no)  ;;
@@ -11837,8 +12422,10 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_xkb" = yes; then
 
+
   ac_save_CPPFLAGS="$CPPFLAGS"
   if test \! -z "$includedir" ; then
     CPPFLAGS="$CPPFLAGS -I$includedir"
@@ -11863,6 +12450,7 @@ elif test "$with_xkb" != no; then
   exit 1
 fi
 
+
 ###############################################################################
 #
 #       Check for HP XHPDisableReset and XHPEnableReset.
@@ -11900,6 +12488,7 @@ rm -rf conftest*
 
   CPPFLAGS="$ac_save_CPPFLAGS"
 
+
 ###############################################################################
 #
 #       Check for /proc/*/oom_score_adj.
@@ -11919,6 +12508,7 @@ else case e in #(
 esac
 fi
 
+
 if test "$with_proc_oom_req" = yes; then
 
   # Note that we may be building in an environment (e.g. Debian buildd chroot)
@@ -11927,6 +12517,7 @@ if test "$with_proc_oom_req" = yes; then
   have_proc_oom=yes
   printf "%s\n" "#define HAVE_PROC_OOM 1" >>confdefs.h
 
+
 elif test "$with_proc_oom_req" = unspecified; then
 
   have_proc_oom=no
@@ -11961,6 +12552,7 @@ elif test "$with_proc_oom" != no; then
   exit 1
 fi
 
+
 ###############################################################################
 #
 #       Check for -lsystemd
@@ -11981,6 +12573,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_systemd" in
     yes) ;;
     no)  ;;
@@ -12024,6 +12618,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_systemd" != yes -a "$with_systemd" != no ; then
   echo "error: must be yes or no: --with-systemd=$with_systemd"
   exit 1
@@ -12193,6 +12788,7 @@ else
   SYSTEMD_LIBS=''
 fi
 
+
 ###############################################################################
 #
 #       Check for -lelogind, a stripped down subset of systemd.
@@ -12213,6 +12809,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_elogind" in
     yes) ;;
     no)  ;;
@@ -12256,6 +12854,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_elogind" != yes -a "$with_elogind" != no ; then
   echo "error: must be yes or no: --with-elogind=$with_elogind"
   exit 1
@@ -12421,6 +13020,7 @@ if test "$have_elogind" = yes; then
 
 fi
 
+
 ###############################################################################
 #
 #       Check for Wayland
@@ -12439,6 +13039,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_wayland" in
     yes) ;;
     no)  ;;
@@ -12482,11 +13084,13 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_wayland" != yes -a "$with_wayland" != no ; then
   echo "error: must be yes or no: --with-wayland=$with_wayland"
   exit 1
 fi
 
+
 if test "$with_wayland" = yes; then
   have_wayland=no
 
@@ -12553,6 +13157,7 @@ printf "%s\n" "$ac_cv_wayland_config_libs" >&6; }
   fi
 fi
 
+
 # Check for the various Gnome help and URL loading programs.
 #
 WITH_BROWSER=gnome-open
@@ -12601,6 +13206,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
   test -n "$gnome_open_program" && break
 done
 
@@ -12648,11 +13254,13 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
   test -n "$gnome_url_show_program" && break
 done
 
 fi
 
+
 ###############################################################################
 #
 #       The --enable-locking option
@@ -12691,6 +13299,7 @@ else
   exit 1
 fi
 
+
 ##############################################################################
 #
 #       Whether to allow root password to unblank.
@@ -12709,6 +13318,7 @@ fi
 #  exit 1
 #fi
 
+
 ###############################################################################
 #
 #       Check for PAM.
@@ -12736,6 +13346,7 @@ esac
 have_pam=no
 with_pam_req=unspecified
 
+
 # Check whether --with-pam was given.
 if test ${with_pam+y}
 then :
@@ -12745,6 +13356,7 @@ else case e in #(
 esac
 fi
 
+
 # There's no good argument for ever changing this.
 pam_service_name="xscreensaver"
 #AC_ARG_WITH([pam_service_name],
@@ -12752,6 +13364,7 @@ pam_service_name="xscreensaver"
 #  --enable-root-passwd    Allow the root password to unlock, if not using PAM.],
 #  [pam_service_name="$withval"],[pam_service_name="xscreensaver"])
 
+
    case "$with_pam" in
     yes) ;;
     no)  ;;
@@ -12795,6 +13408,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$enable_locking" = yes -a "$with_pam" = yes; then
   { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for PAM" >&5
 printf %s "checking for PAM... " >&6; }
@@ -12839,6 +13453,7 @@ printf "%s\n" "$ac_cv_pam" >&6; }
 
     printf "%s\n" "#define PAM_SERVICE_NAME \"$pam_service_name\"" >>confdefs.h
 
+
     PASSWD_LIBS="${PASSWD_LIBS} -lpam"
 
     # libpam typically requires dlopen and dlsym.  On FreeBSD,
@@ -12891,6 +13506,7 @@ then :
   PASSWD_LIBS="${PASSWD_LIBS} -ldl"
 fi
 
+
     # On Linux, sigtimedwait() is in libc; on Solaris, it's in librt.
     have_timedwait=no
     { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for sigtimedwait in -lc" >&5
@@ -13116,6 +13732,7 @@ rm -f core conftest.err conftest.$ac_objext conftest.beam \
 esac
 fi
 
+
       if test "$ac_pam_fail_delay" = yes ; then
         { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: yes" >&5
 printf "%s\n" "yes" >&6; }
@@ -13129,6 +13746,7 @@ printf "%s\n" "no" >&6; }
   fi
 fi
 
+
 ###############################################################################
 #
 #       Check for Kerberos.
@@ -13139,6 +13757,7 @@ have_kerberos=no
 have_kerberos5=no
 with_kerberos_req=unspecified
 
+
 # Check whether --with-kerberos was given.
 if test ${with_kerberos+y}
 then :
@@ -13148,6 +13767,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_kerberos" in
     yes) ;;
     no)  ;;
@@ -13191,6 +13812,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$enable_locking" = yes -a "$with_kerberos" = yes; then
   { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for Kerberos 4" >&5
 printf %s "checking for Kerberos 4... " >&6; }
@@ -13451,6 +14073,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
   elif test "$have_kerberos" = yes ; then
     # from Tim Showalter <tjs@psaux.com> for FreeBSD 4.2
     PASSWD_LIBS="$PASSWD_LIBS -lkrb -ldes -lcom_err"
@@ -13521,6 +14144,7 @@ fi
   fi
 fi
 
+
 ###############################################################################
 #
 #       Check for the nine billion variants of shadow passwords...
@@ -13530,6 +14154,7 @@ fi
 have_shadow=no
 with_shadow_req=unspecified
 
+
 # Check whether --with-shadow was given.
 if test ${with_shadow+y}
 then :
@@ -13539,6 +14164,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_shadow" in
     yes) ;;
     no)  ;;
@@ -13582,11 +14209,13 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$enable_locking" = no ; then
   with_shadow_req=no
   with_shadow=no
 fi
 
+
 ###############################################################################
 #
 #       Check for Sun "adjunct" passwords.
@@ -13644,6 +14273,7 @@ printf "%s\n" "$ac_cv_sun_adjunct" >&6; }
   fi
 fi
 
+
 ###############################################################################
 #
 #       Check for DEC and SCO so-called "enhanced" security.
@@ -13915,6 +14545,7 @@ fi
   fi
 fi
 
+
 ###############################################################################
 #
 #       Check for FreeBSD-style shadow passwords.
@@ -13948,6 +14579,7 @@ printf "%s\n" "$ac_cv_master_passwd" >&6; }
   fi
 fi
 
+
 ###############################################################################
 #
 #       Check for OpenBSD-style shadow passwords.
@@ -14007,6 +14639,7 @@ printf "%s\n" "$ac_cv_pwnam_shadow" >&6; }
   fi
 fi
 
+
 ###############################################################################
 #
 #       Check for traditional (ha!) shadow passwords.
@@ -14167,6 +14800,7 @@ fi
   fi
 fi
 
+
 ###############################################################################
 #
 #       Check for other libraries needed for non-shadow passwords.
@@ -14277,6 +14911,7 @@ fi
   fi
 fi
 
+
 # Most of the above shadow mechanisms will have set setuid_auth to yes,
 # if they were found.  But, on some systems, we need setuid even when
 # using plain old vanilla passwords.
@@ -14289,6 +14924,7 @@ if test "$enable_locking" = yes -a "$have_pam" = no ; then
   esac
 fi
 
+
 if test "$have_shadow_adjunct" = yes ; then
   printf "%s\n" "#define HAVE_ADJUNCT_PASSWD 1" >>confdefs.h
 
@@ -14306,6 +14942,7 @@ elif test "$have_pwnam_shadow" = yes ; then
 
 fi
 
+
 ###############################################################################
 #
 #       Check for -lgtk (and Gnome stuff)
@@ -14326,6 +14963,7 @@ else case e in #(
 esac
 fi
 
+
 # if --with-gtk=/directory/ was specified, remember that directory so that
 # we can also look for the `gtk-config' program in that directory.
 case "$with_gtk" in
@@ -14337,6 +14975,7 @@ case "$with_gtk" in
     ;;
 esac
 
+
    case "$with_gtk" in
     yes) ;;
     no)  ;;
@@ -14380,11 +15019,13 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_gtk" != yes -a "$with_gtk" != no ; then
   echo "error: must be yes or no: --with-gtk=$with_gtk"
   exit 1
 fi
 
+
 gtk_halfassed=no
 
 if test "$with_gtk" = yes; then
@@ -14466,6 +15107,7 @@ printf "%s\n" "$ac_cv_glib_res" >&6; }
 
 fi
 
+
 # Check for the various Gnome help and URL loading programs.
 #
 WITH_BROWSER=gnome-open
@@ -14514,6 +15156,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
   test -n "$gnome_open_program" && break
 done
 
@@ -14561,11 +15204,13 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
   test -n "$gnome_url_show_program" && break
 done
 
 fi
 
+
 ###############################################################################
 #
 #       Check for -lXm.
@@ -14584,6 +15229,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_motif" in
     yes) ;;
     no)  ;;
@@ -14627,6 +15274,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_motif" != yes -a "$with_motif" != no ; then
   echo "error: must be yes or no: --with-motif=$with_motif"
   exit 1
@@ -14656,6 +15304,7 @@ fi
   CPPFLAGS="$ac_save_CPPFLAGS"
 fi
 
+
 if test "$have_motif" = yes; then
 
   ac_save_CPPFLAGS="$CPPFLAGS"
@@ -14677,6 +15326,7 @@ fi
   CPPFLAGS="$ac_save_CPPFLAGS"
 fi
 
+
 ###############################################################################
 #
 #       Checking whether Motif is really Lesstif.
@@ -14725,6 +15375,7 @@ printf "%s\n" "$ac_cv_have_lesstif" >&6; }
   have_lesstif=$ac_cv_have_lesstif
 fi
 
+
 lesstif_version=unknown
 lesstif_version_string=unknown
 
@@ -14782,12 +15433,13 @@ esac
 fi
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $ac_cv_lesstif_version_string" >&5
 printf "%s\n" "$ac_cv_lesstif_version_string" >&6; }
-  rm -rf conftest-lt
+  rm -f conftest-lt
   lesstif_version=$ac_cv_lesstif_version
   lesstif_version_string=$ac_cv_lesstif_version_string
 
 fi
 
+
 if test "$have_motif" = yes ; then
   mtv=unknown
   echo unknown > conftest-mt
@@ -14842,12 +15494,13 @@ esac
 fi
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $ac_cv_motif_version_string" >&5
 printf "%s\n" "$ac_cv_motif_version_string" >&6; }
-  rm -rf conftest-mt
+  rm -f conftest-mt
   motif_version=$ac_cv_motif_version
   motif_version_string=$ac_cv_motif_version_string
 
 fi
 
+
 ###############################################################################
 #
 #       Checking whether Motif requires -lXp.
@@ -14937,6 +15590,7 @@ fi
 
 fi
 
+
 ###############################################################################
 #
 #       Checking whether Motif requires -lXintl (for _Xsetlocale.)
@@ -15024,6 +15678,7 @@ fi
   fi
 fi
 
+
 ###############################################################################
 #
 #       Check for a login manager for a "New Login" button on the lock dialog.
@@ -15037,6 +15692,7 @@ default_login_manager_2='kdmctl reserve'
 default_login_manager_3='lxdm -c USER_SWITCH'
 default_login_manager_4='dm-tool switch-to-greeter'
 
+
 # Check whether --with-login-manager was given.
 if test ${with_login_manager+y}
 then :
@@ -15111,6 +15767,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
       if test ! -z "$login_manager_tmp" ; then
         with_login_manager="$default_login_manager_1"
       fi
@@ -15164,6 +15821,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
       if test ! -z "$login_manager_tmp" ; then
         with_login_manager="$default_login_manager_2"
       fi
@@ -15217,6 +15875,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
       if test ! -z "$login_manager_tmp" ; then
         with_login_manager="$default_login_manager_3"
       fi
@@ -15270,6 +15929,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
       if test ! -z "$login_manager_tmp" ; then
         with_login_manager="$default_login_manager_4"
       fi
@@ -15299,6 +15959,8 @@ else
 printf "%s\n" "$NEW_LOGIN_COMMAND" >&6; }
 fi
 
+
+
 ###############################################################################
 #
 #       Check for a directory full of images to use as the default value
@@ -15309,6 +15971,7 @@ fi
 have_imagedir=no
 with_imagedir_req=unspecified
 
+
 # Check whether --with-image-directory was given.
 if test ${with_image_directory+y}
 then :
@@ -15390,6 +16053,7 @@ if test -z "$DEFAULT_IMAGE_DIRECTORY" ; then
   DEFAULT_IMAGES_P='False'
 fi
 
+
 ###############################################################################
 #
 #       Pick a text file to use as the default of the "textFile" preference.
@@ -15401,6 +16065,7 @@ fi
 have_textfile=no
 with_textfile_req=unspecified
 
+
 # Check whether --with-text-file was given.
 if test ${with_text_file+y}
 then :
@@ -15468,6 +16133,7 @@ ac_cv_textfile="$with_textfile"
 
 DEFAULT_TEXT_FILE="$ac_cv_textfile"
 
+
 ###############################################################################
 #
 #       Check the browser to see help URL
@@ -15477,6 +16143,7 @@ DEFAULT_TEXT_FILE="$ac_cv_textfile"
 have_browser=no
 with_browser_req=unspecified
 
+
 # Check whether --with-browser was given.
 if test ${with_browser+y}
 then :
@@ -15515,6 +16182,7 @@ printf "%s\n" "no" >&6; }
 esac
 ac_cv_browser="$with_browser"
 
+
 ###############################################################################
 #
 #       Check for pthreads. Must come before checking for OpenGL,
@@ -15624,12 +16292,14 @@ with_pthread_req=unspecified
 # quotation around the m4_warn and dnl so they will be written
 # unexpanded into the updated configure.ac.
 
+
 # This is an auxiliary macro that is also run when
 # autoupdate runs m4.  It simply calls m4_warning, but
 # we need a wrapper so that each warning is emitted only
 # once.  We break the quoting in m4_warning's argument in
 # order to expand this macro's arguments, not AU_DEFUN's.
 
+
 # Finally, this is the expansion that is picked up by
 # autoconf, causing NAME to expand to NEW-CODE, plus
 # (if SILENT is not "silent") a m4_warning telling the
@@ -15637,6 +16307,9 @@ with_pthread_req=unspecified
 # from autoconf, because that's instructions for what
 # to do *after* running autoupdate.
 
+
+
+
 # Check whether --with-pthread was given.
 if test ${with_pthread+y}
 then :
@@ -15647,6 +16320,7 @@ else case e in #(
 esac
 fi
 
+
 if test "$with_pthread" = yes; then
   # AX_PTHREAD might want a different compiler.
   { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking target system type" >&5
@@ -15683,6 +16357,7 @@ target_os=$*
 IFS=$ac_save_IFS
 case $target_os in *\ *) target_os=`echo "$target_os" | sed 's/ /-/g'`;; esac
 
+
 # The aliases save the names the user supplied, while $host etc.
 # will get canonicalized.
 test -n "$target_alias" &&
@@ -15690,6 +16365,9 @@ test -n "$target_alias" &&
     NONENONEs,x,x, &&
   program_prefix=${target_alias}-
 
+
+
+
 ac_ext=c
 ac_cpp='$CPP $CPPFLAGS'
 ac_compile='$CC -c $CFLAGS $CPPFLAGS conftest.$ac_ext >&5'
@@ -15884,6 +16562,7 @@ fi
 printf "%s\n" "$ax_cv_PTHREAD_CLANG" >&6; }
 ax_pthread_clang="$ax_cv_PTHREAD_CLANG"
 
+
 # GCC generally uses -pthread, or -pthreads on some platforms (e.g. SPARC)
 
 # Note that for GCC and Clang -pthread generally implies -lpthread,
@@ -15906,6 +16585,7 @@ then :
   ax_pthread_flags="-pthread,-lpthread -pthread"
 fi
 
+
 # The presence of a feature test macro requesting re-entrant function
 # definitions is, on some systems, a strong hint that pthreads support is
 # correctly enabled
@@ -15931,6 +16611,7 @@ else case e in #(
 esac
 fi
 
+
 if test "x$ax_pthread_ok" = "xno"; then
 for ax_pthread_try_flag in $ax_pthread_flags; do
 
@@ -15997,6 +16678,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
                 if test "x$ax_pthread_config" = "xno"
 then :
   continue
@@ -16076,6 +16758,7 @@ fi
 done
 fi
 
+
 # Clang needs special handling, because older versions handle the -pthread
 # option in a rather... idiosyncratic way
 
@@ -16177,6 +16860,8 @@ printf "%s\n" "$ax_cv_PTHREAD_CLANG_NO_WARN_FLAG" >&6; }
 
 fi # $ax_pthread_clang = yes
 
+
+
 # Various other checks:
 if test "x$ax_pthread_ok" = "xyes"; then
         ax_pthread_save_CFLAGS="$CFLAGS"
@@ -16364,6 +17049,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
   test -n "$PTHREAD_CC" && break
 done
 test -n "$PTHREAD_CC" || PTHREAD_CC="$CC"
@@ -16414,6 +17100,7 @@ else
 printf "%s\n" "no" >&6; }
 fi
 
+
   test -n "$PTHREAD_CXX" && break
 done
 test -n "$PTHREAD_CXX" || PTHREAD_CXX="$CXX"
@@ -16434,6 +17121,11 @@ fi
 test -n "$PTHREAD_CC" || PTHREAD_CC="$CC"
 test -n "$PTHREAD_CXX" || PTHREAD_CXX="$CXX"
 
+
+
+
+
+
 # Finally, execute ACTION-IF-FOUND/ACTION-IF-NOT-FOUND:
 if test "x$ax_pthread_ok" = "xyes"; then
         if test "$CC" = "$PTHREAD_CC" -o -z "$ac_original_cc"; then
@@ -16453,6 +17145,8 @@ ac_compile='$CC -c $CFLAGS $CPPFLAGS conftest.$ac_ext >&5'
 ac_link='$CC -o conftest$ac_exeext $CFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
 ac_compiler_gnu=$ac_cv_c_compiler_gnu
 
+
+
   if test "$have_pthread" = yes; then
     printf "%s\n" "#define HAVE_PTHREAD 1" >>confdefs.h
 
@@ -16460,6 +17154,7 @@ ac_compiler_gnu=$ac_cv_c_compiler_gnu
   fi
 fi
 
+
 ###############################################################################
 #
 #       Handle --with-gles
@@ -16480,6 +17175,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_jwzgles" in
     yes) ;;
     no)  ;;
@@ -16523,6 +17220,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_jwzgles" = yes; then
   have_jwzgles=yes
   JWZGLES_OBJS='$(JWXYZ_BIN)/jwzgles.o'
@@ -16533,6 +17231,7 @@ elif test "$with_jwzgles" != no; then
   exit 1
 fi
 
+
 ###############################################################################
 #
 #       Check for -lGL or -lMesaGL.
@@ -16556,6 +17255,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_gl" in
     yes) ;;
     no)  ;;
@@ -16599,6 +17300,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 # Some GL libraries require pthreads, so use the pthreads flags when
 # testing them, then set them back after.
 #
@@ -16759,6 +17461,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
       # Check for GLES 2.x
 
   ac_save_CPPFLAGS="$CPPFLAGS"
@@ -16833,6 +17536,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
       # Check for GLES 1.x
 
   ac_save_CPPFLAGS="$CPPFLAGS"
@@ -16907,6 +17611,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
     else   # $have_jwzgles = no
 
       # Check for OpenGL 1.x and/or OpenGL 4.x
@@ -16983,6 +17688,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
       # Check for MesaGL 1.x, but only if libGL doesn't exist.
       if test "$have_gl" = no; then
 
@@ -17313,6 +18019,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
     # Check for native GLES1
 
   ac_save_CPPFLAGS="$CPPFLAGS"
@@ -17391,6 +18098,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
     # Check for GLSL and GLES2
 
   ac_save_CPPFLAGS="$CPPFLAGS"
@@ -17564,6 +18272,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
     if test "$have_jwzgles" = yes; then
       printf "%s\n" "#define HAVE_JWZGLES 1" >>confdefs.h
 
@@ -17586,6 +18295,7 @@ elif test "$with_gl" != no; then
   exit 1
 fi
 
+
 ###############################################################################
 #
 #       Handle --with-glx
@@ -17605,6 +18315,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_glx" in
     yes) ;;
     no)  ;;
@@ -17648,6 +18360,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_glx" = yes; then
 
   ac_save_CPPFLAGS="$CPPFLAGS"
@@ -18009,6 +18722,7 @@ fi
 CFLAGS="$ac_save_pthreads_CFLAGS"
 LIBS="$ac_save_pthreads_LIBS"
 
+
 ###############################################################################
 #
 #       Check for -lgle.
@@ -18028,6 +18742,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_gle" in
     yes) ;;
     no)  ;;
@@ -18071,12 +18787,14 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 GLE_LIBS=""
 
 if test "$have_gl" = no ; then
  true
 elif test "$with_gle" = yes; then
 
+
   ac_save_CPPFLAGS="$CPPFLAGS"
   if test \! -z "$includedir" ; then
     CPPFLAGS="$CPPFLAGS -I$includedir"
@@ -18393,6 +19111,7 @@ elif test "$with_gle" != no; then
 
 fi
 
+
 ###############################################################################
 #
 #       Check for -ljpeg
@@ -18412,6 +19131,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_jpeg" in
     yes) ;;
     no)  ;;
@@ -18455,6 +19176,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_jpeg" != yes -a "$with_jpeg" != no ; then
   echo "error: must be yes or no: --with-jpeg=$with_jpeg"
   exit 1
@@ -18560,6 +19282,7 @@ fi
   fi
 fi
 
+
 ###############################################################################
 #
 #       Check for -lpng
@@ -18579,6 +19302,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_png" in
     yes) ;;
     no)  ;;
@@ -18622,6 +19347,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_png" != yes -a "$with_png" != no ; then
   echo "error: must be yes or no: --with-png=$with_png"
   exit 1
@@ -18727,6 +19453,7 @@ fi
   fi
 fi
 
+
 ###############################################################################
 #
 #       Check for -lgdk_pixbuf.
@@ -18750,6 +19477,7 @@ else case e in #(
 esac
 fi
 
+
 # if --with-pixbuf=/directory/ was specified, remember that directory so that
 # we can also look for the `gdk-pixbuf-config' program in that directory.
 case "$with_gdk_pixbuf" in
@@ -18761,6 +19489,7 @@ case "$with_gdk_pixbuf" in
     ;;
 esac
 
+
    case "$with_gdk_pixbuf" in
     yes) ;;
     no)  ;;
@@ -18804,6 +19533,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_gdk_pixbuf" != yes -a "$with_gdk_pixbuf" != no ; then
   echo "error: must be yes or no: --with-pixbuf=$with_gdk_pixbuf"
   exit 1
@@ -18847,6 +19577,7 @@ printf "%s\n" "$ac_cv_gdk_pixbuf_config_libs" >&6; }
   ac_gdk_pixbuf_config_cflags=$ac_cv_gdk_pixbuf_config_cflags
   ac_gdk_pixbuf_config_libs=$ac_cv_gdk_pixbuf_config_libs
 
+
   if test "$have_gdk_pixbuf" = yes; then
     #
     # we appear to have pixbuf; check for headers/libs to be sure.
@@ -19035,6 +19766,7 @@ fi
   fi
 fi
 
+
 # Now that we have checked for gdk_pixbuf, check for gdk_pixbuf_xlib
 # separately, since it has fallen out of fashion in recent years.
 #
@@ -19097,6 +19829,7 @@ fi
 #  fi
 #fi
 
+
 ###############################################################################
 #
 #       Check for -lXft
@@ -19183,6 +19916,7 @@ if test "$have_xutf8drawstring" = yes ; then
 
 fi
 
+
 have_xft=no
 with_xft_req=unspecified
 xft_halfassed=no
@@ -19196,6 +19930,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$with_xft" in
     yes) ;;
     no)  ;;
@@ -19239,6 +19975,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$with_xft" != yes -a "$with_xft" != no ; then
   echo "error: must be yes or no: --with-xft=$with_xft"
   exit 1
@@ -19406,6 +20143,7 @@ else
   XFT_OBJS='$(UTILS_BIN)/xft.o'
 fi
 
+
 ###############################################################################
 #
 #       Check for various ffmpeg libraries
@@ -19989,6 +20727,7 @@ fi
 
 fi
 
+
 have_ffmpeg=no
 if test "$have_avformat" = yes -a \
         "$have_avcodec" = yes -a \
@@ -20026,6 +20765,7 @@ if test "$have_ffmpeg" = yes ; then
   FFMPEG_LIBS="$FFMPEG_LIBS $extra_ffmpeg_bs"
 fi
 
+
 ###############################################################################
 #
 #       Check for pty support: this allows 'phosphor' and 'apple2'
@@ -20053,6 +20793,7 @@ then :
 
 fi
 
+
   ac_save_CPPFLAGS="$CPPFLAGS"
   ac_save_LDFLAGS="$LDFLAGS"
 #  ac_save_LIBS="$LIBS"
@@ -20126,6 +20867,7 @@ fi
   LDFLAGS="$ac_save_LDFLAGS"
 #  LIBS="$ac_save_LIBS"
 
+
 if test "$ac_have_forkpty" != yes ; then
   # we don't need (or have) -lutil on macOS 10.4.2...
 
@@ -20203,6 +20945,7 @@ fi
 
 fi
 
+
 ###############################################################################
 #
 #       Check for libcap, which allows "sonar" to work without setuid.
@@ -20243,6 +20986,7 @@ setcap_hacks_req=unspecified
 #  fi
 #fi
 
+
 ###############################################################################
 #
 #       Sonar must be setuid to be able to ping other hosts.
@@ -20266,6 +21010,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$setuid_hacks" in
     yes) ;;
     no)  ;;
@@ -20309,6 +21055,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$setuid_hacks" = yes; then
   true
 elif test "$setuid_hacks" != no; then
@@ -20316,6 +21063,7 @@ elif test "$setuid_hacks" != no; then
   exit 1
 fi
 
+
 ###############################################################################
 #
 #       Check for --with-record-animation
@@ -20334,6 +21082,8 @@ else case e in #(
 esac
 fi
 
+
+
    case "$record_anim" in
     yes) ;;
     no)  ;;
@@ -20377,6 +21127,7 @@ printf "%s\n" "not found ($d: no such directory)" >&6; }
      ;;
    esac
 
+
 if test "$record_anim" = yes; then
   true
 elif test "$record_anim" != no; then
@@ -20398,6 +21149,7 @@ printf "%s\n" "enabling --record-animation" >&6; }
   fi
 fi
 
+
 ###############################################################################
 #
 #       Done testing.  Now, set up the various -I and -L variables,
@@ -20409,6 +21161,7 @@ DEPEND=makedepend
 DEPEND_FLAGS=
 DEPEND_DEFINES=
 
+
 if test \! -z "$includedir" ; then
   INCLUDES="$INCLUDES -I$includedir"
 fi
@@ -20417,6 +21170,7 @@ if test \! -z "$libdir" ; then
   LDFLAGS="$LDFLAGS -L$libdir"
 fi
 
+
 PREFERRED_DEMO_PROGRAM=''
 ALL_DEMO_PROGRAMS=
 if test "$have_motif" = yes; then
@@ -20428,6 +21182,7 @@ if test "$have_gtk" = yes; then
   ALL_DEMO_PROGRAMS="$PREFERRED_DEMO_PROGRAM $ALL_DEMO_PROGRAMS"
 fi
 
+
 if test "$have_kerberos" = yes; then
   PASSWD_SRCS="$PASSWD_SRCS \$(KERBEROS_SRCS)"
   PASSWD_OBJS="$PASSWD_OBJS \$(KERBEROS_OBJS)"
@@ -20440,6 +21195,7 @@ fi
 PASSWD_SRCS="$PASSWD_SRCS \$(PWENT_SRCS)"
 PASSWD_OBJS="$PASSWD_OBJS \$(PWENT_OBJS)"
 
+
 if test "$ac_macosx" = yes; then
   EXES_OSX='$(EXES_OSX)'
   SCRIPTS_OSX='$(SCRIPTS_OSX)'
@@ -20450,6 +21206,7 @@ else
   MEN_OSX=
 fi
 
+
 if test "$setuid_auth" = yes; then
   SETUID_AUTH=yes
 else
@@ -20491,6 +21248,7 @@ if test "$have_jpeg" = yes -a "$have_gdk_pixbuf" = yes; then
  JPEG_EXES='$(JPEG_EXES)'
 fi
 
+
 # Another substitution in the XScreenSaver.ad.in file:
 #
 if test "$gnome_open_program" != ''; then
@@ -20507,6 +21265,7 @@ else
   NOGNOME=''
 fi
 
+
 # Set PO_DATADIR to something sensible.
 #
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for locale directory" >&5
@@ -20531,6 +21290,7 @@ fi
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $PO_DATADIR/locale" >&5
 printf "%s\n" "$PO_DATADIR/locale" >&6; }
 
+
 # canonicalize slashes.
 HACK_CONF_DIR=`echo "${HACK_CONF_DIR}" | sed 's@/$@@;s@//*@/@g'`
 FONT_DIR=`echo "${FONT_DIR}" | sed 's@/$@@;s@//*@/@g'`
@@ -20545,14 +21305,106 @@ FONT_DIR=`echo "${FONT_DIR}" | sed 's@/$@@;s@//*@/@g'`
 #
 INCLUDES=`echo "$INCLUDES" | sed 's@ -I${prefix}/include@@g;'`
 
+
 ###############################################################################
 #
 #       Perform substitutions and write Makefiles.
 #
 ###############################################################################
 
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
 APPDEFAULTS=$ac_x_app_defaults
 
+
+
+
+
+
+
+
 ac_config_files="$ac_config_files Makefile utils/Makefile jwxyz/Makefile hacks/Makefile hacks/fonts/Makefile hacks/images/Makefile hacks/glx/Makefile po/Makefile.in driver/Makefile driver/XScreenSaver.ad"
 
 cat >confcache <<\_ACEOF
@@ -20663,14 +21515,18 @@ LIBOBJS=$ac_libobjs
 
 LTLIBOBJS=$ac_ltlibobjs
 
+
 # Check whether --enable-year2038 was given.
 if test ${enable_year2038+y}
 then :
   enableval=$enable_year2038;
 fi
 
+
   ac_config_commands="$ac_config_commands po/stamp-it"
 
+
+
 : "${CONFIG_STATUS=./config.status}"
 ac_write_fail=0
 ac_clean_files_save=$ac_clean_files
@@ -20717,6 +21573,8 @@ esac ;;
 esac
 fi
 
+
+
 # Reset variables that may have inherited troublesome values from
 # the environment.
 
@@ -20763,6 +21621,7 @@ if ${PATH_SEPARATOR+false} :; then
   }
 fi
 
+
 # Find who we are.  Look in the path if we contain no directory separator.
 as_myself=
 case $0 in #((
@@ -20792,6 +21651,8 @@ if test ! -f "$as_myself"; then
   exit 1
 fi
 
+
+
 # as_fn_error STATUS ERROR [LINENO LOG_FD]
 # ----------------------------------------
 # Output "`basename $0`: error: ERROR" to stderr. If LINENO and LOG_FD are
@@ -20808,6 +21669,7 @@ as_fn_error ()
   as_fn_exit $as_status
 } # as_fn_error
 
+
 # as_fn_set_status STATUS
 # -----------------------
 # Set $? to STATUS, without forking.
@@ -20874,6 +21736,7 @@ else case e in #(
 esac
 fi # as_fn_arith
 
+
 if expr a : '\(a\)' >/dev/null 2>&1 &&
    test "X`expr 00001 : '.*\(...\)'`" = X001; then
   as_expr=expr
@@ -20919,6 +21782,7 @@ as_cr_Letters=$as_cr_letters$as_cr_LETTERS
 as_cr_digits='0123456789'
 as_cr_alnum=$as_cr_Letters$as_cr_digits
 
+
 # Determine whether it's possible to make 'echo' print without a newline.
 # These variables are no longer used directly by Autoconf, but are AC_SUBSTed
 # for compatibility with existing Makefiles.
@@ -20968,6 +21832,7 @@ fi
 rm -f conf$$ conf$$.exe conf$$.dir/conf$$.file conf$$.file
 rmdir conf$$.dir 2>/dev/null
 
+
 # as_fn_mkdir_p
 # -------------
 # Create "$as_dir" as a directory, including parents if necessary.
@@ -21013,6 +21878,7 @@ printf "%s\n" X"$as_dir" |
     test -z "$as_dirs" || eval "mkdir $as_dirs"
   } || test -d "$as_dir" || as_fn_error $? "cannot create directory $as_dir"
 
+
 } # as_fn_mkdir_p
 if mkdir -p . 2>/dev/null; then
   as_mkdir_p='mkdir -p "$as_dir"'
@@ -21021,6 +21887,7 @@ else
   as_mkdir_p=false
 fi
 
+
 # as_fn_executable_p FILE
 # -----------------------
 # Test if FILE is an executable regular file.
@@ -21039,6 +21906,7 @@ as_tr_cpp="eval sed '$as_sed_cpp'" # deprecated
 as_sed_sh="y%*+%pp%;s%[^_$as_cr_alnum]%_%g"
 as_tr_sh="eval sed '$as_sed_sh'" # deprecated
 
+
 exec 6>&1
 ## ----------------------------------- ##
 ## Main body of $CONFIG_STATUS script. ##
@@ -21073,6 +21941,7 @@ case $ac_config_headers in *"
 "*) set x $ac_config_headers; shift; ac_config_headers=$*;;
 esac
 
+
 cat >>$CONFIG_STATUS <<_ACEOF || ac_write_fail=1
 # Files that config.status was made for.
 config_files="$ac_config_files"
@@ -21242,6 +22111,7 @@ cat >>$CONFIG_STATUS <<_ACEOF || ac_write_fail=1
     # Capture the value of LINGUAS because we need it to compute CATALOGS.
     LINGUAS="${LINGUAS-%UNSET%}"
 
+
 _ACEOF
 
 cat >>$CONFIG_STATUS <<\_ACEOF || ac_write_fail=1
@@ -21268,6 +22138,7 @@ do
   esac
 done
 
+
 # If the user did not use the arguments to specify the items to instantiate,
 # then the envvar interface is used.  Set only those that are not.
 # We use the long form for the default assignment because of an extremely
@@ -21310,6 +22181,7 @@ ac_tmp=$tmp
 # This happens for instance with './config.status config.h'.
 if test -n "$CONFIG_FILES"; then
 
+
 ac_cr=`echo X | tr X '\015'`
 # On cygwin, bash can eat \r inside `` if the user requested igncr.
 # But we know of no other shell where ac_cr would be empty at this
@@ -21327,6 +22199,7 @@ fi
 echo 'BEGIN {' >"$ac_tmp/subs1.awk" &&
 _ACEOF
 
+
 {
   echo "cat >conf$$subs.awk <<_ACEOF" &&
   echo "$ac_subst_vars" | sed 's/.*/&!$&$ac_delim/' &&
@@ -21569,6 +22442,7 @@ cat >>$CONFIG_STATUS <<\_ACEOF || ac_write_fail=1
   as_fn_error $? "could not setup config headers machinery" "$LINENO" 5
 fi # test -n "$CONFIG_HEADERS"
 
+
 eval set X "  :F $CONFIG_FILES  :H $CONFIG_HEADERS    :C $CONFIG_COMMANDS"
 shift
 for ac_tag
@@ -21696,6 +22570,7 @@ case $srcdir in
 esac
 ac_abs_srcdir=$ac_abs_top_srcdir$ac_dir_suffix
 
+
   case $ac_mode in
   :F)
   #
@@ -21817,6 +22692,7 @@ printf "%s\n" "$as_me: executing $ac_file commands" >&6;}
  ;;
   esac
 
+
   case $ac_file$ac_mode in
     "po-directories":C)
     for ac_file in $CONFIG_FILES; do
@@ -21955,6 +22831,7 @@ printf "%s\n" "$as_me: executing $ac_file commands" >&6;}
   esac
 done # for ac_tag
 
+
 as_fn_exit 0
 _ACEOF
 ac_clean_files=$ac_clean_files_save
@@ -21962,6 +22839,7 @@ ac_clean_files=$ac_clean_files_save
 test $ac_write_fail = 0 ||
   as_fn_error $? "write failure creating $CONFIG_STATUS" "$LINENO" 5
 
+
 # configure is writing to config.log, and then calls config.status.
 # config.status does its own redirection, appending to config.log.
 # Unfortunately, on DOS this fails, as config.log is still kept open
@@ -21987,9 +22865,11 @@ if test -n "$ac_unrecognized_opts" && test "$enable_option_checking" != no; then
 printf "%s\n" "$as_me: WARNING: unrecognized options: $ac_unrecognized_opts" >&2;}
 fi
 
+
 # Since there is no .in file to force its creation in a different build dir
 test -d driver/wayland-protocols || mkdir driver/wayland-protocols
 
+
 ###############################################################################
 #
 #       Print some warnings at the end.
@@ -22048,6 +22928,7 @@ noteL() {
   warn_prefix="$warn_prefix_1"
 }
 
+
 # ac_prog_cc_no_pthread normally only happens on AIX, because according
 # to AX_PTHREAD, AIX needs CC=xlc_r or CC=cc_r to do threads.
 # If CC is specified, it takes precedence over --with-pthread.
@@ -22128,7 +23009,7 @@ else
   fi
 fi
 
-if test "$gtk_halfassed" != no ; then
+if test "$with_gtk_req" = yes -a  "$gtk_halfassed" != no ; then
   warnL "GTK was found, but $gtk_halfassed_lib was not, so GTK"
   warn2 "can't be used."
   CONF_STATUS=1
@@ -22168,6 +23049,7 @@ elif test "$with_gtk_req" = yes -a "$have_gtk" = no ; then
   CONF_STATUS=1
 fi
 
+
 if test "$ac_cv_libxt" != yes ; then
   warnL 'The Xt library was not found.  It is required.'
   CONF_STATUS=1
@@ -22199,6 +23081,7 @@ if test "$have_motif" = yes -a "$have_lesstif" = yes ; then
   fi
 fi
 
+
 if test "$have_motif" = yes -a "$have_gtk" = no ; then
   warn  'Motif is being used, and GTK is not.'
   echo  ''
@@ -22238,6 +23121,7 @@ if test "$have_gdk_pixbuf" = no -o "$gdk_pixbuf_halfassed" = yes ; then
   warn2 'configure.'
 fi
 
+
 #if test "$have_gdk_pixbuf" = yes -a "$have_gdk_pixbuf_xlib" = no ; then
 #
 #  warnL 'The GDK-Pixbuf-Xlib library was not found.'
@@ -22257,6 +23141,7 @@ fi
 #  warn2 "GDK-Pixbuf-Xlib entirely.  So good luck with that."
 #fi
 
+
 if test "$have_jpeg" = no ; then
   if test "$with_jpeg_req" = yes ; then
     warnL 'Use of libjpeg was requested, but it was not found.'
@@ -22279,6 +23164,7 @@ if test "$have_jpeg" = no ; then
   fi
 fi
 
+
 if test "$have_png" = no ; then
   if test "$with_png_req" = yes ; then
     warnL 'Use of libpng was requested, but it was not found.'
@@ -22294,12 +23180,14 @@ if test "$have_png" = no ; then
   CONF_STATUS=1
 fi
 
+
 if test "$have_xinput" = no ; then
   warnL 'The XInput2 library was not found.  It is required.'
   if test "$xinput_halfassed" = yes ; then halfassery ; else echo '' ; fi
   CONF_STATUS=1
 fi
 
+
 if test "$have_fontconfig" = no ; then
   warnL 'The fontconfig library was not found.  It is required.'
   CONF_STATUS=1
@@ -22309,6 +23197,7 @@ elif test "$have_xft" = no ; then
   CONF_STATUS=1
 fi
 
+
 if test "$have_systemd" = no ; then
   systemd_warned=no
   if test "$systemd_too_old" = yes ; then
@@ -22344,6 +23233,7 @@ if test "$have_systemd" = no ; then
   fi
 fi
 
+
 if test "$have_gl" = no ; then
   if test "$with_gl_req" = yes ; then
     warnL 'The OpenGL 3D library is required, but it was not found.'
@@ -22370,6 +23260,7 @@ if test "$have_gl" = no ; then
 
 fi
 
+
 if test "$have_gl" = yes -a "$have_gle" = no ; then
 
  # nobody cares about this; don't print the warning unless it was
@@ -22400,6 +23291,7 @@ if test "$have_gl" = yes -a "$have_gle" = no ; then
  fi
 fi
 
+
 if test "$with_readdisplay_req" = yes -a "$have_readdisplay" = no ; then
   warn 'Use of XReadDisplay was requested, but it was not found.'
   CONF_STATUS=1
@@ -22448,6 +23340,7 @@ if test "$setuid_hacks" = no; then
   warn2 "Don't do it."
 fi
 
+
 # You are in a twisty maze of namespaces and syntaxes, all alike.
 # Expand all of these as the Makefile will probably expand them.
 # Fuck the skull of Unix.
@@ -22480,6 +23373,7 @@ for bad_choice in xscreensaver xscreensaver-settings xscreensaver-command ; do
   fi
 done
 
+
 # Warn if there's a previous RPM package installed.
 
 rpm_vers=
@@ -22511,6 +23405,7 @@ if test \! -z "$rpm_vers" ; then
 
 fi
 
+
 # Warn if there's a previous Debian package installed.
 #
 deb_vers=
@@ -22536,6 +23431,7 @@ if test \! -z "$deb_vers" ; then
   pkg_fontdir=`dpkg -L $debnames 2>&- | sed -n 's@^\(.*/\).*\.ttf$@\1@p' | head -1`
   pkg_addir=`dpkg   -L $debnames 2>&- | sed -n 's@^\(.*/\)XScreenSaver[^/]*$@\1@p'`
 
+
   if test -z "$pkg_bindir" ; then pkg_bindir="$pkg_bindir2" ; fi
 fi
 
diff --git a/configure.ac b/configure.ac
index a01792f..278fb9e 100644
--- a/configure.ac
+++ b/configure.ac
@@ -4493,7 +4493,7 @@ else
   fi
 fi
 
-if test "$gtk_halfassed" != no ; then
+if test "$with_gtk_req" = yes -a  "$gtk_halfassed" != no ; then
   warnL "GTK was found, but $gtk_halfassed_lib was not, so GTK"
   warn2 "can't be used."
   CONF_STATUS=1
-- 
2.55.0

