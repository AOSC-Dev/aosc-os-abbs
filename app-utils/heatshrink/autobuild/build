abinfo "Making ..."
make PREFIX=/usr $ABMK
abinfo "Installing ..."
mkdir -pv "$PKGDIR"/usr/{bin,lib,include}
make PREFIX="$PKGDIR"/usr install $ABMK

abinfo "Fixing permissions of static libraries ..."
chmod -v a-x "$PKGDIR"/usr/lib/*.a
