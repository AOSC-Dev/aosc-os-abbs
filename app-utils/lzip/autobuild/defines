PKGNAME=lzip
PKGSEC=utils
PKGDEP="gcc-runtime"
PKGDES="A lossless file compressor based on LZMA algorithm"

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
