chgrp 91 "$PKGDIR"/usr/bin/locate
chmod 2755 "$PKGDIR"/usr/bin/locate
ln -s locate "$PKGDIR"/usr/bin/slocate

mkdir -p "$PKGDIR"/var/lib/mlocate
chown 191:191 "$PKGDIR"/var/lib/mlocate
mkdir -p "$PKGDIR"/var/lib/mlocate
chown 192:192 "$PKGDIR"/var/lib/mlocate
