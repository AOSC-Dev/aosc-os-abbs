#!/bin/bash

abinfo "Building neon-clock..."

make PREFIX=/usr V=1 -j$(nproc) || abdie "Build failed" 1
make DESTDIR=\"\$3\" PREFIX=/usr install || abdie "Install failed" 1

abinfo "neon-clock built successfully."
