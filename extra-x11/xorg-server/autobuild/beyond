if [[ "${CROSS:-$ARCH}" != "armv4" && \
      "${CROSS:-$ARCH}" != "armv6hf" && \
      "${CROSS:-$ARCH}" != "armv7hf" ]]; then
    abinfo "Dropping Xwayland ..."
    rm -v "$PKGDIR"/usr/bin/Xwayland
fi
