From 2eaf94f5633292632641673b35a961a74b3d761e Mon Sep 17 00:00:00 2001
From: Icenowy Zheng <uwu@icenowy.me>
Date: Tue, 2 Dec 2025 00:20:12 +0800
Subject: [PATCH 1/3] AOSCOS: Hacks to get it to run on AOSC OS

Signed-off-by: Icenowy Zheng <uwu@icenowy.me>
---
 app_qt/CMakeLists.txt                         | 58 ++++---------------
 .../src/PresentationLayer/BenchmarkPage.cpp   |  4 +-
 frameworks/benchmark_service/CMakeLists.txt   |  3 +-
 .../src/systeminfogateway.cpp                 | 15 ++---
 .../cmake-utils/cmake/compiler_flags.cmake    |  2 +-
 frameworks/ngl/src/ogl_gles31_functions.h     |  4 +-
 frameworks/oglx/dummy/GLES2/gl2ext.h          |  4 +-
 frameworks/systeminfo/CMakeLists.txt          |  4 +-
 .../systeminfo/src/epoxyglinfocollector.cpp   | 18 +++---
 frameworks/systeminfo/src/properties.cpp      |  2 +-
 frameworks/systeminfo/src/systeminfo.h        | 12 ++--
 frameworks/testfw/CMakeLists.txt              |  2 +
 frameworks/testfw/deviceinfo/CMakeLists.txt   |  2 +-
 .../testfw/hostapp/basic/CMakeLists.txt       |  4 +-
 frameworks/testfw/hostapp/basic/main.cpp      |  1 +
 .../testfw/resourcemanager/CMakeLists.txt     |  2 +-
 gfxbench/CMakeLists.txt                       |  2 +-
 .../opengl/gl_wrapper/gl_wrapper_platform.h   |  4 +-
 .../private/gl_wrapper_gl31_impl.cpp          |  1 +
 .../gl_wrapper/private/gl_wrapper_private.cpp |  4 +-
 .../src/common/opengl/glb_discard_functions.h |  4 +-
 gfxbench50/src/common/CMakeLists.txt          |  1 -
 gfxbench50/src/scene5/CMakeLists.txt          |  1 -
 scripts/build.sh                              |  6 +-
 24 files changed, 68 insertions(+), 92 deletions(-)

diff --git a/app_qt/CMakeLists.txt b/app_qt/CMakeLists.txt
index a2c6587d..2981f109 100644
--- a/app_qt/CMakeLists.txt
+++ b/app_qt/CMakeLists.txt
@@ -5,7 +5,7 @@ include(CheckRequiredVars)
 check_required_vars(PRODUCT_ID PRODUCT_VERSION PRODUCT_NAME)
 
 # Set compiler flags to support C++11 features
-set(CMAKE_CXX_STANDARD 11)
+set(CMAKE_CXX_STANDARD 17)
 set(CMAKE_CXX_STANDARD_REQUIRED ON)
 set(CMAKE_CXX_EXTENSIONS OFF)
 
@@ -278,10 +278,7 @@ target_include_directories(${PROJECT_NAME} PRIVATE
 list(APPEND LIBRARIES benchmarkservice ${TESTFW_LIBRARIES} ${APPLY_FIND_LIBRARIES})
 
 if(UNIX AND NOT APPLE)
-    list(APPEND LIBRARIES
-        ${QT_LIBRARY_DIR}/libicuuc.so.56
-        ${QT_LIBRARY_DIR}/libicudata.so.56
-        ${QT_LIBRARY_DIR}/libicui18n.so.56)
+    list(APPEND LIBRARIES icuuc icudata icui18n)
 endif()
 
 if(APPLE)
@@ -313,6 +310,7 @@ endif()
 target_link_libraries(${PROJECT_NAME}
     tfw_schemas
     tfw_deviceinfo
+    GL EGL PocoFoundation PocoXML
     ${LIBRARIES}
     Qt5::Core
     Qt5::Gui
@@ -321,6 +319,13 @@ target_link_libraries(${PROJECT_NAME}
     Qt5::Xml
 )
 
+if(UNIX)
+    target_link_libraries(${PROJECT_NAME}
+        Qt5::GuiPrivate
+    )
+endif()
+
+
 # Installation
 
 if(OPT_COMMUNITY_BUILD)
@@ -642,48 +647,7 @@ elseif(UNIX AND NOT APPLE)
     string(REPLACE "(" "" CPACK_RPM_PACKAGE_REQUIRES "${CPACK_RPM_PACKAGE_REQUIRES}")
     string(REPLACE ")" "" CPACK_RPM_PACKAGE_REQUIRES "${CPACK_RPM_PACKAGE_REQUIRES}")
 
-    if(OPT_COMMUNITY_BUILD)
-    if( CMAKE_SIZEOF_P MATCHES 8)
-        set(LIB1 libicui18n.so.56)
-        set(LIB2 libicuuc.so.56)
-        set(LIB3 libicudata.so.56)
-    else()
-        set(LIB1 libicui18n.so.54)
-        set(LIB2 libicuuc.so.54)
-        set(LIB3 libicudata.so.54)
-    endif()
-
-        get_filename_component(start_scripts ${CMAKE_CURRENT_LIST_DIR}/start.sh REALPATH)
-        get_filename_component(core_dir ${QT_LIBRARY_DIR}/libQt5Core${DEBUG_POSTFIX}.so REALPATH)
-        get_filename_component(gui_dir ${QT_LIBRARY_DIR}/libQt5Gui${DEBUG_POSTFIX}.so REALPATH)
-        get_filename_component(opengl_dir ${QT_LIBRARY_DIR}/libQt5OpenGL${DEBUG_POSTFIX}.so REALPATH)
-        get_filename_component(widgets_dir ${QT_LIBRARY_DIR}/libQt5Widgets${DEBUG_POSTFIX}.so REALPATH)
-        get_filename_component(icui_dir ${QT_LIBRARY_DIR}/${LIB1} REALPATH)
-        get_filename_component(icuu_dir ${QT_LIBRARY_DIR}/${LIB2} REALPATH)
-        get_filename_component(icudata_dir ${QT_LIBRARY_DIR}/${LIB3} REALPATH)
-        get_filename_component(xml_dir ${QT_LIBRARY_DIR}/libQt5Xml.so.5 REALPATH)
-        get_filename_component(dbus_dir ${QT_LIBRARY_DIR}/libQt5DBus.so.5 REALPATH)
-        get_filename_component(xcbqpa_dir ${QT_LIBRARY_DIR}/libQt5XcbQpa.so.5 REALPATH)
-        get_filename_component(xcb_dir ${QT_PLUGINS_DIR}/platforms/libqxcb${DEBUG_POSTFIX}.so REALPATH)
-        install(FILES ${start_scripts} DESTINATION . RENAME start.sh)
-        install(FILES ${core_dir} DESTINATION . RENAME libQt5Core${DEBUG_POSTFIX}.so.5 )
-        install(FILES ${gui_dir} DESTINATION . RENAME libQt5Gui${DEBUG_POSTFIX}.so.5 )
-        install(FILES ${opengl_dir} DESTINATION . RENAME libQt5OpenGL${DEBUG_POSTFIX}.so.5 )
-        install(FILES ${widgets_dir} DESTINATION . RENAME libQt5Widgets${DEBUG_POSTFIX}.so.5 )
-        install(FILES ${xml_dir} DESTINATION . RENAME libQt5Xml${DEBUG_POSTFIX}.so.5 )
-        install(FILES ${icui_dir} DESTINATION . RENAME ${LIB1} )
-        install(FILES ${icuu_dir} DESTINATION . RENAME ${LIB2} )
-        install(FILES ${icudata_dir} DESTINATION . RENAME ${LIB3} )
-        install(FILES ${dbus_dir} DESTINATION . RENAME libQt5DBus.so.5 )
-        install(FILES ${dbus_dir} DESTINATION . RENAME platforms/libQt5DBus.so.5 )
-        install(FILES ${xcbqpa_dir} DESTINATION . RENAME platforms/libQt5XcbQpa.so.5 )
-        install(FILES ${xcb_dir} DESTINATION . RENAME platforms/libqxcb${DEBUG_POSTFIX}.so )
-        install(FILES resources/${PRODUCT_ID}/${PRODUCT_ID}.desktop DESTINATION . RENAME ${PRODUCT_ID}.desktop )
-        install(FILES resources/${PRODUCT_ID}/images/icon.png DESTINATION . RENAME ${PRODUCT_ID}.png )
-        set(CPACK_GENERATOR STGZ)
-    else()
-        set(CPACK_GENERATOR TGZ)
-    endif()
+    set(CPACK_GENERATOR TGZ)
 else()
     set(CPACK_GENERATOR TGZ)
 endif()
diff --git a/app_qt/src/PresentationLayer/BenchmarkPage.cpp b/app_qt/src/PresentationLayer/BenchmarkPage.cpp
index 3c6c3a90..37d2d0ad 100644
--- a/app_qt/src/PresentationLayer/BenchmarkPage.cpp
+++ b/app_qt/src/PresentationLayer/BenchmarkPage.cpp
@@ -113,7 +113,7 @@ void BenchmarkPage::showEvent(QShowEvent* event)
 #ifdef Q_OS_MAC
     //TODO test on mac if works enable it
    // macUtil.disableScreensaver();
-#else
+#elif Q_OS_WIN
     SystemParametersInfo(SPI_SETSCREENSAVEACTIVE, FALSE, NULL, SPIF_SENDWININICHANGE);
     if (SetThreadExecutionState(ES_CONTINUOUS | ES_SYSTEM_REQUIRED | ES_DISPLAY_REQUIRED) == NULL)
     {
@@ -131,7 +131,7 @@ void BenchmarkPage::hideEvent(QHideEvent* event)
 #ifdef Q_OS_MAC
     //TODO test on mac if works enable it
  //   macUtil.releaseScreensaverLock();
-#else
+#elif Q_OS_WIN
     SystemParametersInfo(SPI_SETSCREENSAVEACTIVE, TRUE, NULL, SPIF_SENDWININICHANGE);
     // set state back to normal
     SetThreadExecutionState(ES_CONTINUOUS);
diff --git a/frameworks/benchmark_service/CMakeLists.txt b/frameworks/benchmark_service/CMakeLists.txt
index 599bc31f..9578c9db 100644
--- a/frameworks/benchmark_service/CMakeLists.txt
+++ b/frameworks/benchmark_service/CMakeLists.txt
@@ -2,6 +2,7 @@ cmake_minimum_required(VERSION 2.8.12)
 
 project(benchmark_service)
 
+set(CMAKE_CXX_STANDARD 17)
 
 if( "$ENV{DISABLE_NETMAN}" STREQUAL "1" )
 add_definitions(-DDISABLE_NETMAN)
@@ -44,7 +45,7 @@ find_package(Vulkan)
 if(VULKAN_FOUND)
     apply_find_vars(VULKAN)
 endif()
-apply_find_vars(NGRTL SYSINF POCO)
+apply_find_vars(NGRTL SYSINF)
 
 if(OPT_COMMUNITY_BUILD)
     find_package(OpenSSL REQUIRED)
diff --git a/frameworks/benchmark_service/src/systeminfogateway.cpp b/frameworks/benchmark_service/src/systeminfogateway.cpp
index 456062f2..5282cb0c 100644
--- a/frameworks/benchmark_service/src/systeminfogateway.cpp
+++ b/frameworks/benchmark_service/src/systeminfogateway.cpp
@@ -15,7 +15,7 @@
 
 #ifndef __APPLE__
 #include "clinfocollector.h"
-#include "cudainfocollector.h"
+//#include "cudainfocollector.h"
 #endif
 
 #include "keyvaluevisitor.h"
@@ -159,9 +159,9 @@ public:
     }
     
 #ifndef __APPLE__
-    void operator()(const std::string&, sysinf::CudaInfo cudaInfo) {
+/*    void operator()(const std::string&, sysinf::CudaInfo cudaInfo) {
         operator()("Cuda device", cudaInfo.devices);
-    }
+    }*/
 
     void operator()(const std::string&, sysinf::ClInfo clInfo) {
         for (size_t i = 0; i < clInfo.platforms.size(); ++i) {
@@ -231,7 +231,7 @@ void SystemInfoGateway::collectSystemInfo()
     sysinf::collectMetalInfo(d->systemInfo);
 #ifndef __APPLE__
     sysinf::collectClInfo(d->systemInfo);
-    sysinf::collectCudaInfo(d->systemInfo);
+//    sysinf::collectCudaInfo(d->systemInfo);
     sysinf::collectVulkanInfo(d->systemInfo);
 #endif
 
@@ -596,7 +596,7 @@ std::vector<Configuration> SystemInfoGateway::getConfigurations() const
     std::vector<Configuration> configurations;
 #ifndef __APPLE__
     std::vector<Configuration> configurationsCL = clConfigurations();
-    std::vector<Configuration> configurationsCU = cuConfigurations();
+//    std::vector<Configuration> configurationsCU = cuConfigurations();
 #else
     std::vector<Configuration> configurationsCL;
     std::vector<Configuration> configurationsCU;
@@ -604,7 +604,7 @@ std::vector<Configuration> SystemInfoGateway::getConfigurations() const
     std::vector<Configuration> configurationsGPU = graphicsConfiguration();
 
     configurations.insert(configurations.end(), configurationsCL.begin(), configurationsCL.end());
-    configurations.insert(configurations.end(), configurationsCU.begin(), configurationsCU.end());
+//    configurations.insert(configurations.end(), configurationsCU.begin(), configurationsCU.end());
     configurations.insert(configurations.end(), configurationsGPU.begin(), configurationsGPU.end());
 
     return configurations;
@@ -1110,7 +1110,7 @@ std::vector<Configuration> SystemInfoGateway::clConfigurations() const
     return configurations;
 }
 
-std::vector<Configuration> SystemInfoGateway::cuConfigurations() const
+/*std::vector<Configuration> SystemInfoGateway::cuConfigurations() const
 {
     std::vector<Configuration> configurations;
 
@@ -1175,6 +1175,7 @@ std::vector<Configuration> SystemInfoGateway::cuConfigurations() const
 
     return configurations;
 }
+*/
 #endif // __APPLE__
 
 std::vector<Configuration> SystemInfoGateway::mtlConfigurations() const
diff --git a/frameworks/cmake-utils/cmake/compiler_flags.cmake b/frameworks/cmake-utils/cmake/compiler_flags.cmake
index 061b8429..a80d935b 100644
--- a/frameworks/cmake-utils/cmake/compiler_flags.cmake
+++ b/frameworks/cmake-utils/cmake/compiler_flags.cmake
@@ -24,7 +24,7 @@ elseif (UNIX)
         if(CMAKE_CXX_COMPILER_VERSION GREATER "7.5")
             set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wno-class-memaccess -Wno-deprecated-declarations -Wno-c++20-compat -Wno-format-overflow")
         endif()
-        set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wno-array-bounds -Wno-maybe-uninitialized -std=c++11 -Wall -Werror -Wno-unused-parameter -fno-strict-aliasing -Wno-strict-aliasing -Wno-invalid-offsetof")
+        set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wno-array-bounds -Wno-maybe-uninitialized -std=c++11 -Wall -Wno-unused-parameter -fno-strict-aliasing -Wno-strict-aliasing -Wno-invalid-offsetof")
     endif()
 
 elseif (${CXX_COMPILER_ID_UPPER} STREQUAL MSVC)
diff --git a/frameworks/ngl/src/ogl_gles31_functions.h b/frameworks/ngl/src/ogl_gles31_functions.h
index f2679cf5..a21c74f9 100644
--- a/frameworks/ngl/src/ogl_gles31_functions.h
+++ b/frameworks/ngl/src/ogl_gles31_functions.h
@@ -98,10 +98,10 @@ extern PFNGLOBJECTLABELPROC glObjectLabelProc;
 typedef void(GFXB_APIENTRY* PFNGLGETOBJECTLABELPROC) (GLenum identifier, GLuint name, GLsizei bufSize, GLsizei *length, GLchar *label);
 extern PFNGLGETOBJECTLABELPROC glGetObjectLabelProc;
 
-typedef void(GFXB_APIENTRY* PFNGLOBJECTPTRLABELPROC) (const void* ptr, GLsizei length, const GLchar *label);
+typedef void(GFXB_APIENTRY* PFNGLOBJECTPTRLABELPROC) (void* ptr, GLsizei length, const GLchar *label);
 extern PFNGLOBJECTPTRLABELPROC glObjectPtrLabelProc;
 
-typedef void(GFXB_APIENTRY* PFNGLGETOBJECTPTRLABELPROC) (const void* ptr, GLsizei bufSize, GLsizei *length, GLchar *label);
+typedef void(GFXB_APIENTRY* PFNGLGETOBJECTPTRLABELPROC) (void* ptr, GLsizei bufSize, GLsizei *length, GLchar *label);
 extern PFNGLGETOBJECTPTRLABELPROC glGetObjectPtrLabelProc;
 
 #if defined HAVE_GLES3 || defined ENABLE_FRAME_CAPTURE || GL_WRAPPER_ENABLED || defined EMSCRIPTEN || defined NACL
diff --git a/frameworks/oglx/dummy/GLES2/gl2ext.h b/frameworks/oglx/dummy/GLES2/gl2ext.h
index 03568671..cd2c7260 100644
--- a/frameworks/oglx/dummy/GLES2/gl2ext.h
+++ b/frameworks/oglx/dummy/GLES2/gl2ext.h
@@ -149,8 +149,8 @@ GL_APICALL void GL_APIENTRY glPushDebugGroupKHR (GLenum source, GLuint id, GLsiz
 GL_APICALL void GL_APIENTRY glPopDebugGroupKHR (void);
 GL_APICALL void GL_APIENTRY glObjectLabelKHR (GLenum identifier, GLuint name, GLsizei length, const GLchar *label);
 GL_APICALL void GL_APIENTRY glGetObjectLabelKHR (GLenum identifier, GLuint name, GLsizei bufSize, GLsizei *length, GLchar *label);
-GL_APICALL void GL_APIENTRY glObjectPtrLabelKHR (const void *ptr, GLsizei length, const GLchar *label);
-GL_APICALL void GL_APIENTRY glGetObjectPtrLabelKHR (const void *ptr, GLsizei bufSize, GLsizei *length, GLchar *label);
+GL_APICALL void GL_APIENTRY glObjectPtrLabelKHR (void *ptr, GLsizei length, const GLchar *label);
+GL_APICALL void GL_APIENTRY glGetObjectPtrLabelKHR (void *ptr, GLsizei bufSize, GLsizei *length, GLchar *label);
 GL_APICALL void GL_APIENTRY glGetPointervKHR (GLenum pname, void **params);
 #endif
 #endif /* GL_KHR_debug */
diff --git a/frameworks/systeminfo/CMakeLists.txt b/frameworks/systeminfo/CMakeLists.txt
index 7fd7118a..8cac8d93 100644
--- a/frameworks/systeminfo/CMakeLists.txt
+++ b/frameworks/systeminfo/CMakeLists.txt
@@ -1,6 +1,8 @@
 cmake_minimum_required(VERSION 3.5)
 project(systeminfo)
 
+set(CMAKE_CXX_STANDARD 17)
+
 include(compiler_flags)
 include(apply_find_vars)
 include(EmbedFiles)
@@ -23,7 +25,7 @@ endif()
 
 find_package(Poco REQUIRED COMPONENTS Foundation)
 find_package(ngrtl REQUIRED core pngio)
-apply_find_vars(POCO NGRTL)
+apply_find_vars(NGRTL)
 
 set(ENABLE_FIND_VULKAN 1 CACHE STRING "help desc" FORCE)
 
diff --git a/frameworks/systeminfo/src/epoxyglinfocollector.cpp b/frameworks/systeminfo/src/epoxyglinfocollector.cpp
index 4c46ad23..58d7da21 100644
--- a/frameworks/systeminfo/src/epoxyglinfocollector.cpp
+++ b/frameworks/systeminfo/src/epoxyglinfocollector.cpp
@@ -877,9 +877,9 @@ namespace detail
     void collectGl(GLESInfo& gl, bool& hasGl)
     {
 #if defined(HAVE_GLFW3)
-        if (!epoxy_is_gl_available()) {
+/*        if (!epoxy_is_gl_available()) {
             return;
-        }
+        }*/
         using namespace detail;
         static const Version GL_VERSIONS[] = {
                 { 1, 0, &nullQuery },
@@ -927,7 +927,7 @@ namespace detail
                 /* Ignore */
             }
         }
-        epoxy_gl_reinitialize_function_pointers();
+        //epoxy_gl_reinitialize_function_pointers();
 #endif
     }
 
@@ -936,9 +936,9 @@ namespace detail
     void collectEgl(EGLInfo& egl, bool& hasEgl)
     {
 #if !TARGET_OS_IPHONE
-        if (!epoxy_is_egl_available()) {
+/*        if (!epoxy_is_egl_available()) {
             return;
-        }
+        }*/
 
         EGLDisplay dpy = eglGetDisplay(EGL_DEFAULT_DISPLAY);
         if (dpy == EGL_NO_DISPLAY) {
@@ -978,10 +978,10 @@ namespace detail
 #if !TARGET_OS_IPHONE
     void collectGles(SystemInfo& sysinf, GLESInfo& gles, bool& hasGles)
     {
-        if (!epoxy_is_egl_available()) {
+/*        if (!epoxy_is_egl_available()) {
             NGLOG_INFO("epoxy_is_egl_available() returned false");
             return;
-        }
+        }*/
 
         int androidVersonCode = 1;
         std::map<std::string, std::string>::iterator it = sysinf.osInfo.attributes.find("build_details/GLBPD_OS_BUILD_VERSION.SDK");
@@ -1030,7 +1030,7 @@ namespace detail
                 NGLOG_INFO("Skipping GLES version(%s.%s) for Android Version Code %s", version.major, version.minor, androidVersonCode);    
             }
         }
-        epoxy_gl_reinitialize_function_pointers();
+        //epoxy_gl_reinitialize_function_pointers();
     }
 #else
     void collectGles(SystemInfo& sysinf, GLESInfo& gles, bool& hasGles)
@@ -1076,7 +1076,7 @@ namespace detail
             [context release];
             context = nil;
         }
-        epoxy_gl_reinitialize_function_pointers();
+        //epoxy_gl_reinitialize_function_pointers();
     }
 #endif
 }
diff --git a/frameworks/systeminfo/src/properties.cpp b/frameworks/systeminfo/src/properties.cpp
index 0ca38a68..dd9ce4ad 100644
--- a/frameworks/systeminfo/src/properties.cpp
+++ b/frameworks/systeminfo/src/properties.cpp
@@ -348,7 +348,7 @@ void sysinf::Properties::collect(DeviceInfoCollector& deviceInfoCollector, sysin
     deviceInfoCollector.collectAll(systeminfo);
 #ifndef __APPLE__
     sysinf::collectClInfo(systeminfo);
-    sysinf::collectCudaInfo(systeminfo);
+//    sysinf::collectCudaInfo(systeminfo);
 #endif
     sysinf::collectVulkanInfo(systeminfo);
     sysinf::collectMetalInfo(systeminfo);
diff --git a/frameworks/systeminfo/src/systeminfo.h b/frameworks/systeminfo/src/systeminfo.h
index 82dd6f30..e1dd12fb 100644
--- a/frameworks/systeminfo/src/systeminfo.h
+++ b/frameworks/systeminfo/src/systeminfo.h
@@ -12,7 +12,7 @@
 
 #ifndef __APPLE__
 #include "clinfo.h"
-#include "cudainfo.h"
+//#include "cudainfo.h"
 #endif
 
 #include "metalinfo.h"
@@ -37,7 +37,7 @@ public:
 		hasEGL = 0;
 		hasGLES = 0;
 		hasCl = 0;
-		hasCuda = 0;
+//		hasCuda = 0;
 		hasMetal = 0;
 		hasDirectx = 0;
 		hasDirectx12 = 0;
@@ -66,7 +66,7 @@ public:
     GLESInfo glesInfo;
 #ifndef __APPLE__
     ClInfo clInfo;
-    CudaInfo cudaInfo;
+//    CudaInfo cudaInfo;
 #endif
     MetalInfo metalInfo;
 #ifdef WIN32
@@ -80,7 +80,7 @@ public:
     bool hasEGL;
     bool hasGLES;
     bool hasCl;
-    bool hasCuda;
+//    bool hasCuda;
     bool hasMetal;
 	bool hasDirectx;
 	bool hasDirectx12;
@@ -107,9 +107,9 @@ public:
             visitor("cl", clInfo);
         }
 
-        if (hasCuda) {
+/*        if (hasCuda) {
             visitor("cuda", cudaInfo);
-        }
+        }*/
 #endif // __APPLE__
         if (hasEGL) {
             visitor("egl", eglInfo);
diff --git a/frameworks/testfw/CMakeLists.txt b/frameworks/testfw/CMakeLists.txt
index fd9f665b..6dfbfe36 100644
--- a/frameworks/testfw/CMakeLists.txt
+++ b/frameworks/testfw/CMakeLists.txt
@@ -2,6 +2,8 @@ cmake_minimum_required(VERSION 3.5)
 
 project(testfw)
 
+set(CMAKE_CXX_STANDARD 17)
+
 include(testfw.cmake)
 
 # Optional: if set add each directory to the generated project.
diff --git a/frameworks/testfw/deviceinfo/CMakeLists.txt b/frameworks/testfw/deviceinfo/CMakeLists.txt
index d6801705..b38762e6 100644
--- a/frameworks/testfw/deviceinfo/CMakeLists.txt
+++ b/frameworks/testfw/deviceinfo/CMakeLists.txt
@@ -14,7 +14,7 @@ include(EmbedFiles)
 
 find_package(Poco REQUIRED COMPONENTS Foundation)
 find_package(ngrtl REQUIRED core pngio)
-apply_find_vars(POCO NGRTL)
+apply_find_vars(NGRTL)
 
 include_directories(${CMAKE_CURRENT_LIST_DIR}/..)
 
diff --git a/frameworks/testfw/hostapp/basic/CMakeLists.txt b/frameworks/testfw/hostapp/basic/CMakeLists.txt
index 768b7d63..cb16bd65 100644
--- a/frameworks/testfw/hostapp/basic/CMakeLists.txt
+++ b/frameworks/testfw/hostapp/basic/CMakeLists.txt
@@ -47,7 +47,7 @@ endif()
 
 find_package(Poco REQUIRED Util XML JSON Foundation)
 find_package(ngrtl REQUIRED core)
-apply_find_vars(NGRTL POCO)
+apply_find_vars(NGRTL)
 
 if (NOT TARGET tfw_schemas)
     add_subdirectory(${CMAKE_CURRENT_LIST_DIR}/../../schemas schemas)
@@ -287,6 +287,8 @@ if(MSVC)
     target_link_libraries(${EXECUTABLE_NAME} "advapi32.lib;gdi32.lib;shell32.lib")
 endif()
 
+target_link_libraries(${EXECUTABLE_NAME} Poco::Foundation Poco::Util)
+
 install(TARGETS ${EXECUTABLE_NAME} DESTINATION bin)
 if(APPLE AND NOT IOS)
     if(OPT_FIXUP_BUNDLE)
diff --git a/frameworks/testfw/hostapp/basic/main.cpp b/frameworks/testfw/hostapp/basic/main.cpp
index 8a2f0aea..520a31d2 100644
--- a/frameworks/testfw/hostapp/basic/main.cpp
+++ b/frameworks/testfw/hostapp/basic/main.cpp
@@ -4,6 +4,7 @@
  * This file is part of GFXBench. See the top-level LICENSE file for details.
  */
 #include <Poco/Thread.h>
+#include <Poco/ThreadPool.h>
 #include <Poco/RunnableAdapter.h>
 #include <Poco/Util/Application.h>
 #include <Poco/Util/IntValidator.h>
diff --git a/frameworks/testfw/resourcemanager/CMakeLists.txt b/frameworks/testfw/resourcemanager/CMakeLists.txt
index df9dd205..8ede3f96 100644
--- a/frameworks/testfw/resourcemanager/CMakeLists.txt
+++ b/frameworks/testfw/resourcemanager/CMakeLists.txt
@@ -12,7 +12,7 @@ include(apply_find_vars)
 find_package(Poco REQUIRED COMPONENTS Foundation)
 find_package(ngrtl REQUIRED core)
 
-apply_find_vars(POCO NGRTL)
+apply_find_vars(NGRTL)
 
 include_directories(${TESTFW_ROOT})
 
diff --git a/gfxbench/CMakeLists.txt b/gfxbench/CMakeLists.txt
index 62a0b2d1..d59e2302 100644
--- a/gfxbench/CMakeLists.txt
+++ b/gfxbench/CMakeLists.txt
@@ -41,7 +41,7 @@ if(NOT DISABLE_GFX4)
             build_gfx4_metal()
         elseif(PRODUCT_ID AND ${PRODUCT_ID} STREQUAL "gfxbench" )
             build_gfx4_gl()
-            if (NOT ANDROID)
+	    if (WIN32)
                 build_gfx4_dx()
             endif()
         endif()
diff --git a/gfxbench40/src/common/opengl/gl_wrapper/gl_wrapper_platform.h b/gfxbench40/src/common/opengl/gl_wrapper/gl_wrapper_platform.h
index afd03667..6e0d1315 100644
--- a/gfxbench40/src/common/opengl/gl_wrapper/gl_wrapper_platform.h
+++ b/gfxbench40/src/common/opengl/gl_wrapper/gl_wrapper_platform.h
@@ -33,9 +33,9 @@ GLuint GL_APIENTRY glGetDebugMessageLog(GLuint count, GLsizei bufsize, GLenum* s
 void GL_APIENTRY glDebugMessageControl(GLenum source, GLenum type, GLenum severity, GLsizei count, const GLuint* ids, GLboolean enabled);
 void GL_APIENTRY glDebugMessageInsert(GLenum source, GLenum type, GLuint id, GLenum severity, GLsizei length, const GLchar* buf);
 void GL_APIENTRY glGetObjectLabel(GLenum identifier, GLuint name, GLsizei bufSize, GLsizei* length, GLchar *label);
-void GL_APIENTRY glGetObjectPtrLabel(const void *ptr, GLsizei bufSize, GLsizei* length, GLchar *label);
+void GL_APIENTRY glGetObjectPtrLabel(void *ptr, GLsizei bufSize, GLsizei* length, GLchar *label);
 void GL_APIENTRY glObjectLabel(GLenum identifier, GLuint name, GLsizei length, const GLchar* label);
-void GL_APIENTRY glObjectPtrLabel(const void *ptr, GLsizei length, const GLchar* label);
+void GL_APIENTRY glObjectPtrLabel(void *ptr, GLsizei length, const GLchar* label);
 void GL_APIENTRY glPopDebugGroup(void);
 void GL_APIENTRY glPushDebugGroup(GLenum source, GLuint id, GLsizei length, const GLchar *message);
 
diff --git a/gfxbench40/src/common/opengl/gl_wrapper/private/gl_wrapper_gl31_impl.cpp b/gfxbench40/src/common/opengl/gl_wrapper/private/gl_wrapper_gl31_impl.cpp
index 3a53fb40..0b858072 100644
--- a/gfxbench40/src/common/opengl/gl_wrapper/private/gl_wrapper_gl31_impl.cpp
+++ b/gfxbench40/src/common/opengl/gl_wrapper/private/gl_wrapper_gl31_impl.cpp
@@ -3,6 +3,7 @@
  * SPDX-License-Identifier: BSD-3-Clause
  * This file is part of GFXBench. See the top-level LICENSE file for details.
  */
+#include <cstddef>
 #include "gl_wrapper_private.h"
 #ifdef HAVE_GLEW
 GLWrapperImpl * wrapper = GLWrapperImpl::Get();
diff --git a/gfxbench40/src/common/opengl/gl_wrapper/private/gl_wrapper_private.cpp b/gfxbench40/src/common/opengl/gl_wrapper/private/gl_wrapper_private.cpp
index 20735440..a79513cd 100644
--- a/gfxbench40/src/common/opengl/gl_wrapper/private/gl_wrapper_private.cpp
+++ b/gfxbench40/src/common/opengl/gl_wrapper/private/gl_wrapper_private.cpp
@@ -508,7 +508,7 @@ void GL_APIENTRY glGetObjectLabel(GLenum identifier, GLuint name, GLsizei bufSiz
 
 #pragma push_macro("glGetObjectPtrLabel")
 #undef glGetObjectPtrLabel
-void GL_APIENTRY glGetObjectPtrLabel(const void *ptr, GLsizei bufSize, GLsizei* length, GLchar *label)
+void GL_APIENTRY glGetObjectPtrLabel(void *ptr, GLsizei bufSize, GLsizei* length, GLchar *label)
 {
 #pragma pop_macro("glGetObjectPtrLabel")
     GL_Impl::glGetObjectPtrLabel(ptr, bufSize, length, label);
@@ -524,7 +524,7 @@ void GL_APIENTRY glObjectLabel(GLenum identifier, GLuint name, GLsizei length, c
 
 #pragma push_macro("glObjectPtrLabel")
 #undef glObjectPtrLabel
-void GL_APIENTRY glObjectPtrLabel(const void *ptr, GLsizei length, const GLchar* label)
+void GL_APIENTRY glObjectPtrLabel(void *ptr, GLsizei length, const GLchar* label)
 {
 #pragma pop_macro("glObjectPtrLabel")
      GL_Impl::glObjectPtrLabel(ptr, length, label);
diff --git a/gfxbench40/src/common/opengl/glb_discard_functions.h b/gfxbench40/src/common/opengl/glb_discard_functions.h
index 9189e323..ad0a6b41 100644
--- a/gfxbench40/src/common/opengl/glb_discard_functions.h
+++ b/gfxbench40/src/common/opengl/glb_discard_functions.h
@@ -94,10 +94,10 @@ extern PFNGLOBJECTLABELPROC glObjectLabelProc;
 typedef void(GFXB_APIENTRY* PFNGLGETOBJECTLABELPROC) (GLenum identifier, GLuint name, GLsizei bufSize, GLsizei *length, GLchar *label);
 extern PFNGLGETOBJECTLABELPROC glGetObjectLabelProc;
 
-typedef void(GFXB_APIENTRY* PFNGLOBJECTPTRLABELPROC) (const void* ptr, GLsizei length, const GLchar *label);
+typedef void(GFXB_APIENTRY* PFNGLOBJECTPTRLABELPROC) (void* ptr, GLsizei length, const GLchar *label);
 extern PFNGLOBJECTPTRLABELPROC glObjectPtrLabelProc;
      
-typedef void(GFXB_APIENTRY* PFNGLGETOBJECTPTRLABELPROC) (const void* ptr, GLsizei bufSize, GLsizei *length, GLchar *label);
+typedef void(GFXB_APIENTRY* PFNGLGETOBJECTPTRLABELPROC) (void* ptr, GLsizei bufSize, GLsizei *length, GLchar *label);
 extern PFNGLGETOBJECTPTRLABELPROC glGetObjectPtrLabelProc;
 
 
diff --git a/gfxbench50/src/common/CMakeLists.txt b/gfxbench50/src/common/CMakeLists.txt
index b1ee671f..8b29081b 100644
--- a/gfxbench50/src/common/CMakeLists.txt
+++ b/gfxbench50/src/common/CMakeLists.txt
@@ -78,7 +78,6 @@ set(COMMON_SRC
 source_group( "" FILES ${COMMON_SRC} )
 
 find_package(Poco REQUIRED Foundation)
-apply_find_vars(POCO)
 
 add_library(gfxb_common STATIC ${COMMON_SRC})
 
diff --git a/gfxbench50/src/scene5/CMakeLists.txt b/gfxbench50/src/scene5/CMakeLists.txt
index ca8417e0..d49a9fd7 100644
--- a/gfxbench50/src/scene5/CMakeLists.txt
+++ b/gfxbench50/src/scene5/CMakeLists.txt
@@ -145,7 +145,6 @@ endif()
 source_group( "" FILES ${SOURCES} )
 
 find_package(Poco REQUIRED Foundation)
-apply_find_vars(POCO)
 
 if(MSVC)
     set(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} /DELAYLOAD:vulkan-1.dll")
diff --git a/scripts/build.sh b/scripts/build.sh
index 8e501d7a..15a67722 100755
--- a/scripts/build.sh
+++ b/scripts/build.sh
@@ -306,7 +306,7 @@ case $PLATFORM in
     ios|macosx)
     ;;
     *)
-    PROJECTS+=" frameworks/clew frameworks/cudaw"
+    PROJECTS+=" frameworks/clew"
     ;;
 esac
 #systeminfo have to followed by clew and cudaw
@@ -461,6 +461,10 @@ case $PLATFORM in
             linux_arm64)
                COMMON_OPTS+=" -DOGLX_VARIANT=dummy"
         esac
+        case $PLATFORM in
+            linux*)
+               COMMON_OPTS+=" -DCMAKE_DISABLE_FIND_PACKAGE_cudaw=YES":
+        esac
         case $PLATFORM in
                 macosx)
                     DISPLAY_PROTOCOL=UNSET
-- 
2.52.0

