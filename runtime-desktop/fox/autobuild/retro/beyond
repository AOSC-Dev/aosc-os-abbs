abinfo "Dropping extra documentation..."
rm -rfv "$PKGDIR"/usr/share/doc

abinfo "Dropping extra programs..."
rm -fv "$PKGDIR"/usr/bin/Adie.stx
rm -fv "$PKGDIR"/usr/bin/PathFinder
rm -fv "$PKGDIR"/usr/bin/adie
rm -fv "$PKGDIR"/usr/bin/reswrap
rm -fv "$PKGDIR"/usr/bin/shutterbug
rm -fv "$PKGDIR"/usr/share/man/man1/PathFinder.1.xz
rm -fv "$PKGDIR"/usr/share/man/man1/adie.1.xz
rm -fv "$PKGDIR"/usr/share/man/man1/reswrap.1.xz
rm -fv "$PKGDIR"/usr/share/man/man1/shutterbug.1.xz
