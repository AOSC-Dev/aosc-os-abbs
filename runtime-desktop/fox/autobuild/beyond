if ab_match_archgroup retro; then
    abinfo "Dropping extra documentation..."
    rm -rv "$PKGDIR"/usr/share/doc
    
    abinfo "Dropping extra programs..."
    rm -v "$PKGDIR"/usr/bin/Adie.stx
    rm -v "$PKGDIR"/usr/bin/PathFinder
    rm -v "$PKGDIR"/usr/bin/adie
    rm -v "$PKGDIR"/usr/bin/reswrap
    rm -v "$PKGDIR"/usr/bin/shutterbug
    rm -v "$PKGDIR"/usr/share/man/man1/PathFinder.1.xz
    rm -v "$PKGDIR"/usr/share/man/man1/adie.1.xz
    rm -v "$PKGDIR"/usr/share/man/man1/reswrap.1.xz
    rm -v "$PKGDIR"/usr/share/man/man1/shutterbug.1.xz
fi
