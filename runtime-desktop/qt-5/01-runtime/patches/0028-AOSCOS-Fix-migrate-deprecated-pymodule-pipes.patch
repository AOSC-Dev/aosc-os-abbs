From 2d66c637fc9195e5867ea23a892b63e0dfd7d96a Mon Sep 17 00:00:00 2001
From: CyberNeko-AI <cyberneko.ai@aosc.io>
Date: Sun, 1 Mar 2026 16:14:44 +0800
Subject: [PATCH 28/29] [AOSCOS] Fix: migrate deprecated python module `pipes`
 (use `shlex` or `subprocess` instead)

---
 .../chromium/build/android/apk_operations.py  |  8 +--
 .../chromium/build/chromeos/test_runner.py    |  4 +-
 .../src/3rdparty/chromium/build/env_dump.py   |  4 +-
 .../chromium/build/print_python_deps.py       |  4 +-
 .../3rdparty/chromium/build/vs_toolchain.py   |  2 +-
 .../buildtools/checkdeps/graphdeps.py         | 56 ++++++++++++-------
 .../blinkpy/web_tests/port/server_process.py  |  4 +-
 .../boringssl/src/util/bot/vs_toolchain.py    |  4 +-
 .../crashpad/crashpad/build/run_tests.py      |  6 +-
 .../chromium/third_party/crashpad/update.py   |  4 +-
 .../third_party/webrtc/tools_webrtc/mb/mb.py  |  6 +-
 .../chromium/tools/clang/scripts/build.py     |  4 +-
 .../chromium/tools/cr/cr/base/host.py         |  4 +-
 .../src/3rdparty/chromium/tools/mb/mb.py      |  6 +-
 .../src/3rdparty/chromium/v8/tools/mb/mb.py   |  6 +-
 .../chromium/v8/tools/node/node_common.py     |  6 +-
 qtwebengine/src/3rdparty/ninja/configure.py   |  4 +-
 17 files changed, 75 insertions(+), 57 deletions(-)

diff --git a/qtwebengine/src/3rdparty/chromium/build/android/apk_operations.py b/qtwebengine/src/3rdparty/chromium/build/android/apk_operations.py
index d2798147a0..9c4673baeb 100755
--- a/qtwebengine/src/3rdparty/chromium/build/android/apk_operations.py
+++ b/qtwebengine/src/3rdparty/chromium/build/android/apk_operations.py
@@ -13,7 +13,7 @@ import collections
 import json
 import logging
 import os
-import pipes
+import shlex
 import posixpath
 import random
 import re
@@ -321,7 +321,7 @@ def _RunGdb(device, package_name, debug_process_name, pid, output_directory,
     cmd.append('--verbose')
   if target_cpu:
     cmd.append('--target-arch=%s' % _TargetCpuToTargetArch(target_cpu))
-  logging.warning('Running: %s', ' '.join(pipes.quote(x) for x in cmd))
+  logging.warning('Running: %s', ' '.join(shlex.quote(x) for x in cmd))
   print(_Colorize('All subsequent output is from adb_gdb script.',
                   colorama.Fore.YELLOW))
   os.execv(gdb_script_path, cmd)
@@ -525,7 +525,7 @@ def _RunDiskUsage(devices, package_name):
               odex_paths.append('/data/dalvik-cache/%s@classes%s.dex' % (
                   mangled_apk_path, suffix))
 
-    odex_sizes = _DuHelper(d, ' '.join(pipes.quote(p) for p in odex_paths))
+    odex_sizes = _DuHelper(d, ' '.join(shlex.quote(p) for p in odex_paths))
 
     return (data_dir_sizes, code_cache_sizes, apk_sizes, lib_sizes, odex_sizes,
             compilation_filter)
@@ -1001,7 +1001,7 @@ def _DisplayArgs(devices, command_line_flags_file):
   print('Existing flags per-device (via /data/local/tmp/{}):'.format(
       command_line_flags_file))
   for flags in _PrintPerDeviceOutput(devices, outputs, single_line=True):
-    quoted_flags = ' '.join(pipes.quote(f) for f in flags)
+    quoted_flags = ' '.join(shlex.quote(f) for f in flags)
     print(quoted_flags or 'No flags set.')
 
 
diff --git a/qtwebengine/src/3rdparty/chromium/build/chromeos/test_runner.py b/qtwebengine/src/3rdparty/chromium/build/chromeos/test_runner.py
index f7e791423c..af4699c8cf 100755
--- a/qtwebengine/src/3rdparty/chromium/build/chromeos/test_runner.py
+++ b/qtwebengine/src/3rdparty/chromium/build/chromeos/test_runner.py
@@ -9,7 +9,7 @@ import collections
 import json
 import logging
 import os
-import pipes
+import shlex
 import re
 import shutil
 import signal
@@ -292,7 +292,7 @@ class TastTest(RemoteTest):
         # aren't compatible.
         local_test_runner_cmd.append('-extrauseflags=tast_vm')
       if self._conditional:
-        local_test_runner_cmd.append(pipes.quote(self._conditional))
+        local_test_runner_cmd.append(shlex.quote(self._conditional))
       else:
         local_test_runner_cmd.extend(self._tests)
       device_test_script_contents.append(' '.join(local_test_runner_cmd))
diff --git a/qtwebengine/src/3rdparty/chromium/build/env_dump.py b/qtwebengine/src/3rdparty/chromium/build/env_dump.py
index 3f8217398c..bcee92ae7a 100755
--- a/qtwebengine/src/3rdparty/chromium/build/env_dump.py
+++ b/qtwebengine/src/3rdparty/chromium/build/env_dump.py
@@ -9,7 +9,7 @@
 import json
 import optparse
 import os
-import pipes
+import shlex
 import subprocess
 import sys
 
@@ -32,7 +32,7 @@ def main():
     if not options.output_json:
       parser.error('Requires --output-json option.')
 
-    envsetup_cmd = ' '.join(map(pipes.quote, args))
+    envsetup_cmd = ' '.join(map(shlex.quote, args))
     full_cmd = [
         'bash', '-c',
         '. %s > /dev/null; %s -d' % (envsetup_cmd, os.path.abspath(__file__))
diff --git a/qtwebengine/src/3rdparty/chromium/build/print_python_deps.py b/qtwebengine/src/3rdparty/chromium/build/print_python_deps.py
index 2a0118563e..ba7d996b85 100755
--- a/qtwebengine/src/3rdparty/chromium/build/print_python_deps.py
+++ b/qtwebengine/src/3rdparty/chromium/build/print_python_deps.py
@@ -13,7 +13,7 @@ This script should be compatible with Python 2 and Python 3.
 
 import argparse
 import os
-import pipes
+import shlex
 import sys
 
 # Don't use any helper modules, or else they will end up in the results.
@@ -60,7 +60,7 @@ def _NormalizeCommandLine(options):
   for whitelist in sorted(options.whitelists):
     args.extend(('--whitelist', os.path.relpath(whitelist, _SRC_ROOT)))
   args.append(os.path.relpath(options.module, _SRC_ROOT))
-  return ' '.join(pipes.quote(x) for x in args)
+  return ' '.join(shlex.quote(x) for x in args)
 
 
 def _FindPythonInDirectory(directory):
diff --git a/qtwebengine/src/3rdparty/chromium/build/vs_toolchain.py b/qtwebengine/src/3rdparty/chromium/build/vs_toolchain.py
index 6bc24a9212..85e52f67e0 100755
--- a/qtwebengine/src/3rdparty/chromium/build/vs_toolchain.py
+++ b/qtwebengine/src/3rdparty/chromium/build/vs_toolchain.py
@@ -9,7 +9,7 @@ import collections
 import glob
 import json
 import os
-import pipes
+import shlex
 import platform
 import re
 import shutil
diff --git a/qtwebengine/src/3rdparty/chromium/buildtools/checkdeps/graphdeps.py b/qtwebengine/src/3rdparty/chromium/buildtools/checkdeps/graphdeps.py
index aff3c765c8..d20b6adb23 100755
--- a/qtwebengine/src/3rdparty/chromium/buildtools/checkdeps/graphdeps.py
+++ b/qtwebengine/src/3rdparty/chromium/buildtools/checkdeps/graphdeps.py
@@ -17,7 +17,7 @@ See README.md for a detailed description of the DEPS format.
 
 import os
 import optparse
-import pipes
+import subprocess
 import re
 import sys
 
@@ -114,27 +114,45 @@ class DepsGrapher(DepsBuilder):
       self.deps.update(deps)
 
   def _DumpDependencies(self):
-    """Dumps the built dependency graph to the specified file with specified
-    format."""
-    if self.out_format == 'dot' and not self.layout_engine:
+      """Dumps the built dependency graph to the specified file with specified
+      format."""
+      pipeline_cmds = []
+
+      # 1. Determine whether unflatten processing is needed
       if self.unflatten_graph:
-        pipe = pipes.Template()
-        pipe.append('unflatten -l 2 -c 3', '--')
-        out = pipe.open(self.out_file, 'w')
+        pipeline_cmds.append('unflatten -l 2 -c 3')
+
+      # 2. Check if format conversion is needed through the dot engine
+      if not (self.out_format == 'dot' and not self.layout_engine):
+        dot_cmd = 'dot -T' + self.out_format
+        if self.layout_engine:
+          dot_cmd += ' -K' + self.layout_engine
+        pipeline_cmds.append(dot_cmd)
+
+      # 3. Execute pipeline commands or write directly to file
+      if pipeline_cmds:
+        # Connect commands with pipe symbols and redirect the final result to the target file
+        cmd_string = ' | '.join(pipeline_cmds) + f' > "{self.out_file}"'
+
+        # Start a subprocess, open stdin so Python can write data
+        process = subprocess.Popen(
+            cmd_string,
+            shell=True,
+            stdin=subprocess.PIPE,
+            universal_newlines=True
+        )
+
+        # Stream dependency graph data to stdin of subprocess
+        self._DumpDependenciesImpl(self.deps, process.stdin)
+
+        # Must close stdin and wait for process to finish, otherwise data truncation or deadlock may occur
+        process.stdin.close()
+        process.wait()
       else:
+        # If there are no commands (pure dot format without layout needed), write directly to file
         out = open(self.out_file, 'w')
-    else:
-      pipe = pipes.Template()
-      if self.unflatten_graph:
-        pipe.append('unflatten -l 2 -c 3', '--')
-      dot_cmd = 'dot -T' + self.out_format
-      if self.layout_engine:
-        dot_cmd += ' -K' + self.layout_engine
-      pipe.append(dot_cmd, '--')
-      out = pipe.open(self.out_file, 'w')
-
-    self._DumpDependenciesImpl(self.deps, out)
-    out.close()
+        self._DumpDependenciesImpl(self.deps, out)
+        out.close()
 
   def _DumpDependenciesImpl(self, deps, out):
     """Computes nodes' and edges' properties for the dependency graph |deps| and
diff --git a/qtwebengine/src/3rdparty/chromium/third_party/blink/tools/blinkpy/web_tests/port/server_process.py b/qtwebengine/src/3rdparty/chromium/third_party/blink/tools/blinkpy/web_tests/port/server_process.py
index ffe637f031..52c398dc2f 100644
--- a/qtwebengine/src/3rdparty/chromium/third_party/blink/tools/blinkpy/web_tests/port/server_process.py
+++ b/qtwebengine/src/3rdparty/chromium/third_party/blink/tools/blinkpy/web_tests/port/server_process.py
@@ -48,9 +48,9 @@ if sys.platform == 'win32':
 else:
     import fcntl
     import os
-    import pipes
+    import shlex
     import select
-    _quote_cmd = lambda cmdline: ' '.join(pipes.quote(arg) for arg in cmdline)
+    _quote_cmd = lambda cmdline: ' '.join(shlex.quote(arg) for arg in cmdline)
 
 _log = logging.getLogger(__name__)
 
diff --git a/qtwebengine/src/3rdparty/chromium/third_party/boringssl/src/util/bot/vs_toolchain.py b/qtwebengine/src/3rdparty/chromium/third_party/boringssl/src/util/bot/vs_toolchain.py
index eb3e04973d..9b5d8ea4e3 100644
--- a/qtwebengine/src/3rdparty/chromium/third_party/boringssl/src/util/bot/vs_toolchain.py
+++ b/qtwebengine/src/3rdparty/chromium/third_party/boringssl/src/util/bot/vs_toolchain.py
@@ -4,7 +4,7 @@
 
 import json
 import os
-import pipes
+import shlex
 import shutil
 import subprocess
 import sys
@@ -55,7 +55,7 @@ def SetEnvironmentAndGetRuntimeDllDirs():
     # values there.
     gyp_defines_dict = gyp.NameValueListToDict(gyp.ShlexEnv('GYP_DEFINES'))
     gyp_defines_dict['windows_sdk_path'] = win_sdk
-    os.environ['GYP_DEFINES'] = ' '.join('%s=%s' % (k, pipes.quote(str(v)))
+    os.environ['GYP_DEFINES'] = ' '.join('%s=%s' % (k, shlex.quote(str(v)))
         for k, v in gyp_defines_dict.iteritems())
     os.environ['WINDOWSSDKDIR'] = win_sdk
     os.environ['WDK_DIR'] = wdk
diff --git a/qtwebengine/src/3rdparty/chromium/third_party/crashpad/crashpad/build/run_tests.py b/qtwebengine/src/3rdparty/chromium/third_party/crashpad/crashpad/build/run_tests.py
index f48652ff78..db138c75e5 100755
--- a/qtwebengine/src/3rdparty/chromium/third_party/crashpad/crashpad/build/run_tests.py
+++ b/qtwebengine/src/3rdparty/chromium/third_party/crashpad/crashpad/build/run_tests.py
@@ -19,7 +19,7 @@ from __future__ import print_function
 
 import argparse
 import os
-import pipes
+import shlex
 import posixpath
 import re
 import subprocess
@@ -187,9 +187,9 @@ def _RunOnAndroidTarget(binary_dir, test, android_device, extra_command_line):
         script_commands = []
         for k, v in env.items():
             script_commands.append('export %s=%s' %
-                                   (pipes.quote(k), pipes.quote(v)))
+                                   (shlex.quote(k), shlex.quote(v)))
         script_commands.extend([
-            ' '.join(pipes.quote(x) for x in command_args), 'status=${?}',
+            ' '.join(shlex.quote(x) for x in command_args), 'status=${?}',
             'echo "status=${status}"', 'exit ${status}'
         ])
         adb_command.append('; '.join(script_commands))
diff --git a/qtwebengine/src/3rdparty/chromium/third_party/crashpad/update.py b/qtwebengine/src/3rdparty/chromium/third_party/crashpad/update.py
index f99640f8c9..8914d34347 100755
--- a/qtwebengine/src/3rdparty/chromium/third_party/crashpad/update.py
+++ b/qtwebengine/src/3rdparty/chromium/third_party/crashpad/update.py
@@ -9,7 +9,7 @@ from __future__ import print_function
 
 import argparse
 import os
-import pipes
+import shlex
 import re
 import subprocess
 import sys
@@ -138,7 +138,7 @@ def main(args):
          '--force',
          '--index-filter',
          'git rm --cached --ignore-unmatch ' +
-             ' '.join(pipes.quote(path) for path in parsed.exclude),
+             ' '.join(shlex.quote(path) for path in parsed.exclude),
          revision_old + '..UPDATE_TO'],
         cwd=toplevel,
         shell=IS_WINDOWS)
diff --git a/qtwebengine/src/3rdparty/chromium/third_party/webrtc/tools_webrtc/mb/mb.py b/qtwebengine/src/3rdparty/chromium/third_party/webrtc/tools_webrtc/mb/mb.py
index 6287ca2366..8957e6f6a2 100755
--- a/qtwebengine/src/3rdparty/chromium/third_party/webrtc/tools_webrtc/mb/mb.py
+++ b/qtwebengine/src/3rdparty/chromium/third_party/webrtc/tools_webrtc/mb/mb.py
@@ -20,7 +20,7 @@ import ast
 import errno
 import json
 import os
-import pipes
+import shlex
 import pprint
 import re
 import shutil
@@ -1097,8 +1097,8 @@ class MetaBuildWrapper(object):
       shell_quoter = QuoteForCmd
     else:
       env_prefix = ''
-      env_quoter = pipes.quote
-      shell_quoter = pipes.quote
+      env_quoter = shlex.quote
+      shell_quoter = shlex.quote
 
     def print_env(var):
       if env and var in env:
diff --git a/qtwebengine/src/3rdparty/chromium/tools/clang/scripts/build.py b/qtwebengine/src/3rdparty/chromium/tools/clang/scripts/build.py
index 3d009d1d49..b17475ac1a 100755
--- a/qtwebengine/src/3rdparty/chromium/tools/clang/scripts/build.py
+++ b/qtwebengine/src/3rdparty/chromium/tools/clang/scripts/build.py
@@ -17,7 +17,7 @@ import glob
 import io
 import json
 import os
-import pipes
+import shlex
 import re
 import shutil
 import subprocess
@@ -102,7 +102,7 @@ def RunCommand(command, msvc_arch=None, env=None, fail_hard=True):
   # Windows follow-on args are passed to args[0] instead of the shell, don't
   # do the single-string transformation there.
   if sys.platform != 'win32':
-    command = ' '.join([pipes.quote(c) for c in command])
+    command = ' '.join([shlex.quote(c) for c in command])
   print('Running', command)
   if subprocess.call(command, env=env, shell=True) == 0:
     return True
diff --git a/qtwebengine/src/3rdparty/chromium/tools/cr/cr/base/host.py b/qtwebengine/src/3rdparty/chromium/tools/cr/cr/base/host.py
index 35e03deca4..0463460b46 100644
--- a/qtwebengine/src/3rdparty/chromium/tools/cr/cr/base/host.py
+++ b/qtwebengine/src/3rdparty/chromium/tools/cr/cr/base/host.py
@@ -7,7 +7,7 @@
 from __future__ import print_function
 
 import os
-import pipes
+import shlex
 import signal
 import subprocess
 
@@ -122,7 +122,7 @@ class Host(cr.Plugin, cr.Plugin.Type):
 
   @cr.Plugin.activemethod
   def Shell(self, *command):
-    command = ' '.join([pipes.quote(arg) for arg in command])
+    command = ' '.join([shlex.quote(arg) for arg in command])
     return self._Execute([command], shell=True, ignore_interrupt_signal=True)
 
   @cr.Plugin.activemethod
diff --git a/qtwebengine/src/3rdparty/chromium/tools/mb/mb.py b/qtwebengine/src/3rdparty/chromium/tools/mb/mb.py
index 10c8a68d3e..51091a0a4e 100755
--- a/qtwebengine/src/3rdparty/chromium/tools/mb/mb.py
+++ b/qtwebengine/src/3rdparty/chromium/tools/mb/mb.py
@@ -18,7 +18,7 @@ import collections
 import errno
 import json
 import os
-import pipes
+import shlex
 import platform
 import pprint
 import re
@@ -1723,8 +1723,8 @@ class MetaBuildWrapper(object):
       shell_quoter = QuoteForCmd
     else:
       env_prefix = ''
-      env_quoter = pipes.quote
-      shell_quoter = pipes.quote
+      env_quoter = shlex.quote
+      shell_quoter = shlex.quote
 
     def print_env(var):
       if env and var in env:
diff --git a/qtwebengine/src/3rdparty/chromium/v8/tools/mb/mb.py b/qtwebengine/src/3rdparty/chromium/v8/tools/mb/mb.py
index f3e46158b9..b2e2ffec96 100755
--- a/qtwebengine/src/3rdparty/chromium/v8/tools/mb/mb.py
+++ b/qtwebengine/src/3rdparty/chromium/v8/tools/mb/mb.py
@@ -18,7 +18,7 @@ import ast
 import errno
 import json
 import os
-import pipes
+import shlex
 import platform
 import pprint
 import re
@@ -1131,8 +1131,8 @@ class MetaBuildWrapper(object):
       shell_quoter = QuoteForCmd
     else:
       env_prefix = ''
-      env_quoter = pipes.quote
-      shell_quoter = pipes.quote
+      env_quoter = shlex.quote
+      shell_quoter = shlex.quote
 
     def print_env(var):
       if env and var in env:
diff --git a/qtwebengine/src/3rdparty/chromium/v8/tools/node/node_common.py b/qtwebengine/src/3rdparty/chromium/v8/tools/node/node_common.py
index 2efb21860e..c3766c0a21 100755
--- a/qtwebengine/src/3rdparty/chromium/v8/tools/node/node_common.py
+++ b/qtwebengine/src/3rdparty/chromium/v8/tools/node/node_common.py
@@ -7,7 +7,7 @@
 from __future__ import print_function
 
 import os
-import pipes
+import shlex
 import shutil
 import stat
 import subprocess
@@ -29,8 +29,8 @@ def EnsureDepotTools(v8_path, fetch_if_not_exist):
       print("Checking out depot_tools.")
       # shell=True needed on Windows to resolve git.bat.
       subprocess.check_call("git clone {} {}".format(
-          pipes.quote(DEPOT_TOOLS_URL),
-          pipes.quote(depot_tools)), shell=True)
+          shlex.quote(DEPOT_TOOLS_URL),
+          shlex.quote(depot_tools)), shell=True)
       # Using check_output to hide warning messages.
       subprocess.check_output(
           [sys.executable, gclient_path, "metrics", "--opt-out"],
diff --git a/qtwebengine/src/3rdparty/ninja/configure.py b/qtwebengine/src/3rdparty/ninja/configure.py
index a443748942..9900bb91f7 100755
--- a/qtwebengine/src/3rdparty/ninja/configure.py
+++ b/qtwebengine/src/3rdparty/ninja/configure.py
@@ -23,7 +23,7 @@ from __future__ import print_function
 
 from optparse import OptionParser
 import os
-import pipes
+import shlex
 import string
 import subprocess
 import sys
@@ -259,7 +259,7 @@ n.variable('configure_args', ' '.join(configure_args))
 env_keys = set(['CXX', 'AR', 'CFLAGS', 'LDFLAGS'])
 configure_env = dict((k, os.environ[k]) for k in os.environ if k in env_keys)
 if configure_env:
-    config_str = ' '.join([k + '=' + pipes.quote(configure_env[k])
+    config_str = ' '.join([k + '=' + shlex.quote(configure_env[k])
                            for k in configure_env])
     n.variable('configure_env', config_str + '$ ')
 n.newline()
-- 
2.52.0

