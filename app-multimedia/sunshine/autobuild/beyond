abinfo "Removing symlink to sunshine ..."
rm -v "${PKGDIR}"/usr/bin/sunshine

abinfo "Renaming sunshine executable file ..."
mv -v "${PKGDIR}"/usr/bin/sunshine-"${PKGVER}" "${PKGDIR}"/usr/bin/sunshine
