PKGNAME=carla
PKGSEC=sound
PKGDES="A fully-featured audio plugin host"
PKGDEP="file ffmpeg fftw fluidsynth glu gtk-2 gtk-3 liblo linuxsampler \
        mxml ntk projectm pulseaudio python-3 pyqt5 qt-4 qt-5 zlib"
PKGSUG="zynaddsubfx rdflib"
PKGSUG__AMD64="${PKGSUG} wine"
BUILDDEP="${PKGSUG}"
BUILDDEP__AMD64="${BUILDDEP} mingw+w64 gcc+w64 wine"

MAKE_AFTER="DEFAULT_QT=5 SKIP_STRIPPING=true"

PKGEPOCH=1
