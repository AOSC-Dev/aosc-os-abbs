From d1f7674851b4f77a285aade7348871ba00f0d06b Mon Sep 17 00:00:00 2001
From: xfangfang <2553041586@qq.com>
Date: Sun, 18 Jan 2026 20:15:08 +0800
Subject: [PATCH 2/6] Manually set cookie, because cpr does not conform to the
 rfc 6265

---
 wiliwili/include/api/bilibili/util/http.hpp | 13 +++----------
 wiliwili/source/api/bilibili.cpp            |  2 ++
 wiliwili/source/api/mine_api.cpp            |  4 ++++
 wiliwili/source/api/util/http.cpp           | 19 +++++++++++++++++++
 4 files changed, 28 insertions(+), 10 deletions(-)

diff --git a/wiliwili/include/api/bilibili/util/http.hpp b/wiliwili/include/api/bilibili/util/http.hpp
index b6b940b..e6f78a1 100644
--- a/wiliwili/include/api/bilibili/util/http.hpp
+++ b/wiliwili/include/api/bilibili/util/http.hpp
@@ -78,6 +78,8 @@ public:
     static inline std::string PROTOCOL = "https:";
     static inline CurlSharedObject CURL_SHARE;
 
+    static std::string getEncodedCookie(const cpr::Cookies& cookies);
+
     static std::shared_ptr<cpr::Session> createSession() {
         auto session = std::make_shared<cpr::Session>();
         CURL* curl = session->GetCurlHolder()->handle;
@@ -86,7 +88,6 @@ public:
         session->SetTimeout(cpr::Timeout{bilibili::HTTP::TIMEOUT});
         session->SetConnectTimeout(cpr::ConnectTimeout{bilibili::HTTP::CONNECTION_TIMEOUT});
         session->SetHeader(bilibili::HTTP::HEADERS);
-        session->SetCookies(bilibili::HTTP::COOKIES);
         session->SetProxies(bilibili::HTTP::PROXIES);
         session->SetVerifySsl(bilibili::HTTP::VERIFY);
         return session;
@@ -165,15 +166,7 @@ public:
         return 1;
     }
 
-    static void signParameters(cpr::Parameters& parameters) {
-        parameters.Add({{"appkey", BILIBILI_APP_KEY},
-                        {"build", BILIBILI_BUILD},
-                        {"ts", std::to_string(wiliwili::getUnixTime() * 1000)}});
-        std::vector<std::string> kv;
-        pystring::split(parameters.GetContent(cpr::CurlHolder()), kv, "&");
-        std::sort(kv.begin(), kv.end());
-        parameters.Add({{"sign", websocketpp::md5::md5_hash_hex(pystring::join("&", kv) + BILIBILI_APP_SECRET)}});
-    }
+    static void signParameters(cpr::Parameters& parameters);
 
     template <typename ReturnType>
     static void getResultAsync(const std::string& url,
diff --git a/wiliwili/source/api/bilibili.cpp b/wiliwili/source/api/bilibili.cpp
index c378a11..63f9969 100644
--- a/wiliwili/source/api/bilibili.cpp
+++ b/wiliwili/source/api/bilibili.cpp
@@ -31,6 +31,8 @@ void BilibiliClient::init(Cookies& data, std::function<void(Cookies, std::string
     for (const auto& cookie : data) {
         HTTP::COOKIES.emplace_back({cookie.first, cookie.second});
     }
+    // Manually set cookie, because cpr's cookie algorithm does not conform to the rfc6265
+    HTTP::HEADERS["cookie"] = HTTP::getEncodedCookie(HTTP::COOKIES);
 }
 
 void BilibiliClient::setProxy(const std::string& httpProxy, const std::string& httpsProxy) {
diff --git a/wiliwili/source/api/mine_api.cpp b/wiliwili/source/api/mine_api.cpp
index e7574b3..5ccff28 100644
--- a/wiliwili/source/api/mine_api.cpp
+++ b/wiliwili/source/api/mine_api.cpp
@@ -79,6 +79,8 @@ void BilibiliClient::get_login_info_v2(const std::string& qrcodeKey, const std::
                       {"device_id", deviceID},
                       {"device_name", deviceName}},
                      false};
+    // Manually set cookie, because cpr's cookie algorithm does not conform to the rfc6265
+    HTTP::HEADERS["cookie"] = HTTP::getEncodedCookie(HTTP::COOKIES);
 
     HTTP::_cpr_get(
         Api::QrLoginInfoV2, {{"qrcode_key", qrcodeKey}, {"source", "main_electron_pc"}},
@@ -94,6 +96,8 @@ void BilibiliClient::get_login_info_v2(const std::string& qrcodeKey, const std::
                         cookies[cookie.GetName()] = cookie.GetValue();
                         HTTP::COOKIES.emplace_back({cookie.GetName(), cookie.GetValue()});
                     }
+                    // Manually set cookie, because cpr's cookie algorithm does not conform to the rfc6265
+                    HTTP::HEADERS["cookie"] = HTTP::getEncodedCookie(HTTP::COOKIES);
                     if (BilibiliClient::writeCookiesCallback) {
                         BilibiliClient::writeCookiesCallback(cookies, data.refresh_token);
                     }
diff --git a/wiliwili/source/api/util/http.cpp b/wiliwili/source/api/util/http.cpp
index c08fb7c..fa3eafe 100644
--- a/wiliwili/source/api/util/http.cpp
+++ b/wiliwili/source/api/util/http.cpp
@@ -6,5 +6,24 @@
 
 namespace bilibili {
 
+std::string HTTP::getEncodedCookie(const cpr::Cookies& cookies) {
+    std::stringstream stream;
+    for (auto it = cookies.begin(); it != cookies.end(); ++it) {
+        const cpr::Cookie& item = *it;
+        stream << item.GetName() << "=" << item.GetValue();
+        if (std::next(it) != cookies.end()) stream << "; ";
+    }
+    return stream.str();
+}
+
+void HTTP::signParameters(cpr::Parameters& parameters) {
+    parameters.Add({{"appkey", BILIBILI_APP_KEY},
+                    {"build", BILIBILI_BUILD},
+                    {"ts", std::to_string(wiliwili::getUnixTime() * 1000)}});
+    std::vector<std::string> kv;
+    pystring::split(parameters.GetContent(cpr::CurlHolder()), kv, "&");
+    std::sort(kv.begin(), kv.end());
+    parameters.Add({{"sign", websocketpp::md5::md5_hash_hex(pystring::join("&", kv) + BILIBILI_APP_SECRET)}});
+}
 
 };  // namespace bilibili
\ No newline at end of file
-- 
2.52.0

