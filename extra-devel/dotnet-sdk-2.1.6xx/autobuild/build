if [[ "${CROSS:-$ARCH}" = "amd64" ]]; then
  DNCRID="linux-x64"
fi
if [[ "${CROSS:-$ARCH}" = "arm64" ]]; then
  DNCRID="linux-arm64"
fi
if [[ "${CROSS:-$ARCH}" = "armel" ]]; then
  DNCRID="linux-arm"
fi
abinfo "RID is $DNCRID"

abinfo "Download pre-built binaries"
wget "https://dotnetcli.azureedge.net/dotnet/Sdk/$PKGVER/dotnet-sdk-$PKGVER-$DNCRID.tar.gz"
tar -xf "dotnet-sdk-$PKGVER-$DNCRID.tar.gz" -C "$SRCDIR"

abinfo "Deploy files"
mkdir -p "$PKGDIR"/usr/lib/dotnet
cp -r "$SRCDIR"/sdk "$PKGDIR"/usr/lib/dotnet
