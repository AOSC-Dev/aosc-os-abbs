abinfo "Installing extra plugins ..."
mkdir -pv "$PKGDIR"/usr/share/skktools
cp -av -t "$PKGDIR"/usr/share/skktools \
    "$SRCDIR"/{convert2skk,dbm,filters} \
    "$SRCDIR"/{saihenkan.rb,skk2cdb.py,skkdic-diff.scm,unannotation.awk}

abinfo "Installing Emacs plugin ..."
install -Dvm644 "$SRCDIR"/skk-xml.el \
    "$PKGDIR"/usr/share/emacs/site-lisp/skk-xml.el
