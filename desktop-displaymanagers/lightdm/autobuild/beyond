abinfo "Removing /etc/init ..."
rm -rv "$PKGDIR"/etc/init

# Conflicts with package `dm-shared-dbus-config`.
abinfo "Removing org.freedesktop.DisplayManager.conf ..."
rm -rv "$PKGDIR"/usr/share/dbus-1/system.d/org.freedesktop.DisplayManager.conf
