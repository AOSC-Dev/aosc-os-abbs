--- a/sys/unix/Makefile.src
+++ b/sys/unix/Makefile.src
@@ -504,9 +504,9 @@ MAKEDEFS = ../util/makedefs
 # -lm required by lua
 LUA_VERSION ?=5.4.8
 LUABASE = liblua-$(LUA_VERSION).a
-LUALIB = ../lib/lua/$(LUABASE)
+LUALIB = -llua-5.4
 LUALIBS = $(LUALIB) -lm $(DLLIB)
-LUAHEADERS = lib/lua-$(LUA_VERSION)/src
+LUAHEADERS = /usr/include/lua5.4
 
 # timestamp files to reduce `make' overhead and shorten .o dependency lists
 CONFIG_H = ../src/config.h-t
@@ -849,9 +849,9 @@ updatedepend-setup:
 	(HFILE=../../include/nhlua.h && \
 	echo '/* bogus nhlua.h for updatedepend ONLY */' > $$HFILE && \
 	echo '#error bogus include/nhlua.h' >> $$HFILE && \
-	echo '#include "../$(LUAHEADERS)/lua.h"' >> $$HFILE && \
-	echo '#include "../$(LUAHEADERS)/lualib.h"' >> $$HFILE && \
-	echo '#include "../$(LUAHEADERS)/lauxlib.h"' >> $$HFILE && \
+	echo '#include "$(LUAHEADERS)/lua.h"' >> $$HFILE && \
+	echo '#include "$(LUAHEADERS)/lualib.h"' >> $$HFILE && \
+	echo '#include "$(LUAHEADERS)/lauxlib.h"' >> $$HFILE && \
 	echo '/*nhlua.h*/' >> $$HFILE )
 	@echo "Generating bogus src/tile.c"
 	@echo '#error bogus include/nhlua.h' > ../../src/tile.c
--- a/sys/unix/Makefile.top
+++ b/sys/unix/Makefile.top
@@ -121,13 +121,13 @@ DAT = $(DATNODLB) $(DATDLB)
 RECOVERBIN = recover
 
 # Lua
-LUAHEADERS = lib/lua-$(LUA_VERSION)/src
+LUAHEADERS = /usr/include/lua5.4
 LUATESTTARGET = $(LUAHEADERS)/lua.h
 LUATOP = $(LUAHEADERS)
 LUAMAKEFLAGS = CC='$(CC)' SYSCFLAGS='$(SYSCFLAGS)'
 LUA2NHTOP = ../../..
-LUABASELIB = liblua-$(LUA_VERSION).a
-TOPLUALIB = lib/lua/$(LUABASELIB)
+LUABASELIB = liblua-5.4.so
+TOPLUALIB = /usr/lib/$(LUABASELIB)
 
 ALLDEP = $(PRECHECK) $(GAME) recover Guidebook $(VARDAT) spec_levs check-dlb
 
@@ -141,20 +141,15 @@ $(GAME): lua_support
 
 lua_support: include/nhlua.h
 	@true
-$(LUATOP)/liblua.a: $(LUAHEADERS)/lua.h
-	( cd $(LUATOP) && make $(LUAMAKEFLAGS) a )
-$(TOPLUALIB): $(LUATOP)/liblua.a
-	@( if test -d lib/lua ; then true ; else mkdir -p lib/lua ; fi )
-	cp $(LUATOP)/liblua.a $@
 
-include/nhlua.h: $(TOPLUALIB)
+include/nhlua.h:
 	echo '/* nhlua.h - generated by top Makefile */' > $@
-	@echo '#include "../$(LUAHEADERS)/lua.h"' >> $@
+	@echo '#include "$(LUAHEADERS)/lua.h"' >> $@
 	@sed -e '/(lua_error)/!d' \
 		-e '/(lua_error)/s/LUA_API/ATTRNORETURN LUA_API/1' \
 		-e '/(lua_error)/s/;/ NORETURN;/1' < $(LUAHEADERS)/lua.h >> $@
-	@echo '#include "../$(LUAHEADERS)/lualib.h"' >> $@
-	@echo '#include "../$(LUAHEADERS)/lauxlib.h"' >> $@
+	@echo '#include "$(LUAHEADERS)/lualib.h"' >> $@
+	@echo '#include "$(LUAHEADERS)/lauxlib.h"' >> $@
 	@echo '/*nhlua.h*/' >> $@
 # LUATESTTARGET is this by default
 lib/lua-$(LUA_VERSION)/src/lua.h:
