abinfo "Appending flags to CPPFLAGS as Makefile does not honour other variables ..."
export CPPFLAGS="${CPPFLAGS} ${CFLAGS} ${LDFLAGS}"

abinfo "Creating directories that were not created by make install ..."
mkdir -pv "$PKGDIR"/usr/share/man/man6
