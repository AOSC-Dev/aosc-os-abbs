PKGNAME=incus-agent
PKGSEC=admin
PKGDES="Generic helper for information exchange between incus host and guest"
BUILDDEP="go"

# Incus repo itself is not a buildable gomod, which fails autobuild gomod
# configuration; disable gomod to manually handle build steps.
# ABTYPE=gomod

ABSPLITDBG=0
