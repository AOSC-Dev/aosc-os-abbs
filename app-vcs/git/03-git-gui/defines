PKGNAME=git-gui
PKGSEC=vcs
PKGDES="A graphical user interface for Git"
PKGDEP="git"
# git-gui is written in Tcl.
ABHOST=noarch
ABTYPE=meson

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
