PKGNAME=gitk
PKGSEC=vcs
PKGDES="The Git repository browser"
PKGDEP="git tk"
# gitk is written in Tcl.
ABHOST=noarch
ABTYPE=meson

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
