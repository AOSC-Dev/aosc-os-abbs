abinfo "Installing shell completions ..."
install -Dvm644 "$SRCDIR"/breezy_src/contrib/bash/brz \
    "$PKGDIR"/usr/share/bash-completion/completions/brz
abinfo "Creating symlink for bzr to brz for compatibility ..."
ln -sv brz "$PKGDIR"/usr/bin/bzr
