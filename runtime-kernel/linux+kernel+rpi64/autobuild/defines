PKGNAME=linux+kernel+rpi64
PKGSEC=kernel
PKGDEP="rpi-firmware-boot firmware-nonfree firmware-free linux-kernel-rpi64-6.18.25"
PKGDES="Generic Linux Kernel for AOSC OS (for 64-bit Raspberry Pi devices)"

PKGREP="linux+image+new linux+header+new linux+image linux+header linux+image+old linux+header+old"
PKGPROV="linux+kernel linux+image+new linux+header+new linux+image linux+header linux+image+old linux+header+old"

ABTYPE=dummy

# Note: Architecture-specific but is only a metapackage.
ABSTRIP=0
ABELFDEP=0

FAIL_ARCH="!(arm64)"
