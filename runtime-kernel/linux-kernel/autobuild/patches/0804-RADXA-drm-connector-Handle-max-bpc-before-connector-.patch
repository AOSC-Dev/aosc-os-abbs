From 3e0dd0aa067d90ca0fae9b818f7f913ae357334f Mon Sep 17 00:00:00 2001
From: Xilin Wu <sophon@radxa.com>
Date: Thu, 14 May 2026 22:11:06 +0800
Subject: [PATCH 804/823] RADXA: drm/connector: Handle max bpc before connector
 reset

Signed-off-by: Mingcong Bai <jeffbai@aosc.io>
---
 drivers/gpu/drm/drm_atomic_state_helper.c | 5 +++++
 drivers/gpu/drm/drm_connector.c           | 7 +++++--
 2 files changed, 10 insertions(+), 2 deletions(-)

diff --git a/drivers/gpu/drm/drm_atomic_state_helper.c b/drivers/gpu/drm/drm_atomic_state_helper.c
index 41f916f11a766..f94ae03f53003 100644
--- a/drivers/gpu/drm/drm_atomic_state_helper.c
+++ b/drivers/gpu/drm/drm_atomic_state_helper.c
@@ -480,6 +480,11 @@ void drm_atomic_helper_connector_reset(struct drm_connector *connector)
 
 	kfree(connector->state);
 	__drm_atomic_helper_connector_reset(connector, conn_state);
+
+	if (connector->state && connector->max_bpc_property) {
+		connector->state->max_requested_bpc = connector->max_bpc;
+		connector->state->max_bpc = connector->max_bpc;
+	}
 }
 EXPORT_SYMBOL(drm_atomic_helper_connector_reset);
 
diff --git a/drivers/gpu/drm/drm_connector.c b/drivers/gpu/drm/drm_connector.c
index 2a3a1fa0594b3..11a009e38c07f 100644
--- a/drivers/gpu/drm/drm_connector.c
+++ b/drivers/gpu/drm/drm_connector.c
@@ -2843,9 +2843,12 @@ int drm_connector_attach_max_bpc_property(struct drm_connector *connector,
 		connector->max_bpc_property = prop;
 	}
 
+	connector->max_bpc = max;
 	drm_object_attach_property(&connector->base, prop, max);
-	connector->state->max_requested_bpc = max;
-	connector->state->max_bpc = max;
+	if (connector->state) {
+		connector->state->max_requested_bpc = max;
+		connector->state->max_bpc = max;
+	}
 
 	return 0;
 }
-- 
2.52.0

