PKGNAME=botan
PKGSEC=libs
PKGDES="C++ cryptography library"
PKGDEP="gcc-runtime bash asio"
BUILDDEP="python-2 asio git"

AB_FLAGS_PIE=0

CONFOPT="--enable-modules=cvc --with-openssl \
         --with-gnump --with-bzip2 --with-zlib"

if [[ "${CROSS:-$ARCH}" = "amd64" ]]; then
    CONFOPT+=" --enable-sse2"
fi
