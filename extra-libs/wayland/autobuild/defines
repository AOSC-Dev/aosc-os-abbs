PKGNAME=wayland
PKGSEC=libs
PKGDEP="libffi expat libxml2 wayland-protocols"
BUILDDEP="docbook-xsl doxygen xmlto"
if [[ "${CROSS:-$ARCH}" = "amd64" ]]; then
    BUILDDEP+=" graphviz"
fi
PKGDES="A display server to replace X.Org"

if [[ "${CROSS:-$ARCH}" != "amd64" ]]; then
    AUTOTOOLS_AFTER=" --disable-documentation"
fi
