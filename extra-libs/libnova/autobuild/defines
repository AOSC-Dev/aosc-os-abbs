PKGNAME=libnova
PKGSEC=libs
PKGDEP="glibc"
PKGDES="A general purpose, double precision, celestial mechanics, astrometry and astrodynamics library"

ABSHADOW=no

if [[ "${CROSS:-$ARCH}" = "armel" ]]; then
    AB_FLAGS_SPECS=0
fi
