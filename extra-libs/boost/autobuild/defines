PKGNAME=boost
PKGDES="Boost C++ libs"
PKGDEP="python-2 bzip2 zlib icu"
if [[ "ARCH" != armel ]]; then PKGDEP+=' python-3 openmpi'; fi
PKGSEC=libs

NOSTATIC=no
RPMEXTRAPROVIDE="$(cat autobuild/extra)"
