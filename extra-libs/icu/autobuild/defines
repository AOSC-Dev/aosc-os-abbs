PKGNAME=icu
PKGSEC=libs
PKGDES="The International Components for Unicode"
PKGDEP=glibc

# FIXME: Clang on mipsel still segfaults.
if [[ "${CROSS:-$ARCH}" != "mipsel" ]]; then
    USECLANG=yes
else
    USECLANG=no
fi
