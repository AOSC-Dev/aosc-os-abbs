PKGNAME=glib-1
PKGSEC=libs
PKGDEP="glibc"
PKGDES="Common C routines used by GTK+ and other libs (version 1)"

NOPARALLEL=1
AUTOTOOLS_AFTER="--host=${ARCH_TARGET[$ARCH]} --target=${ARCH_TARGET[$ARCH]}"
