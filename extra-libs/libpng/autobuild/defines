PKGNAME=libpng
PKGDES="libraries used by other programs for reading and writing PNG files"
PKGDEP="zlib"
PKGSEC=libs

AB_FLAGS_PIE=0
[[ "${CROSS:-$ARCH}" = arm* ]] && AUTOTOOLS_AFTER+=" --enable-arm-neon=api"
