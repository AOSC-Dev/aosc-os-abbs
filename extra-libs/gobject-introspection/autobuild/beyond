python3 -m compileall -d /usr/lib/gobject-introspection \
    "$PKGDIR"/usr/lib/gobject-introspection
python3 -O -m compileall -d /usr/lib/gobject-introspection \
    "$PKGDIR"/usr/lib/gobject-introspection
