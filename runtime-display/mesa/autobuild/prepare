abinfo "Importing DirectX-Headers as subproject ..."
cp -rv "$SRCDIR"/../dxheaders \
    "$SRCDIR"/subprojects/DirectX-Headers-1.0

abinfo "Creating meson native configuation file to override llvm-config"
cat > "$SRCDIR"/llvm-20.ini << "EOF"
[binaries]
llvm-config = '/usr/bin/llvm-config-20'
EOF
