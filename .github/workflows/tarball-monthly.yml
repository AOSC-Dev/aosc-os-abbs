name: Publish tarball monthly

on:
  schedule:
    - cron: '0 0 1 * *'

  workflow_dispatch:

env:
  DEV_SSH_PUBLIC_KEY: ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAhL37Q/xX95T3YDGskoZp8LeF/2p3AEPl9kfkLqYms9 GitHub Actions

jobs:
  tarball:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        recipe: 
          - base
          - base+nvidia
          - base+nvidia340
          - base+nvidia390
          - buildkit
          - cinnamon
          - cinnamon+nvidia
          - cinnamon+nvidia340
          - cinnamon+nvidia390
          - container
          - gnome
          - gnome+nvidia
          - gnome+nvidia340
          - gnome+nvidia390
          - kde
          - kde+nvidia
          - kde+nvidia340
          - kde+nvidia390
          - lxde
          - lxde+nvidia
          - lxde+nvidia340
          - lxde+nvidia390
          - mate
          - mate+nvidia
          - mate+nvidia340
          - mate+nvidia390
          - server
          - xfce
          - xfce+nvidia
          - xfce+nvidia340
          - xfce+nvidia390
        buildbot:
          - Ry3950X
          - kp920
          - powernv
          - Resonance
        include:
          - buildbot: Ry3950X
            architecture: amd64
          - buildbot: kp920
            architecture: arm64
          - buildbot: powernv
            architecture: ppc64el
          - buildbot: Resonance
            architecture: loongson3
      fail-fast: false
      max-parallel: 1

    steps:
      - name: Install buildbot configurations
        env:
          DEV_SSH_PRIVATE_KEY: ${{secrets.DEV_SSH_PRIVATE_KEY}}
        run: |
          curl -LSso ~/.ssh/config.d/aosc --create-dirs "https://cdn.jsdelivr.net/gh/AOSC-Dev/Buildbots/ssh_config"
          curl -LSso ~/.ssh/known_hosts.d/aosc --create-dirs "https://cdn.jsdelivr.net/gh/AOSC-Dev/Buildbots/ssh_known_hosts"
          cat << EOF >> ~/.ssh/config
          Host *
            Include config.d/*
          EOF
          echo $DEV_SSH_PRIVATE_KEY | base64 -d > ~/.ssh/id_ed25519
          echo $DEV_SSH_PUBLIC_KEY > ~/.ssh/id_ed25519.pub
          chmod 644 ~/.ssh/config ~/.ssh/config.d/aosc
          chmod 600 ~/.ssh/id_ed25519

      - name: Build and download tarball
        env:
          RECIPE: ${{matrix.recipe}}
          BUILDBOT: ${{matrix.buildbot}}
          ARCH: ${{matrix.architecture}}
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        run: |
          ssh -tt $BUILDBOT "
            set -ex \
            && mkdir -p /buildroots/githubactions/$(echo -n $GITHUB_TOKEN | sha256sum | head -c 64) \
            && cd /buildroots/githubactions/$(echo -n $GITHUB_TOKEN | sha256sum | head -c 64) \
            && generate-releases $RECIPE"
          sftp $BUILDBOT << EOF
            get -R /buildroots/githubactions/$(echo -n $GITHUB_TOKEN | sha256sum | head -c 64)/os-$ARCH
          EOF

      - name: Upload tarball
        uses: actions/upload-artifact@v2
        with:
          name: os-${{matrix.architecture}}_${{matrix.recipe}}
          path: os-${{matrix.architecture}}/${{matrix.recipe}}
          if-no-files-found: error

      - name: Remove artifact
        env:
          BUILDBOT: ${{matrix.buildbot}}
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        if: always()
        run: ssh $BUILDBOT "rm -r /buildroots/githubactions/$(echo -n $GITHUB_TOKEN | sha256sum | head -c 64)"
