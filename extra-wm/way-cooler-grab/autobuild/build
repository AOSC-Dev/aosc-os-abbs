cargo build --release
cargo install wc-grab --root "$PKGDIR"/usr

abinfo "Removing excessive /usr/.crates.toml ..."
rm -v "$PKGDIR"/usr/.crates.toml

abinfo "Removing excessive /usr/.crates2.json"
rm -v "$PKGDIR"/usr/.crates2.json
