mkdir build
cd build
cmake .. "${CMAKE_DEF}"
make

for i in miner TurtleCoind turtle-service wallet-api wallet-upgrader zedwallet; do
	install -Dm755 "src/$i"  "${PKGDIR}/usr/bin/turtlecoin-${i}"
done
