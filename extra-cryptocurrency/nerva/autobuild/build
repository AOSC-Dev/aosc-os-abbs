abinfo "Running CMake for Nerva ..."
cmake "$SRCDIR" ${CMAKE_DEF}

abinfo "Building Nerva ..."
make

abinfo "Build done, installing files ..."
install -Dvm755 "$SRCDIR"/bin/nerva* \
    -t "${PKGDIR}/usr/bin"
