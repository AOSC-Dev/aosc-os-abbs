PKGNAME=libusb
PKGDES="A library providing USB device access and manipulation support"
PKGDEP="glibc systemd"
PKGSEC=libs

ABTYPE=autotools

# Note: Sync with Debian.
PKGEPOCH_SPIRAL=2

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
