PKGNAME=libatasmart
PKGSEC=libs
PKGDEP="systemd"
PKGDES="Library to read and parse ATA SMART data"

ABTYPE=autotools

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
