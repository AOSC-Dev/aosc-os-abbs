PKGNAME=zssh
PKGSEC=net
PKGDEP="readline"
PKGDES="SSH and Telnet client with ZMODEM file transfer capability"

ABTYPE=autotools

# FIXME: Shadow build fails.
#
# cat: VERSION: No such file or directory
# echo '#define ZSSH_VERSION "' `cat VERSION` ', built' `date` '"' > version.h
# make: *** No rule to make target 'completion.c', needed by 'completion.o'.  Stop.
# make: *** Waiting for unfinished jobs....
# cat: VERSION: No such file or directory
ABSHADOW=0
