PKGNAME=ethtool
PKGSEC=net
PKGDEP="glibc libmnl"
PKGDES="Utility for controlling network drivers and hardware"

AUTOTOOLS_AFTER=(
	"--enable-pretty-dump"
	"--enable-netlink"
)

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
