PKGNAME=lksctp-tools
PKGSEC=libs
PKGDEP="glibc"
PKGDES="Utilities and libraries to work with the SCTP protocal"

ABTYPE=autotools

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
