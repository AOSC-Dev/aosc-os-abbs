abinfo "Applying common patches ..."
ab_apply_patches \
	"$SRCDIR"/autobuild/patches/*.patch

if ab_match_archgroup retro ; then
	abinfo "Applying patches for retro ..."
	ab_apply_patches \
		"$SRCDIR"/autobuild/patches/*.patch.retro
fi
