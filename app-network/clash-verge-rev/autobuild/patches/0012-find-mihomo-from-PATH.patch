From 5edbe7cd7dbc1b0559c8f979eb365476500af24f Mon Sep 17 00:00:00 2001
From: eatradish <sakiiily@aosc.io>
Date: Mon, 13 Jan 2025 16:02:52 +0800
Subject: [PATCH 12/20] find `mihomo` from `$PATH`

---
 src-tauri/Cargo.lock          | 27 ++++++++++++++++++++++++++-
 src-tauri/Cargo.toml          |  2 +-
 src-tauri/src/core/service.rs | 14 +++++++-------
 3 files changed, 34 insertions(+), 9 deletions(-)

diff --git a/src-tauri/Cargo.lock b/src-tauri/Cargo.lock
index 364c4767..46973d52 100644
--- a/src-tauri/Cargo.lock
+++ b/src-tauri/Cargo.lock
@@ -1194,6 +1194,7 @@ dependencies = [
  "url",
  "users",
  "warp",
+ "which 7.0.2",
  "window-shadows",
  "winreg 0.55.0",
  "zip",
@@ -2100,6 +2101,12 @@ dependencies = [
  "regex",
 ]
 
+[[package]]
+name = "env_home"
+version = "0.1.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "c7f84e12ccf0a7ddc17a6c41c93326024c42920d7ee630d04950e6926645c0fe"
+
 [[package]]
 name = "env_logger"
 version = "0.11.7"
@@ -6150,7 +6157,7 @@ checksum = "b96d6b6c505282b007a9b009f2aa38b2fd0359b81a0430ceacc60f69ade4c6a0"
 dependencies = [
  "libc",
  "security-framework-sys",
- "which",
+ "which 4.4.2",
  "windows-sys 0.48.0",
 ]
 
@@ -8897,6 +8904,18 @@ dependencies = [
  "rustix 0.38.44",
 ]
 
+[[package]]
+name = "which"
+version = "7.0.2"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "2774c861e1f072b3aadc02f8ba886c26ad6321567ecc294c935434cad06f1283"
+dependencies = [
+ "either",
+ "env_home",
+ "rustix 0.38.44",
+ "winsafe",
+]
+
 [[package]]
 name = "wide"
 version = "0.7.32"
@@ -9601,6 +9620,12 @@ dependencies = [
  "windows-sys 0.59.0",
 ]
 
+[[package]]
+name = "winsafe"
+version = "0.0.19"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "d135d17ab770252ad95e9a872d365cf3090e3be864a34ab46f48555993efc904"
+
 [[package]]
 name = "wit-bindgen-rt"
 version = "0.39.0"
diff --git a/src-tauri/Cargo.toml b/src-tauri/Cargo.toml
index fae48b51..6ff5d76c 100755
--- a/src-tauri/Cargo.toml
+++ b/src-tauri/Cargo.toml
@@ -73,7 +73,7 @@ async-trait = "0.1.87"
 mihomo_api = { path = "src_crates/crate_mihomo_api" }
 ab_glyph = "0.2.29"
 tungstenite = "0.26.2"
-
+which = "7"
 
 [target.'cfg(windows)'.dependencies]
 runas = "=1.2.0"
diff --git a/src-tauri/src/core/service.rs b/src-tauri/src/core/service.rs
index c77b1199..eca48124 100644
--- a/src-tauri/src/core/service.rs
+++ b/src-tauri/src/core/service.rs
@@ -1,7 +1,10 @@
 use crate::{config::Config, utils::dirs};
 use anyhow::{bail, Context, Result};
 use serde::{Deserialize, Serialize};
-use std::{collections::HashMap, env::current_exe, path::PathBuf, process::Command as StdCommand, time::{SystemTime, UNIX_EPOCH}};
+use std::collections::HashMap;
+use std::path::PathBuf;
+use std::process::Command as StdCommand;
+use std::time::{SystemTime, UNIX_EPOCH};
 use tokio::time::Duration;
 
 // Windows only
@@ -377,11 +380,6 @@ pub(super) async fn start_with_existing_service(config_file: &PathBuf) -> Result
     let clash_core = { Config::verge().latest().clash_core.clone() };
     let clash_core = clash_core.unwrap_or("mihomo".into());
 
-    let bin_ext = if cfg!(windows) { ".exe" } else { "" };
-    let clash_bin = format!("{clash_core}{bin_ext}");
-    let bin_path = current_exe()?.with_file_name(clash_bin);
-    let bin_path = dirs::path_to_str(&bin_path)?;
-
     let config_dir = dirs::app_home_dir()?;
     let config_dir = dirs::path_to_str(&config_dir)?;
     #[cfg(target_os = "linux")]
@@ -392,9 +390,11 @@ pub(super) async fn start_with_existing_service(config_file: &PathBuf) -> Result
 
     let config_file = dirs::path_to_str(config_file)?;
 
+    let bin = which::which(&clash_core)?.to_string_lossy().to_string();
+
     let mut map = HashMap::new();
     map.insert("core_type", clash_core.as_str());
-    map.insert("bin_path", bin_path);
+    map.insert("bin_path", &bin);
     map.insert("config_dir", config_dir);
     map.insert("config_file", config_file);
     map.insert("log_file", log_path);
-- 
2.49.0

