PKGNAME="graftcp"
PKGDES="A flexible tool for redirecting a given program's TCP traffic to SOCKS5 or HTTP proxy"
PKGDEP="glibc"
BUILDDEP="go git gcc make"
PKGSEC="net"

# FIXME: Upstream supports building for arm64 architecture in development
# branch, and is likely to be contained in release 0.5.x.
# Packager may want to toggle building for arm64 on when 0.5.x is released.
FAIL_ARCH="!(amd64)"
ABSPLITDBG=0

PKGEPOCH=1
