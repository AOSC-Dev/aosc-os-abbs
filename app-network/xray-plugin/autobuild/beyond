abinfo "Removing upstream build release script ..."
rm -v "$PKGDIR"/usr/bin/build-release.sh

abinfo "Creating symlink v2ray-plugin => xray-plugin ..."
ln -sv xray-plugin "$PKGDIR"/usr/bin/v2ray-plugin
