abinfo "Move some binary to /usr/libexec ..."
mkdir -pv "$PKGDIR"/usr/libexec
mv -v "$PKGDIR"/usr/bin/install-service "$PKGDIR"/usr/libexec/clash-verge-install-service
mv -v "$PKGDIR"/usr/bin/uninstall-service "$PKGDIR"/usr/libexec/clash-verge-uninstall-service
