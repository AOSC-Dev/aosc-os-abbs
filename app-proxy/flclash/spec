VER=0.8.90
# Note: Upstream submodule url need ssh authorization, so we need to manually fetch submodule.
SRCS="git::commit=tags/v$VER;copy-repo=true;submodule=false::https://github.com/chen08209/FlClash"
CHKSUMS="SKIP"
CHKUPDATE="anitya::id=383523"
