From 6fee910b2c6496ba9af052a464a73f2bb73c9d04 Mon Sep 17 00:00:00 2001
From: eatradish <sakiiily@aosc.io>
Date: Sun, 2 Mar 2025 14:41:06 +0800
Subject: [PATCH 11/12] Do not sidecar mihomo program

---
 src-tauri/src/core/manager/state.rs | 5 +----
 src-tauri/src/core/validate.rs      | 4 +---
 2 files changed, 2 insertions(+), 7 deletions(-)

diff --git a/src-tauri/src/core/manager/state.rs b/src-tauri/src/core/manager/state.rs
index 518c6caa..c73a0432 100644
--- a/src-tauri/src/core/manager/state.rs
+++ b/src-tauri/src/core/manager/state.rs
@@ -151,10 +151,7 @@ impl CoreManager {
 
         #[cfg(unix)]
         let previous_mask = unsafe { tauri_plugin_clash_verge_sysinfo::libc::umask(0o077) };
-        let command = app_handle
-            .shell()
-            .sidecar(clash_core.as_str())
-            .map_err(|error| anyhow::anyhow!("failed to build sidecar command for core {clash_core:?}: {error:#}"))?;
+        let command = app_handle.shell().command(clash_core.as_str());
         let command = command.args([
             "-d",
             dirs::path_to_str(&config_dir)?,
diff --git a/src-tauri/src/core/validate.rs b/src-tauri/src/core/validate.rs
index 586f84e9..abe05896 100644
--- a/src-tauri/src/core/validate.rs
+++ b/src-tauri/src/core/validate.rs
@@ -358,9 +358,7 @@ impl CoreConfigValidator {
         let app_dir_str = dirs::path_to_str(&app_dir)?;
         logging!(info, Type::Validate, "验证目录: {}", app_dir_str);
 
-        let command = app_handle.shell().sidecar(clash_core.as_str()).map_err(|error| {
-            anyhow::anyhow!("failed to build validation command for core {clash_core:?}: {error:#}")
-        })?;
+        let command = app_handle.shell().command(clash_core.as_str());
         let command = command.args(["-t", "-d", app_dir_str, "-f", config_path]);
         let output = command.output().await.map_err(|error| {
             anyhow::anyhow!(
-- 
2.55.0

