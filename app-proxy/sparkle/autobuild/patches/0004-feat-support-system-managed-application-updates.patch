From 5080bcfe026060deec769e922b9c16c34a2a67d8 Mon Sep 17 00:00:00 2001
From: miwu04 <me@alanlin.icu>
Date: Sun, 13 Sep 2026 17:35:01 +0800
Subject: [PATCH 4/5] feat: support system-managed application updates

---
 electron.vite.config.ts                       | 10 +++-
 scripts/build-env.ts                          |  2 +
 src/main/resolve/autoUpdater.ts               | 10 +++-
 src/renderer/src/App.tsx                      |  5 +-
 .../src/components/settings/actions.tsx       |  6 +++
 .../components/settings/general-config.tsx    | 49 ++++++++++---------
 src/shared/build-flags.ts                     |  1 +
 src/shared/types/types.d.ts                   |  1 +
 8 files changed, 57 insertions(+), 27 deletions(-)

diff --git a/electron.vite.config.ts b/electron.vite.config.ts
index ad26ef9..da9883e 100644
--- a/electron.vite.config.ts
+++ b/electron.vite.config.ts
@@ -2,11 +2,17 @@ import { resolve } from 'path'
 import { defineConfig } from 'electron-vite'
 import react from '@vitejs/plugin-react'
 import tailwindcss from '@tailwindcss/vite'
-import { systemCoreDefaultPath, systemCoreOnlyBuild, systemServicePath } from './scripts/build-env'
+import {
+  systemCoreDefaultPath,
+  systemCoreOnlyBuild,
+  systemServicePath,
+  systemUpdateOnlyBuild
+} from './scripts/build-env'
 
 const buildDefines = {
   __SPARKLE_SYSTEM_CORE_PATH__: JSON.stringify(systemCoreDefaultPath),
-  __SPARKLE_SYSTEM_SERVICE_PATH__: JSON.stringify(systemServicePath)
+  __SPARKLE_SYSTEM_SERVICE_PATH__: JSON.stringify(systemServicePath),
+  __SPARKLE_SYSTEM_UPDATE__: JSON.stringify(systemUpdateOnlyBuild)
 }
 const omitExternalRendererResources = {
   name: 'omit-external-renderer-resources',
diff --git a/scripts/build-env.ts b/scripts/build-env.ts
index e5241ac..5eb5141 100644
--- a/scripts/build-env.ts
+++ b/scripts/build-env.ts
@@ -8,6 +8,7 @@ const configuredSystemServicePath =
   !systemServiceBuildValue || systemServiceBuildValue === '1'
     ? '/usr/bin/sparkle-service'
     : systemServiceBuildValue
+const systemUpdateBuildValue = process.env.SPARKLE_SYSTEM_UPDATE?.trim() || ''
 
 if (
   process.platform === 'linux' &&
@@ -28,3 +29,4 @@ if (
 export const systemCoreDefaultPath = process.platform === 'linux' ? configuredSystemCorePath : ''
 export const systemCoreOnlyBuild = systemCoreDefaultPath !== ''
 export const systemServicePath = systemCoreOnlyBuild ? configuredSystemServicePath : ''
+export const systemUpdateOnlyBuild = systemUpdateBuildValue === '1'
diff --git a/src/main/resolve/autoUpdater.ts b/src/main/resolve/autoUpdater.ts
index 13b98e6..73f6764 100644
--- a/src/main/resolve/autoUpdater.ts
+++ b/src/main/resolve/autoUpdater.ts
@@ -18,7 +18,7 @@ import {
   pauseServiceFallbackForAppUpdate
 } from '../service/fallback'
 import { appendAppLog } from '../utils/log'
-import { systemCoreOnlyBuild } from '../../shared/build-flags'
+import { systemCoreOnlyBuild, systemUpdateOnlyBuild } from '../../shared/build-flags'
 
 let downloadCancelToken: CancelTokenSource | null = null
 const WINDOWS_INSTALLER_MIN_TEMP_SPACE_BYTES = 1024 * 1024 * 1024
@@ -49,6 +49,10 @@ async function ensureFreeSpace(dir: string, requiredBytes: number, message: stri
 }
 
 export async function checkUpdate(): Promise<AppVersion | undefined> {
+  if (systemUpdateOnlyBuild) {
+    throw new Error('更新由您的操作系统管理')
+  }
+
   const { 'mixed-port': mixedPort = 7890 } = await getControledMihomoConfig()
   const { updateChannel = 'stable', githubToken } = await getAppConfig()
   const url = UPDATE_MANIFEST_URLS[updateChannel]
@@ -99,6 +103,10 @@ async function ensureWindowsInstallerTempSpace(): Promise<void> {
 }
 
 export async function downloadAndInstallUpdate(version: string, tag?: string): Promise<void> {
+  if (systemUpdateOnlyBuild) {
+    throw new Error('更新由您的操作系统管理')
+  }
+
   let appUpdateInstalling = false
   let sysProxyPaused = false
   const pauseSysProxy = async (): Promise<void> => {
diff --git a/src/renderer/src/App.tsx b/src/renderer/src/App.tsx
index 9999f89..46826a0 100644
--- a/src/renderer/src/App.tsx
+++ b/src/renderer/src/App.tsx
@@ -9,6 +9,7 @@ import { useAppConfig } from '@renderer/hooks/use-app-config'
 import { applyTheme, checkUpdate, setNativeTheme, setTitleBarOverlay } from '@renderer/utils/ipc'
 import { platform } from '@renderer/utils/init'
 import { TitleBarOverlayOptions } from 'electron'
+import { systemUpdateOnlyBuild } from '../../shared/build-flags'
 import MihomoIcon from './components/base/mihomo-icon'
 import useSWR from 'swr'
 
@@ -56,8 +57,8 @@ const App: React.FC = () => {
     }
   }
   const { data: latest } = useSWR(
-    autoCheckUpdate ? ['checkUpdate', updateChannel] : undefined,
-    autoCheckUpdate ? checkUpdate : (): undefined => {},
+    !systemUpdateOnlyBuild && autoCheckUpdate ? ['checkUpdate', updateChannel] : undefined,
+    !systemUpdateOnlyBuild && autoCheckUpdate ? checkUpdate : (): undefined => {},
     {
       refreshInterval: 1000 * 60 * 10
     }
diff --git a/src/renderer/src/components/settings/actions.tsx b/src/renderer/src/components/settings/actions.tsx
index 4e48361..88cb28f 100644
--- a/src/renderer/src/components/settings/actions.tsx
+++ b/src/renderer/src/components/settings/actions.tsx
@@ -17,6 +17,7 @@ import { startTour } from '@renderer/utils/driver'
 import { useNavigate } from 'react-router-dom'
 import ConfirmModal from '../base/base-confirm'
 import { notify } from '@renderer/utils/notification'
+import { systemUpdateOnlyBuild } from '../../../../shared/build-flags'
 
 async function handleCreateHeapSnapshot(): Promise<void> {
   try {
@@ -111,6 +112,11 @@ const Actions: React.FC = () => {
             size="sm"
             isLoading={checkingUpdate}
             onPress={async () => {
+              if (systemUpdateOnlyBuild) {
+                notify('更新由您的操作系统管理')
+                return
+              }
+
               try {
                 setCheckingUpdate(true)
                 const version = await checkUpdate()
diff --git a/src/renderer/src/components/settings/general-config.tsx b/src/renderer/src/components/settings/general-config.tsx
index 295944b..4f27b2a 100644
--- a/src/renderer/src/components/settings/general-config.tsx
+++ b/src/renderer/src/components/settings/general-config.tsx
@@ -8,6 +8,7 @@ import { useAppConfig } from '@renderer/hooks/use-app-config'
 import { IoIosHelpCircle } from 'react-icons/io'
 import ConfirmModal from '../base/base-confirm'
 import { notify } from '@renderer/utils/notification'
+import { systemUpdateOnlyBuild } from '../../../../shared/build-flags'
 
 const GeneralConfig: React.FC = () => {
   const { data: enable, mutate: mutateEnable } = useSWR('checkAutoRun', checkAutoRun)
@@ -80,28 +81,32 @@ const GeneralConfig: React.FC = () => {
             }}
           />
         </SettingItem>
-        <SettingItem compatKey="legacy" title="自动检查更新" divider>
-          <Switch
-            size="sm"
-            isSelected={autoCheckUpdate}
-            onValueChange={(v) => {
-              patchAppConfig({ autoCheckUpdate: v })
-            }}
-          />
-        </SettingItem>
-        <SettingItem compatKey="legacy" title="更新通道" divider>
-          <Tabs
-            size="sm"
-            color="primary"
-            selectedKey={updateChannel}
-            onSelectionChange={async (v) => {
-              patchAppConfig({ updateChannel: v as AppUpdateChannel })
-            }}
-          >
-            <Tab key="stable" title="正式版" />
-            <Tab key="rolling" title="滚动版" />
-          </Tabs>
-        </SettingItem>
+        {!systemUpdateOnlyBuild && (
+          <SettingItem compatKey="legacy" title="自动检查更新" divider>
+            <Switch
+              size="sm"
+              isSelected={autoCheckUpdate}
+              onValueChange={(v) => {
+                patchAppConfig({ autoCheckUpdate: v })
+              }}
+            />
+          </SettingItem>
+        )}
+        {!systemUpdateOnlyBuild && (
+          <SettingItem compatKey="legacy" title="更新通道" divider>
+            <Tabs
+              size="sm"
+              color="primary"
+              selectedKey={updateChannel}
+              onSelectionChange={async (v) => {
+                patchAppConfig({ updateChannel: v as AppUpdateChannel })
+              }}
+            >
+              <Tab key="stable" title="正式版" />
+              <Tab key="rolling" title="滚动版" />
+            </Tabs>
+          </SettingItem>
+        )}
         <SettingItem compatKey="legacy" title="通知形式" divider>
           <Tabs
             size="sm"
diff --git a/src/shared/build-flags.ts b/src/shared/build-flags.ts
index 7e70293..40c491e 100644
--- a/src/shared/build-flags.ts
+++ b/src/shared/build-flags.ts
@@ -1,3 +1,4 @@
 export const systemCoreDefaultPath = __SPARKLE_SYSTEM_CORE_PATH__
 export const systemCoreOnlyBuild = systemCoreDefaultPath !== ''
 export const systemServicePath = __SPARKLE_SYSTEM_SERVICE_PATH__
+export const systemUpdateOnlyBuild = __SPARKLE_SYSTEM_UPDATE__
diff --git a/src/shared/types/types.d.ts b/src/shared/types/types.d.ts
index af890a2..8e51246 100644
--- a/src/shared/types/types.d.ts
+++ b/src/shared/types/types.d.ts
@@ -1,5 +1,6 @@
 declare const __SPARKLE_SYSTEM_CORE_PATH__: string
 declare const __SPARKLE_SYSTEM_SERVICE_PATH__: string
+declare const __SPARKLE_SYSTEM_UPDATE__: boolean
 
 type OutboundMode = 'rule' | 'global' | 'direct'
 type LogLevel = 'info' | 'debug' | 'warning' | 'error' | 'silent'
-- 
2.55.0

