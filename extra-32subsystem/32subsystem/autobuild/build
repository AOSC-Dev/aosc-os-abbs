echo Working...
mkdir -p "$PKGDIR"/{opt/32/,usr/include/gnu}
for f in /usr/lib/locale /usr/share /etc /var; do
	ln -svt "$(dirname "$PKGDIR/opt/32/${f#/usr/}")" "$f"
done
ln -st "$PKGDIR/usr/include/gnu" /opt/32/include/gnu/stubs-32.h
