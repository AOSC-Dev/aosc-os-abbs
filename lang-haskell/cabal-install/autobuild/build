./bootstrap.sh --sandbox "$SRCDIR"/tempinstall

mkdir -p "$PKGDIR"/usr/share
cp -r "$SRCDIR"/tempinstall/bin "$PKGDIR"/usr/
cp -r "$SRCDIR"/tempinstall/share/man "$PKGDIR"/usr/share/

cp -r "$SRCDIR"/bash-completion "$PKGDIR"/usr/share/
