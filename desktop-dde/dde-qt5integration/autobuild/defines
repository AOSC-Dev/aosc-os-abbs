PKGNAME=dde-qt5integration
PKGSEC=libs
# Note: dde-qt5platform-plugins is not an ELF dependency. However, Deepin
# still marks it as a dependency and many software do not discern this
# correctly as a result (treating dde-qt5platform-plugins as an implicit
# dependency). Keeping this dependency to ease Spiral compatibility.
PKGDEP="dde-qt5platform-plugins dtkwidget libqtxdg xcb-util-renderutil"
BUILDDEP="gtest"
PKGDES="Qt platform theme integration for the Deepin Desktop Environment"
