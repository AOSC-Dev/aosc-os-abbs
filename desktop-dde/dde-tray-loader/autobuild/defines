PKGNAME=dde-tray-loader
PKGSEC=dde
PKGDEP="dde-qt6integration dde-qt6platform-plugins dtk6widget qt-6 wayland x11-lib"
BUILDDEP="cmake extra-cmake-modules ninja"
PKGDES=" A set of tray plugins that integrated into task bar and the tool loader which can load the plugins"

ABTYPE=cmakeninja
CMAKE_AFTER=(
    '-DCMAKE_INSTALL_LIBDIR=lib'
    '-DCMAKE_INSTALL_PREFIX=/usr'
    '-DCMAKE_INSTALL_SYSCONFDIR=/etc'
)
