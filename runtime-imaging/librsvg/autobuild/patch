if ab_match_archgroup retro ; then
	ab_apply_patches \
		"$SRCDIR"/autobuild/patches/*.{patch,diff}.retro
else
	ab_apply_patches \
		"$SRCDIR"/autobuild/patches/*.{patch,diff} \
		"$SRCDIR"/autobuild/patches/*.{patch,diff}."${CROSS:-$ARCH}"
fi
