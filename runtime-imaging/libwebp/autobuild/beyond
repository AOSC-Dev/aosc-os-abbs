abinfo "Creating compatibility symlink for ABI version 6 ..."
# Note: This should be safe with no removed symbols.
#
# Ref: https://abi-laboratory.pro/?view=timeline&l=libwebp
ln -sv libwebp.so.7 \
    "$PKGDIR"/usr/lib/libwebp.so.6
