abinfo "Checking if gobject-introspection is installed ..."
if [ -e /var/lib/dpkg/info/gobject-introspection.list ] ; then
	abinfo "Enabling introspection ..."
	MESON_AFTER+=('-Dintrospection=enabled')
fi
