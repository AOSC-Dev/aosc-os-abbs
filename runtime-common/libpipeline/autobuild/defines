PKGNAME=libpipeline
PKGDES="A C library for manipulating pipelines of subprocesses in a flexible and convenient way"
PKGDEP="glibc"
PKGSEC=libs

RECONF=0

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
