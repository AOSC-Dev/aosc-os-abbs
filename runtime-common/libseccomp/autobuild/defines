PKGNAME=libseccomp
PKGSEC=libs
PKGDEP="glibc"
BUILDDEP="gperf"
PKGDES="Library to interface with the Linux Kernel's syscall filtering mechanism"

NOSTATIC=0

AUTOTOOLS_AFTER=(
    '--enable-static'
)
AUTOTOOLS_AFTER__RETRO=(
    "${AUTOTOOLS_AFTER[@]}"
    '--disable-static'
)
