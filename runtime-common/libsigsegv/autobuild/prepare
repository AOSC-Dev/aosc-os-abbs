abinfo "Disabling optimizations ..."
export CFLAGS="${CFLAGS/-O2/}"

# Strangely this version of libsigsegv doesn't ship a copy of gnulib.
abinfo "Copying gnulib ..."
cp -rv "$SRCDIR"/../gnulib "$SRCDIR"/
