PKGNAME=jansson
PKGDES="C library for encoding, decoding and manipulating JSON data"
PKGDEP="glibc"
PKGSEC=libs

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
