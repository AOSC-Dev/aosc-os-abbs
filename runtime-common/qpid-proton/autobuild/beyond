if [ -d "$PKGDIR"/usr/lib64 ]; then
    abinfo "Correcting /usr/lib64 => /usr/lib ..."
    mv -v "$PKGDIR"/usr/lib{64,}
fi

abinfo "Removing examples (installed unconditionally) ..."
rm -rv "$PKGDIR"/usr/share/proton/examples
