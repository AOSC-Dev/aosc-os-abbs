PKGNAME=utfcpp
PKGSEC=devel
PKGPROV="utf8cpp"
PKGDES="C/C++ library to handle UTF-8 encoded strings"

ABHOST=noarch
CMAKE_AFTER=(
    '-DUTF8_TESTS=OFF'
)

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
