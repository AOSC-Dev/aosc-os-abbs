# FIXME: 
# Workaround: Run CMake to generate pkg-config file but don’t build with it, it’s quite broken
# Link: https://gitlab.archlinux.org/archlinux/packaging/packages/hdf5
# Commit b292d306
abinfo "Generating pkg-config file ..."
cmake -S "$SRCDIR" -B "$SRCDIR"/build \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DHDF5_BUILD_HL_LIB=ON \
    -DBUILD_STATIC_LIBS=ON \

abinfo "Installing pkg-config file ..."
install -Dvm644 "$SRCDIR"/build/CMakeFiles/hdf5{,_hl}.pc -t\
    "$PKGDIR"/usr/lib/pkgconfig/
