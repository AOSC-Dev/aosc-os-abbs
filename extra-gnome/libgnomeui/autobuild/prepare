abinfo "Appending -lm to LDFLAGS to fix build ..."
export LDFLAGS="${LDFLAGS} -lm"

abinfo "Appending -Wno-error=format-nonliteral to CFLAGS to fix build ..."
export CFLAGS="${CFLAGS} -Wno-error=format-nonliteral"
