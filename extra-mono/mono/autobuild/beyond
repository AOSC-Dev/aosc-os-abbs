cd mcs/jay
make
make DESTDIR=$PKGDIR prefix=/usr INSTALL=$SRCDIR/install-sh install
cd ../../

sed -i -e "s:#Requires:Requires:" $PKGDIR/usr/lib/pkgconfig/mono.pc

# Compatibility "bless"
ln -sv /usr/bin/mcs /usr/bin/gmcs
