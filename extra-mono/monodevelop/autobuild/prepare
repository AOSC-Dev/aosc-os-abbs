export MONO_SHARED_DIR=$SRCDIR/src/.wabi
mkdir -p $MONO_SHARED_DIR

git submodule update --init --recursive

patch -Np1 -i $SRCDIR/autobuild/patches/5.10-update-gnome-gconf-path.patch

export AUTOTOOLS_DEF="--prefix=/usr"
