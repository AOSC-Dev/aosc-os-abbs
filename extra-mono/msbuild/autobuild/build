abinfo "Build MSBuild"
make

abinfo "Deploy files"
mkdir -p "$PKGDIR"/usr/bin/
./install-mono-prefix.sh "$PKGDIR"/usr/bin/
mkdir -p "$PKGDIR"/usr/share/doc/"$PKGNAME"
install -Dm644 "$SRCDIR"/LICENCE "$PKGDIR"/usr/share/doc/"$PKGNAME"
