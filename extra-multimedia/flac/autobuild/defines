PKGNAME=flac
PKGDES="FLAC analzying library"
PKGSEC=libs
PKGDEP="libogg"

ABSHADOW=no
if [ "${CROSS:-$ARCH}" = "amd64" ]; then
	AUTOTOOLS_AFTER+=" --enable-sse"
else
	AUTOTOOLS_AFTER+=" --disable-sse"
fi
