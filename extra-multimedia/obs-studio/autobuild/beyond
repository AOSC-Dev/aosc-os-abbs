abinfo "Correcting permissions for the browser plugin (if any) ..."
chmod -v a+x "$PKGDIR"/usr/lib/obs-plugins/libcef.so || true

abinfo "Adding plugin folder to linker search path ..."
install -dv "$PKGDIR"/etc/ld.so.conf.d/
echo "/usr/lib/obs-plugins/" > "$PKGDIR"/etc/ld.so.conf.d/obs.conf
