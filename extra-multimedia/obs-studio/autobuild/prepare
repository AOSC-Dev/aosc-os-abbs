CMAKE_AFTER="-DUSE_SSL=ON
             -DBUILD_BROWSER=OFF
             -DOBS_VERSION_OVERRIDE=${PKGVER}"

abinfo "Setting up module obs-vst"
pushd "${SRCDIR}"/plugins/obs-vst
tar -xvf "${SRCDIR}"/../vst.tar.gz --strip-components=1
popd
