PKGNAME=alsa-plugins
PKGDES="extra ALSA plugins"
PKGSEC=libs
PKGDEP="alsa-lib"
BUILDDEP="speex jack pulseaudio ffmpeg libsamplerate"

# Soft dependencies used, as the plugins are not loaded at runtime if not specified.
ABRPMAUTOPROVONLY=yes
