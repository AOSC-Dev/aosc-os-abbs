PKGNAME=gnash
PKGDES="GNU Flash player"
PKGSEC=video
PKGDEP="jemalloc libjpeg-turbo giflib libpng gtk-2 pango glib atk gst-plugins-base-0-10 cairo sdl curl speex agg boost zlib freetype fontconfig ffmpeg gconf"
BUILDDEP="npapi-sdk"

RECONF=0
AUTOTOOLS_AFTER="--with-npapi-install=system --with-npapi-plugindir=/usr/lib/mozilla/plugins --disable-kparts4 --disable-kparts3 --enable-gui=gtk --with-jemalloc-incl=/usr/include/jemalloc"
ABSHADOW=0
ABTYPE=autotools

if [[ "${CROSS:-$ARCH}" = "powerpc" ]]; then
    AUTOTOOLS_AFTER+=" LIBS=-latomic"
fi
