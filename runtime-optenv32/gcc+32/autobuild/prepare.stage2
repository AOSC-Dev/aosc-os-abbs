abinfo "Extracting gmp..."
tar xvf "$SRCDIR"/../gmp-${__GMP_VER}.tar.xz
mv "$SRCDIR"/gmp-${__GMP_VER} gmp

abinfo "Extracting mpc..."
tar xvf "$SRCDIR"/../mpc-${__MPC_VER}.tar.gz
mv "$SRCDIR"/mpc-${__MPC_VER} mpc

abinfo "Extracting mpfr..."
tar xvf "$SRCDIR"/../mpfr-${__MPFR_VER}.tar.xz
mv "$SRCDIR"/mpfr-${__MPFR_VER} mpfr
