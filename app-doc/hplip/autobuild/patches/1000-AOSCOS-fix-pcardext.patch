--- hplip-3.25.2/pcard/pcardext/pcardext.c.orig	2025-02-27 20:12:29.000000000 +0800
+++ hplip-3.25.2/pcard/pcardext/pcardext.c	2025-05-18 11:18:41.331429107 +0800
@@ -24,6 +24,7 @@ Author: Don Welch
 
 \*****************************************************************************/
 
+#include "modsupport.h"
 #define PY_SSIZE_T_CLEAN
 
 
@@ -47,7 +48,7 @@ PyObject * writesectorFunc = NULL;
 int ReadSector(int sector, int nsector, void *buf, int size)
 {
     PyObject * result;
-    char * result_str;
+    const char * result_str;
     
     if( readsectorFunc )
     {
@@ -59,7 +60,7 @@ int ReadSector(int sector, int nsector,
         if( result )
         {
             Py_ssize_t len = 0;
-            PyString_AsStringAndSize( result, &result_str, &len );
+            result_str = PyUnicode_AsUTF8AndSize( result, &len );
             
             if( len < nsector*FAT_HARDSECT )
             {
@@ -83,7 +84,7 @@ int WriteSector(int sector, int nsector,
     {
         result = PyObject_CallFunction( writesectorFunc, "iis#", sector, nsector, buf, size );
         
-        return PyInt_AS_LONG( result );
+        return PyLong_AsLong( result );
     }
 
     return 1;
@@ -208,7 +209,7 @@ PyObject * pcardext_read( PyObject * sel
     
     if( FatReadFileExt( name, offset, len, buffer ) == len )
     {
-        return PyString_FromStringAndSize( (char *)buffer, len );
+        return PyUnicode_FromStringAndSize( (char *)buffer, len );
     }
     else
     {
@@ -236,11 +237,17 @@ static PyMethodDef pcardext_methods[] =
 
 static char pcardext_documentation[] = "Python extension for HP photocard services";
 
+static PyModuleDef pcardext_custommodule = {
+    .m_base = PyModuleDef_HEAD_INIT,
+    .m_name = "pcardext",
+    .m_doc = pcardext_documentation,
+    .m_size = -1,
+    .m_methods = pcardext_methods
+};
+
 void initpcardext( void )
 {
-    PyObject * mod = Py_InitModule4( "pcardext", pcardext_methods, 
-                                     pcardext_documentation, (PyObject*)NULL, 
-                                     PYTHON_API_VERSION );
+    PyObject * mod = PyModule_Create2(pcardext_custommodule, PYTHON_API_VERSION);
                      
     if (mod == NULL)
       return;
