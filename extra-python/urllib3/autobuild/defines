PKGNAME=urllib3
PKGSEC=python
PKGDEP="python-2 python-3"
BUILDDEP="ndg-httpsclient setuptools"
if [[ "${CROSS:-$ARCH}" = "amd64" ]]; then
    BUILDDEP+=" imagesize sphinx"
fi
PKGDES="HTTP library with thread-safe connection pooling and file post support"

ABTYPE=python
ABHOST=noarch
