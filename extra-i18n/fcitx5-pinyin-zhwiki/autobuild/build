wget https://dumps.wikimedia.org/zhwiki/${dict_ver}/zhwiki-${dict_ver}-all-titles-in-ns0.gz

make FILENAME=zhwiki-${dict_ver}-all-titles-in-ns0
make DESTDIR="$PKGDIR" install

abinfo "installing rime dict to $PKGDIR..."
make DESTDIR="$PKGDIR" install_rime_dict
