# FIXME: /usr/bin/ld: failed to set dynamic section sizes: memory exhausted
if ab_match_archgroup 32bit; then
    abinfo "Appending -Wl,--no-keep-memory to reduce memory footprint during linkage ..."
    export LDFLAGS="${LDFLAGS} -Wl,--no-keep-memory"
fi
