abinfo "Install state machine editor"
install -Dvm755 "$SRCDIR"/abbuild/bin/kdstatemachineeditor \
    "$PKGDIR"/usr/bin/kdstatemachineeditor
install -Dvm644 "$SRCDIR"/data/scxml/* \
    -t "$PKGDIR"/usr/share/kdsme/data/scxml/
