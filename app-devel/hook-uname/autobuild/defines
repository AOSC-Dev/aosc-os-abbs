PKGNAME=hook-uname
PKGSEC=devel
PKGDEP="glibc"
PKGDES="A system-level hack to let kernel report the native architecture"

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
