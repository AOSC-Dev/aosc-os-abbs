abinfo "Building unifdef ..."
make

abinfo "Installing unifdef (executable) ..."
install -Dvm0755 "$SRCDIR"/unifdef \
    "$PKGDIR"/usr/bin/unifdef

abinfo "Installing unifdef (man page) ..."
install -Dvm0644 "$SRCDIR"/unifdef.1 \
    "$PKGDIR"/usr/share/man/man1/unifdef.1
