abinfo "Installing repo ..."
mkdir -pv "$PKGDIR"/usr/{bin,lib/android-repo}
cp -av "$SRCDIR"/repo/* \
    "$PKGDIR"/usr/lib/android-repo/

abinfo "Creating a symlink for the repo executable ..."
ln -sv ../lib/android-repo/repo \
    "$PKGDIR"/usr/bin/repo
