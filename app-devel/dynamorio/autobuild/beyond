abinfo "Moving documentation, samples and binaries to /usr/share/dynamorio ..."
mkdir -vp "$PKGDIR"/usr/share/dynamorio
mv -v "$PKGDIR"/usr/{README,License.txt,ACKNOWLEDGEMENTS} "$PKGDIR"/usr/share/dynamorio/
mv -v "$PKGDIR"/usr/{samples,ext,docs_embed,docs,tools,bin64,lib64,logs,cmake} "$PKGDIR"/usr/share/dynamorio/
