PKGNAME=avr-libc
PKGSEC=devel
PKGDEP="avr-gcc"
PKGDES="C runtime libraries for AVR microcontrollers"

NOSTATIC=0
ABSTRIP=0
AB_FLAGS_O3=1

_PREFIX=/opt/abcross/avr

AUTOTOOLS_AFTER=(
    "--prefix=${_PREFIX}"
    '--host=avr'
    "CC=${_PREFIX}/bin/avr-gcc"
)

ABHOST=noarch
