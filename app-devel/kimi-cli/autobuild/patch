abinfo "Applying patch for architectures except amd64 and arm64 ..."
if ! ab_match_arch amd64 && \
   ! ab_match_arch arm64; then
	patch -Np1 -i "$SRCDIR"/autobuild/patches/0001-delete-web-and-vis-for-architecture-except-amd64-and-arm64.patch \
	           -d "$SRCDIR"
fi
