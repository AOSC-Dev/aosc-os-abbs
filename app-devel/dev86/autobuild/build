abinfo "Start compiling..."
# NOTE: If DIST is empty, options will be encountered.
make PREFIX="$PKGDIR"/usr DIST="/"

abinfo "Start install..."
make install-all

abinfo "Clear useless file..."
rm -v "$PKGDIR"/usr/bin/{as,ld,nm,objdump,size}86
rm -v "$PKGDIR"/usr/share/man/man1/{as,ld}86.1
