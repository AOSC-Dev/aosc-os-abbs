abinfo "Building cosmic-files-applet"
# Follow https://github.com/pop-os/cosmic-files/blob/master/justfile#L46-L47
# Otherwise https://github.com/pop-os/cosmic-files/issues/742
cargo build "${DEFAULT_CARGO_CONFIG[@]}" \
    --release \
    --locked \
    --package cosmic-files-applet

abinfo "Installing cosmic-files ..."
just rootdir="$PKGDIR" install
