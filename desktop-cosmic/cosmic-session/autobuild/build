abinfo "Building cosmic-session ..."
cargo build --release
just rootdir="${PKGDIR}" install
install -Dvm644 ${SRCDIR}/data/dconf/profile/cosmic -t \
                ${PKGDIR}/usr/share/dconf/profile/
