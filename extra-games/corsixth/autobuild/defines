PKGNAME=corsixth
PKGSEC=games
PKGDEP="sdl2 sdl2-mixer freetype ffmpeg lua luafilesystem lpeg wqy-microhei"
BUILDDEP="doxygen"
# Note: do not change the font used above (wqy-microhei), as the special font
# size has compatibility issue with fonts
PKGDES="A Theme Hosptial clone which uses the original game's resource files"

PKGEPOCH=1
