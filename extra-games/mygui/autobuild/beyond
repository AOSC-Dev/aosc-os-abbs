abinfo "Installing libCommonso ..."
install -Dvm775 "$SRCDIR"/abbuild/lib/libCommon.so \
    "${PKGDIR}"/usr/lib/libCommon.so

abinfo "HACK: Setting executable bit for resources.xml ..."
chmod -v +x "$PKGDIR"/usr/bin/resources.xml
