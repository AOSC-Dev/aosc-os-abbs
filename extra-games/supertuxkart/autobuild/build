mkdir build; cd build

cmake .. \
    -DIRRLICHT_DIR=/usr \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DCMAKE_CXX_FLAGS="-fpermissive -lpthread -lm -ldl $CXXFLAGS"

make
make install DESTDIR=$PKGDIR

cd ..
