abinfo "Building tree-sitter-c ..."
make

abinfo "Installing tree-sitter-c ..."
make install \
    DESTDIR="$PKGDIR" \
    PREFIX="/usr"

abinfo "Symlinking the library ..."
install -dv "$PKGDIR"/usr/lib/tree_sitter
ln -sv ../libtree-sitter-c.so "$PKGDIR"/usr/lib/tree_sitter/c.so
