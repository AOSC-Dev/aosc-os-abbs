abinfo "Building tree-sitter-query ..."
make

abinfo "Installing tree-sitter-query ..."
make install \
    DESTDIR="$PKGDIR" \
    PREFIX="/usr"

abinfo "Symlinking the library ..."
install -dv "$PKGDIR"/usr/lib/tree_sitter
ln -sv ../libtree-sitter-query.so "$PKGDIR"/usr/lib/tree_sitter/query.so
