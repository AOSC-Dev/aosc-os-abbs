abinfo "Building tree-sitter-lua ..."
make

abinfo "Installing tree-sitter-lua ..."
make install \
    DESTDIR="$PKGDIR" \
    PREFIX="/usr"

abinfo "Symlinking the library ..."
install -dv "$PKGDIR"/usr/lib/tree_sitter
ln -sv ../libtree-sitter-lua.so "$PKGDIR"/usr/lib/tree_sitter/lua.so
