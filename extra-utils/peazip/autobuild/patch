abinfo Converting files for patching...
dos2unix "${SRCDIR}/rmd160.pas" "${SRCDIR}/tsc.pas"
pushd ${SRCDIR}
abinfo Applying the patches...
for i in "${SRCDIR}"/autobuild/patches/*.patch
do
  patch -Np1 -i $i
done
popd
