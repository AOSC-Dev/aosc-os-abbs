abinfo "Fedora: Makefile expects CFLAGS to contain linker flags ..."
export CFLAGS="${CPPFLAGS} ${CFLAGS} ${LDFLAGS}"

abinfo "Creating directories for make install ..."
mkdir -pv "$PKGDIR"/usr/{bin,share/man/man8}
