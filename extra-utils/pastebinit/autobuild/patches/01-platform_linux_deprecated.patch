diff --git a/root/data/buildkit/building/bin/pastebinit b/bin/pastebinit
index 995cb9af8d..d9ca9e116d 100755
--- a/root/data/buildkit/building/bin/pastebinit
+++ b/bin/pastebinit
@@ -38,7 +38,10 @@ defaultPB = "pastebin.com"
 
 # Now try to override it with a distributor pastebin
 try:
-    import platform
+    if sys.version_info[0] == 3 and sys.version_info[1] > 7:
+        import distro as platform
+    else:
+        import platform
     release = platform.linux_distribution()[0].lower()
     if release == 'debian':
         defaultPB = "paste.debian.net"
