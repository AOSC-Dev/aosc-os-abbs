From 054d85d9fe48a4041081d46e8e70918ebb5bcf98 Mon Sep 17 00:00:00 2001
From: xdBronch <51252236+xdBronch@users.noreply.github.com>
Date: Sat, 15 Nov 2025 14:53:32 -0500
Subject: [PATCH 65/87] sema: improve codegen of for loop safety checks
X-Developer-Signature: v=1; a=openpgp-sha256; l=1864; i=xtexchooser@duck.com;
 h=from:subject; bh=ETKgWgNkAUFcUsP63vDZmm6eoSu7NgwkSpUNn1yyK6c=;
 b=owGbwMvMwCW2U4Ij7wZL9ETG02pJDJkmdo58j9+uijVv2dFdv0Lqrbb3TFnn7MOPs9Jkjt1g+
 VQnw6rcUcrCIMbFICumyFJk2ODNqpPOL7qsXBZmDisTyBAGLk4BmMilU4wMNyTVbG1Mzh3ddC3R
 UJXPM3JKZFTxHB7p6pMtjSvcWHY/YGT4kjPpHlfej+mb5vamH51ooP5k1ttI3mMWCcoR2pNnWyU
 xAQA=
X-Developer-Key: i=xtexchooser@duck.com; a=openpgp;
 fpr=7231804B052C670F15A6771DB918086ED8045B91

(cherry picked from commit 29a1d0c06ff80836d744d2ab5db3626bf17fa0d1)
---
 src/Sema.zig | 13 +++++++++++--
 1 file changed, 11 insertions(+), 2 deletions(-)

diff --git a/src/Sema.zig b/src/Sema.zig
index 43f08562aa91..2dcf6be98fc7 100644
--- a/src/Sema.zig
+++ b/src/Sema.zig
@@ -4550,6 +4550,9 @@ fn zirForLen(sema: *Sema, block: *Block, inst: Zir.Inst.Index) CompileError!Air.
             // This argument is a range.
             const range_start = try sema.resolveInst(zir_arg_pair[0]);
             const range_end = try sema.resolveInst(zir_arg_pair[1]);
+            if (try sema.resolveDefinedValue(block, arg_src, range_start)) |start| {
+                if (try sema.valuesEqual(start, .zero_usize, .usize)) break :l range_end;
+            }
             break :l try sema.analyzeArithmetic(block, .sub, range_end, range_start, arg_src, arg_src, arg_src, true);
         };
         const arg_len = try sema.coerce(block, .usize, arg_len_uncoerced, arg_src);
@@ -4613,12 +4616,18 @@ fn zirForLen(sema: *Sema, block: *Block, inst: Zir.Inst.Index) CompileError!Air.
 
     // Now for the runtime checks.
     if (any_runtime and block.wantSafety()) {
+        var ok: Air.Inst.Ref = .none;
         for (runtime_arg_lens, 0..) |arg_len, i| {
             if (arg_len == .none) continue;
             if (i == len_idx) continue;
-            const ok = try block.addBinOp(.cmp_eq, len, arg_len);
-            try sema.addSafetyCheck(block, src, ok, .for_len_mismatch);
+            const eq = try block.addBinOp(.cmp_eq, len, arg_len);
+            ok = if (ok != .none)
+                try block.addBinOp(.bool_and, ok, eq)
+            else
+                eq;
         }
+        if (ok != .none)
+            try sema.addSafetyCheck(block, src, ok, .for_len_mismatch);
     }
 
     return len;
-- 
2.52.0

