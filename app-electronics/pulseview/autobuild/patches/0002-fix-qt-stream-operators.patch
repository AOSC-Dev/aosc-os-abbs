diff -Naur pulseview-0.4.2/pv/util.cpp pulseview-0.4.2-new/pv/util.cpp
--- pulseview-0.4.2/pv/util.cpp	2020-03-31 13:41:18.000000000 -0700
+++ pulseview-0.4.2-new/pv/util.cpp	2021-02-01 04:09:40.627983844 -0800
@@ -137,7 +137,7 @@
 	QString s;
 	QTextStream ts(&s);
 	if (sign && !v.is_zero())
-		ts << forcesign;
+		ts << Qt::forcesign;
 	ts << qSetRealNumberPrecision(precision) << (v * multiplier);
 	ts << ' ' << prefix << unit;
 
@@ -175,7 +175,7 @@
 	QString s;
 	QTextStream ts(&s);
 	if (sign && (v != 0))
-		ts << forcesign;
+		ts << Qt::forcesign;
 	ts.setRealNumberNotation(QTextStream::FixedNotation);
 	ts.setRealNumberPrecision(precision);
 	ts << (v * multiplier) << ' ' << prefix << unit;
