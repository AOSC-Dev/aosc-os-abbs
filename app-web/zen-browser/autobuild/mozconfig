# FIXME: Temporarily disable wasm-sandboxed libraries due to unresolved build dependencies.
# Re-enable once the missing requirements are identified.
ac_add_options --without-wasm-sandboxed-libraries

# Avoid downloading prebuilt binaries and force a fully source-based build.
ac_add_options --disable-bootstrap
