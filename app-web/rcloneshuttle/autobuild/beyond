abinfo "Installing icon, matainfo and desktop files"
install -Dvm644 "$SRCDIR"/meta/io.github.pieterdd.RcloneShuttle.desktop -t \
    "$PKGDIR"/usr/share/applications/
install -Dvm644 "$SRCDIR"/meta/io.github.pieterdd.RcloneShuttle.metainfo.xml \
    -t "$PKGDIR"/usr/share/metainfo/
install -Dvm644 "$SRCDIR"/meta/io.github.pieterdd.RcloneShuttle.svg -t \
    "$PKGDIR"/usr/share/icons/hicolor/scalable/apps/
