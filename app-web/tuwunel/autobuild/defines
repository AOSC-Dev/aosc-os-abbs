PKGNAME=tuwunel
PKGSEC=web
PKGDES="Matrix homeserver implementation"
PKGDEP="glibc liburing gcc-runtime jemalloc"
BUILDDEP="rustc llvm"
PKGSUG="nginx caddy"

ABTYPE=rust
CARGO_AFTER="--all-features"

# FIXME: disable LTO, for rocksdb symbol not found
NOLTO=1

# FIXME:
# error[E0658]: inline assembly is not stable yet on this architecture
# --> src/core/config/regenerate.rs:161:1
# |
# 161 | #[link_section::section(typed)]
# | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
# |
# = note: see issue #93335 <https://github.com/rust-lang/rust/issues/93335> for more information
# = note: this error originates in the macro `$crate::__weak_section_symbols` which comes from the expansion of the attribute macro `link_section::section` (in Nightly builds, run with -Z macro-backtrace for more info)
# but if try to install nightly Rustc, you will get:
# error: target 'mips64el-unknown-linux-gnuabi64' not found in channel.  Perhaps check https://doc.rust-lang.org/nightly/rustc/platform-support.html for available targets
FAIL_ARCH="@(loongson3|retro)"
