PKGNAME=element-desktop
PKGSEC=web
PKGDEP="sqlcipher"
BUILDDEP="nodejs rustc llvm python-3"
PKGDES="Matrix client for desktop"

USECLANG=1

# FIXME: they use nx to build everything, which does not provide native bindings
# for other architectures
FAIL_ARCH="!(amd64|arm64)"
