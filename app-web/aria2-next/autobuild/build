abinfo "Generate Ninja Configuration..."
cmake "$SRCDIR" "${CMAKE_AFTER[@]}" -GNinja

abinfo "Building binaries ..."
cmake --build .

abinfo "Installing binaries ..."
DESTDIR="$PKGDIR" cmake --install ./source --prefix /usr
