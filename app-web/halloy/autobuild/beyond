abinfo "Installing desktop entry ..."
install -Dvm644 "$SRCDIR/assets/linux/org.squidowl.halloy.appdata.xml" -t "$PKGDIR/usr/share/appdata"
install -Dvm644 "$SRCDIR/assets/linux/org.squidowl.halloy.desktop" -t "$PKGDIR/usr/share/applications"

abinfo "Installing icons ..."
cp -rv "$SRCDIR/assets/linux/icons" "$PKGDIR/usr/share/"
