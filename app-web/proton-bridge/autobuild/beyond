abinfo "Installing proton-bridge icon ..."
# The executable and the icon name in the supplied .desktop file is
# protonmail-bridge instead of proton-bridge.
install -Dvm644 "$SRCDIR/dist/bridge.svg" \
	"$PKGDIR/usr/share/icons/hicolor/scalable/apps/protonmail-bridge.svg"

abinfo "Installing desktop entry ..."
install -Dvm644 "$SRCDIR/dist/proton-bridge.desktop" \
	"$PKGDIR/usr/share/applications/protonmail-bridge.desktop"
