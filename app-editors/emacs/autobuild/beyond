abinfo "Adding .emacs suffix to bundled ctags ..."
mv "$PKGDIR"/usr/bin/ctags{,.emacs}
mv "$PKGDIR"/usr/share/man/man1/ctags.{,emacs.}1.gz

abinfo "Removing info page bundle ..."
rm -v "$PKGDIR"/usr/share/info/info.info.gz
