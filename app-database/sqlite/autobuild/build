abinfo "Creating build directory ..."
mkdir -vp "$BLDDIR"
pushd "$BLDDIR"

abinfo "Configuring SQLite ..."
ab_tostringarray AUTOTOOLS_AFTER
"$SRCDIR"/configure "${AUTOTOOLS_AFTER[@]}"

abinfo "Compiling SQLite ..."
make

abinfo "Installing SQLite ..."
make install DESTDIR="$PKGDIR"

popd
