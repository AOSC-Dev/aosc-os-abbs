PKGNAME=gdbm
PKGDES="GNU Database Manager library"
PKGSEC=libs
PKGDEP="glibc"

# Note: Enable GDBM compatibility libraries, disable when applicable.
AUTOTOOLS_AFTER=(
    '--enable-libgdbm-compat'
)

RECONF=0
