autoreconf -fi

if [[ "${CROSS:-$ARCH}" = arm64 || "${CROSS:-$ARCH}" = powerpc ]]; then
    unset CFLAGS CPPFLAGS CXXFLAGS LDFLAGS
fi

if [[ "${CROSS:-$ARCH}" = powerpc ]]; then
    ln -sv /usr/bin/ld.bfd "$SRCDIR"/ld
    export PATH="$SRCDIR:$PATH"
fi

export optflags="${CPPFLAGS} ${CFLAGS}"
