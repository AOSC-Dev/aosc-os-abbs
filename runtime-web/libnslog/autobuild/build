abinfo "Building libnslog ..."
make \
    PREFIX=/usr \
    LIBDIR=lib \
    COMPONENT_TYPE=lib-shared

abinfo "Installing libnslog ..."
make install \
    PREFIX=/usr \
    LIBDIR=lib \
    COMPONENT_TYPE=lib-shared \
    DESTDIR="$PKGDIR"
