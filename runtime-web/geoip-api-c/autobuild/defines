PKGNAME=geoip-api-c
PKGSEC=libs
PKGDEP="zlib"
PKGDES="Non-DNS IP-to-country resolver C library and utilities"

AUTOTOOLS_AFTER="--sysconfdir=/etc/geoip \
                 --mandir=/usr/share/man"

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
