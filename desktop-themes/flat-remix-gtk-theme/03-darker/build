abinfo "Installing Flat Remix GTK Theme Darker..."
# Ensure these directories exist

mkdir -pv "$PKGDIR"/usr/share/themes

# Copy theme to destdir
# We do not use `make install' because it will install every theme and result in a mess

cp -arv "$SRCDIR"/Flat-Remix-GTK-{Blue,Green,Red,Yellow}-Darker "$PKGDIR"/usr/share/themes
cp -arv "$SRCDIR"/Flat-Remix-GTK-{Blue,Green,Red,Yellow}-Darker-Solid "$PKGDIR"/usr/share/themes
