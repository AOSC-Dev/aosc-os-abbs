abinfo "Installing Flat Remix GTK Theme Light..."
# Ensure these directories exist

mkdir -pv "$PKGDIR"/usr/share/themes

# Copy theme to destdir
# We do not use `make install' because it will install every theme and result in a mess

cp -arv "$SRCDIR"/themes/Flat-Remix-GTK-{Black,Blue,Brown,Cyan,Green,Grey,Magenta,Orange,Red,Teal,Violet,Yellow}-Light "$PKGDIR"/usr/share/themes
cp -arv "$SRCDIR"/themes/Flat-Remix-GTK-{Black,Blue,Brown,Cyan,Green,Grey,Magenta,Orange,Red,Teal,Violet,Yellow}-Light-Solid "$PKGDIR"/usr/share/themes
