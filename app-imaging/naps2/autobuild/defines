PKGNAME=naps2
PKGSEC=graphics
PKGDES="A document scanning application"
PKGDEP="gtk-3 sane-backends"
BUILDDEP="dotnet-sdk-10.0 grpc protobuf"

# FIXME: dotnet doesnt support mips, and ppc64el's 
#        different CLR impl causes failed to compile
FAIL_ARCH="(loongson3|ppc64el)"

# FIXME: naps2 must be built in self-contained mode, 
#        ABSTRIP can not process it correctly.
ABSTRIP=0
