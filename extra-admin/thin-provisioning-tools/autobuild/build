autoconf

./configure --prefix=/usr

make

make DESTDIR="${PKGDIR}" BINDIR="${PKGDIR}"/usr/bin MANDIR=/usr/share/man install

abinfo "Building additional Rust tools..."
make DESTDIR="${PKGDIR}" BINDIR="${PKGDIR}"/usr/bin MANDIR=/usr/share/man install-rust-tools
