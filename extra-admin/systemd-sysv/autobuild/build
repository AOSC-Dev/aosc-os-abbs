install -dm755 "$PKGDIR"/{,usr}/bin
for tool in runlevel reboot shutdown poweroff halt telinit; do
  ln -s "/usr/bin/systemctl" "$PKGDIR"/usr/bin/$tool
done

ln -sv /usr/lib/systemd/systemd "$PKGDIR"/bin/init
