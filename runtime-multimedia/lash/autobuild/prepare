abinfo "Appending /usr/include/tirpc to include paths ..."
export CFLAGS="${CFLAGS} -I/usr/include/tirpc"

abinfo "Appening -ldl, -lm, -lreadline, -ltirpc to linkage ..."
export LDFLAGS="${LDFLAGS} -ldl -lm -lreadline -ltirpc"

