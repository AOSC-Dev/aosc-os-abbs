PKGNAME=aom
PKGDES="AV1 Video Codec Library"
PKGDEP="gcc-runtime"
BUILDDEP__AMD64="${BUILDDEP} yasm"
BUILDDEP__I486="${BUILDDEP} yasm"
PKGSEC="libs"

ABTYPE=cmakeninja
CMAKE_AFTER=(
    "-DBUILD_SHARED_LIBS=1"
    "-DENABLE_DOCS=OFF"
)
# Note: ARMv4 is not a NEON target (obviously).
CMAKE_AFTER__ARMV4=(
    "${CMAKE_AFTER[@]}"
    '-DENABLE_NEON=OFF'
    '-DENABLE_NEON_DOTPROD=OFF'
    '-DENABLE_NEON_I8MM=OFF'
)

# FIXME: lto1: fatal error: target specific builtin not available
NOLTO__RISCV64=1

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
