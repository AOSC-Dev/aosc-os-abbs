PKGNAME=libfreeaptx
PKGSEC=sound
PKGDES="Free implementation of Audio Processing Technology codec (aptX)"
PKGDEP="glibc"

# Needed by Afterglow, whereas FAIL_ARCH defaults to mainline-only.
FAIL_ARCH="!(mainline|retro)"
