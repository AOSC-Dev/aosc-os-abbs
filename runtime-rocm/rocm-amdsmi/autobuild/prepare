abinfo "Adding ROCm LLVM to PATH ..."
export PATH="/usr/lib/rocm/lib/llvm/bin:$PATH"

BUILD_READY(){
  abinfo "Patch esmi_ib_library ..."
  cd "$SRCDIR"/esmi_ib_library
  ab_apply_patches "$SRCDIR"/autobuild/patches/0001-esmi-CPUID-check.patch
  cd "$SRCDIR"/abbuild
}
