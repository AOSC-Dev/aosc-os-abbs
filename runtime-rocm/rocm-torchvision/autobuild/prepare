abinfo "Setting ROCM_PATH ..."
export ROCM_PATH=/usr/lib/rocm

abinfo "Adding ROCm LLVM to PATH ..."
export PATH="/usr/lib/rocm/lib/llvm/bin:$PATH"

abinfo "Adding ROCm to PATH ..."
export PATH="/usr/lib/rocm/bin:$PATH"

abinfo "Use LLD for Link ..."
export LDFLAGS="${LDFLAGS} -fuse-ld=lld"

abinfo "Use clang/clang++ compile ..."
export CC="clang"
export CXX="clang++"

abinfo "Fix duplicate symbol ..."
# FIXME: ld.lld: error: duplicate symbol: vision::cuda_version()
# >>> defined in build/temp.linux-loongarch64-cpython-314torchvision/csrc/vision.o
# >>> defined in build/temp.linux-loongarch64-cpython-314torchvision/csrc/vision_hip.o
# See https://github.com/pytorch/vision/pull/9565#discussion_r3652055596
export FORCE_CUDA=1
