abinfo "Start Project Configure ..."
python3 $SRCDIR/setup.py build
python3 $SRCDIR/setup.py bdist_wheel

abinfo "Install PyTorch ..."
python3 -m pip install $SRCDIR/dist/torch-*.whl \
    --root="$PKGDIR" \
    --prefix="/usr" \
    --no-deps \
    --ignore-installed
