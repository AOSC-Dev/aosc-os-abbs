PKGNAME=libcddb+32
PKGDES="Library that implements the different protocols (CDDBP, HTTP, SMTP) to access data on a CDDB server (e.g. http://freedb.org) (optenv32)"
PKGDEP="glibc+32"
PKGSEC=libs
NOLTO=yes
ABHOST=noarch
