#!/bin/bash

# Read SCRCPY_LAUNCH_OPTIONS from /etc/discord-launcher.conf
source /etc/scrcpy-launcher.conf

# Launch Discord ...
/usr/bin/scrcpy \
    $SCRCPY_LAUNCH_OPTIONS_DISTRO $SCRCPY_LAUNCH_OPTIONS "$@"
