PKGNAME=nodejs
PKGSEC=devel
PKGDEP="icu python-2 zlib"
PKGDES="Node.js is a JavaScript runtime built on Chrome's V8 JavaScript engine"

if [[ "${ARCH}" == "mips64el" ]]; then
    MIPS64CONF="--with-mips-arch-variant=r2 --with-mips-fpu-mode=fp64"
fi

AB_FLAGS_O3=1
PKGEPOCH=2
