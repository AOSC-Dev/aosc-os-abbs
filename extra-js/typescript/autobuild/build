yarn global add "typescript@${PKGVER}" --prefix "$PKGDIR"/usr --global-folder "$PKGDIR"/usr/lib/
# remove Yarn's cache (caches might be useful if you install tons of npm packages to system)
rm -f "$PKGDIR"/usr/lib/{package.json,yarn.lock,.bin,.yarn-integrity}

