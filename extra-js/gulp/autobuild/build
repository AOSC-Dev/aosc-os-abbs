# FIXME: reuse of tarball.
npm install -g --user root \
            --prefix $PKGDIR/usr /var/lib/abbs/tarballs/gulp-$PKGVER.tgz
rm -r $PKGDIR/usr/etc

find "$PKGDIR"/usr -type d -exec chmod 755 '{}' +
