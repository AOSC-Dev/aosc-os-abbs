#  Build recipe for maxima
## It discoverd that when using autobuild3's autotools.sh script to handle
## the building process, some files of maxima will become missing and therefore
## lead to a FTBFS. 

abinfo "Running configure ..."
"$SRCDIR"/configure $AUTOTOOLS_AFTER --enable-option-checking=fatal

abinfo "Building project ..."
make V=1 

abinfo "Installing files ..."
make V=1 install DESTDIR="$PKGDIR"
